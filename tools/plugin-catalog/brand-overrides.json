{
  "_comment": [
    "\u00a725 brand-icon corrections for the generated plugin catalog.",
    "The generator auto-matches a plugin's name, then its repo owner, against the",
    "real simple-icons slugs parsed out of node_modules/simple-icons-font. Auto-match",
    "is right about 45% of the time and WRONG often enough to need this file: a logo",
    "reads as a claim about who published the plugin, so a plausible-but-wrong one is",
    "worse than the neutral monogram BrandMark falls back to.",
    "",
    "Key = plugin name. Value = a simple-icons class, or null to force the monogram.",
    "Overrides always win over auto-match. Every value is validated against the real",
    "slug set by tests/plugin-catalog.test.ts, so a typo fails the suite rather than",
    "shipping a blank square."
  ],
  "overrides": {
    "circle-skills": "si-circleci",
    "context7": "si-upstash",
    "aws-serverless": null,
    "informatica-for-claude-platform": null
  },
  "_notes": {
    "circle-skills": "auto-match picked si-circle, a payments company; this is CircleCI",
    "context7": "a product of Upstash; simple-icons has no si-context7",
    "aws-serverless": "auto-match picked si-serverless, which is a different vendor. simple-icons ships no AWS mark at all \u2014 it was removed in v13's 'request permission, or remove' batch (see BrandMark.tsx) \u2014 so this takes the monogram rather than someone else's logo.",
    "informatica-for-claude-platform": "auto-match picked si-claude off the 'claude' word in the name \u2014 Anthropic's mark on Informatica's plugin is a false publisher claim; simple-icons ships no Informatica mark, so monogram."
  }
}
