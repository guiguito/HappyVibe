# D1 Validation — extension_ui_request over RPC (Gate V4)

> **Re-audited 2026-08-02 against `@earendil-works/pi-coding-agent@0.83.0` — NO DRIFT.**
> Checked field-by-field against `dist/modes/rpc/rpc-types.d.ts`:
> `RpcExtensionUIRequest` still has `select {title, options[]}`, `confirm {title, message}`,
> `input {title, placeholder?}` (all with an optional `timeout?` we deliberately never set —
> permission prompts do not time out); `RpcExtensionUIResponse` still has all three variants
> `{value}` / `{confirmed}` / `{cancelled:true}`. `method:"notify"` still present (:415), and
> every event the bridge relies on still exists: `tool_call`, `tool_execution_start|update|end`,
> `session_start`, `turn_start`, `agent_end`, `queue_update`.
> Nothing in this document needed changing. Re-stamp on the next pin bump.

## Verdict: YES — Path A confirmed

`ctx.ui.confirm()` surfaces over RPC as `extension_ui_request` on stdout and is answered by writing `extension_ui_response` to stdin. The full round-trip was proven empirically.

## Verbatim `extension_ui_request` observed on Pi's stdout

```json
{"type":"extension_ui_request","id":"2a300a28-3aaf-4cae-ba55-ffe8de8401f6","method":"confirm","title":"D1-PROBE","message":"Reply to prove ui-over-rpc works"}
```

## Verbatim working `extension_ui_response` written to Pi's stdin

```json
{"type":"extension_ui_response","id":"2a300a28-3aaf-4cae-ba55-ffe8de8401f6","confirmed":true}
```

## Extension stderr output proving round-trip completion

```
D1-PROBE-RESULT: true
```

## Wire shape summary

| Direction | Key fields |
|-----------|-----------|
| stdout (Pi → client) | `type: "extension_ui_request"`, `id: string`, `method: "confirm"`, `title: string`, `message: string` |
| stdin (client → Pi) | `type: "extension_ui_response"`, `id: string`, `confirmed: boolean` |

**Critical**: For `confirm` dialogs, the response field is `confirmed: boolean` (NOT `value`). This is per `RpcExtensionUIResponse` in `rpc-types.d.ts`.

## select shape — PROVEN empirically by Task 6 (V5-core)

**Verbatim `extension_ui_request` for select observed in bridge.test.ts (2026-07-03):**

```json
{"type":"extension_ui_request","id":"43c31d65-5e14-48b5-a437-c585f3249c69","method":"select","title":"{\"kind\":\"hv.permission\",\"tool\":\"bash\",\"summary\":\"touch forbidden.txt\"}","options":["Allow","Allow for session","Deny"]}
```

**Working `extension_ui_response` for select:**

```json
{"type":"extension_ui_response","id":"43c31d65-5e14-48b5-a437-c585f3249c69","value":"Deny"}
```

**v5 — workspace confinement.** The pure engine (`hv-rules.ts`) gained a
`Verdict.source` value `"outside-workspace"` (action `"ask"`, plus `outsidePath`):
a FILE tool (`FILE_TOOLS` = read/write/edit/multi_edit/grep/glob/ls/list; **not**
bash) whose path arg escapes the workspace root asks by default — even a read that
`SAFE_TOOLS` would otherwise allow. Explicit rules still win (checked first);
`escapesWorkspace(p, ws)` is posix-only and does not realpath (symlink caveat).
When this fires, the `hv.permission` title carries `reason:"outside-workspace"`
and `path:<offending path>` so the modal shows the factual path; an in-memory
"Allow for session" grant covers it just like a default ask.

The response field for `select` is `value: string` — the string value of the chosen option (e.g., `"Deny"`, `"Allow"`, `"Allow for session"`). This matches `rpc-types.d.ts` and is now empirically confirmed. Deny blocked the bash tool call (forbidden.txt was NOT created) and `agent_end` still arrived (agent continued gracefully).

## Files

- Probe extension: `scripts/d1-probe-ext.ts`
- Probe driver: `scripts/d1-probe.mjs`

## Adaptation note — JSONL reader timing

During `session_start`, Pi's JSONL stdin reader is not yet attached (it's set up synchronously after `rebindSession()` returns). Calling `ctx.ui.confirm()` directly inside the `session_start` handler creates a pending promise with no active stdin reader — the Node.js event loop empties and Pi exits with code 0 before processing any response.

**Fix**: The probe extension fires the confirm asynchronously (detached via `void (async () => {...})()`) and returns synchronously from `session_start`. A 50ms delay ensures `rebindSession()` has completed and the JSONL reader is attached before `confirm()` emits `extension_ui_request`. Task 6/10 implementations MUST use the same async-detached pattern (or hook a later lifecycle event) when calling `ctx.ui.*` from RPC mode.

## Date

2026-07-03

## B4 wire shapes — hv.audit / hv.rules / hv.dangerous (2026-07-05)

All three ride `ctx.ui.notify`, which surfaces over RPC as a **fire-and-forget** `extension_ui_request` with `method: "notify"` — no `extension_ui_response` is ever written (responding is a protocol error; main must only parse). The JSON payload rides the `message` field (notify carries JSON in `message`; input/select carry it in `title` — same convention as `hv.auth`).

### hv.audit — one notify per permission decision (bridge → main → EventLog)

**Verbatim `extension_ui_request` observed in rules-bridge.test.ts (real RPC, rule-deny of `touch forbidden.txt`):**

```json
{"type":"extension_ui_request","id":"…","method":"notify","message":"{\"kind\":\"hv.audit\",\"ts\":\"2026-07-05T…Z\",\"tool\":\"bash\",\"summary\":\"touch forbidden.txt\",\"decision\":\"deny\",\"source\":\"rule\",\"rule\":{\"layer\":\"command\",\"pattern\":\"touch *\",\"action\":\"deny\",\"scope\":\"global\"}}","notifyType":"info"}
```

Payload fields:

| field | values |
|-------|--------|
| `kind` | `"hv.audit"` (constant) |
| `ts` | ISO 8601, written by the bridge at decision time |
| `tool` | Pi tool name (`bash`, `write`, …) |
| `summary` | bash command or JSON'd input, ≤300 chars |
| `decision` | `"allow"` \| `"allow-session"` \| `"deny"` |
| `source` | `"rule"` \| `"user"` \| `"bypass"` \| `"safe-default"` \| `"plan"` \| `"terminal"` |
| `rule?` | `{layer, pattern, action, scope: "global"\|"workspace"}` — the matching rule; on a bypassed row, the rule that WOULD have matched |
| `grant?` | `"session"` — user allow riding an earlier "Allow for session" grant |
| `wouldHave?` | `"allow"` \| `"ask"` \| `"deny"` — round 15, bypassed rows only |

Main (`src/main/ipc.ts`) intercepts these before renderer forwarding and appends them to the EventLog as `type: "permission.decision"` with `sessionId`/`workspaceId` — the payload above becomes the event's `data`.

**Round 15 — `dangerous` became `bypass`, and a bypassed row records the verdict it overrode.** With a bypass active every single call logged `source:"dangerous"`, rendered in red, so the column named the *mode* once per row and distinguished nothing — reported as "everything ends up being marked as dangerous so this info ends up being useless". Two changes: the value is now `bypass` (calm colour; red is reserved for what a command DOES, i.e. the card's destructive badge), and the branch runs the rule engine anyway and reports `wouldHave` — the verdict a bypass-free session would have reached. `wouldHave` is the ENGINE's `RuleAction`, which has an `"ask"` that `decision` does not: a prompt is not an outcome, and mapping it onto one would be the exact lie the field exists to prevent.

**Old logs keep the old string.** Nothing rewrites history, so `AuditView` maps both `dangerous` and `bypass` to one label and one filter value, and `analytics.ts` folds them into a single `bySource` bucket — otherwise one fact appears as two half-sized buckets either side of the rename. Pinned by `tests/oneshot-audit.test.ts`.

### assistant.oneshot — the app's own model calls (round 15)

`src/main/oneShotLog.ts`, appended directly by main (no bridge involvement — these calls have no session and therefore no extension host).

```json
{"kind":"commit-message","model":"deepseek/deepseek-v4-flash","estTokens":1050,"ok":true}
```

| field | values |
|-------|--------|
| `kind` | `"title"` \| `"agents-md"` \| `"commit-message"` \| `"pr-draft"` |
| `model` | `provider/modelId`, resolved at call time |
| `estTokens` | `ceil((promptChars + outputChars) / 4)` — a rule of thumb, labelled estimated everywhere it renders |
| `ok` | false when the child errored, exited non-zero, or produced nothing usable |

Four callers, all `pi -p --no-session --no-tools --no-extensions`: `titles.ts`, `agentsMd.ts`, and `gitMessage.ts` twice. Each takes an `onDone` callback rather than importing the log, so the modules stay pure and testable; `ipc.ts` binds them with the right `kind` and workspace.

**No dollars, deliberately.** §19's ledger reads Pi's own per-call `usage.cost` out of a session file; a sessionless call has none, so a price here would have to come from a table main does not have and must not grow a second copy of. That is §19 ruling 3's "unknown price rendered as a number" failure one surface over. Tokens are an honest estimate; a dollar figure would not be. Surfaced on the **Audit log** page (interleaved with permission rows by timestamp — one page, because "what ran and did I see it?" is one question) and as one line on **Stats**, outside the cost total.

### hv.rules — /hv-rules-reload acknowledgment

Rules file (`permission-rules.json` in userData, `{global: Rule[], workspaces: Record<absWorkspacePath, Rule[]>}`) reaches the bridge via the `HV_RULES_FILE` spawn env var. Main rewrites the file on UI edits then broadcasts the `/hv-rules-reload` slash command (fire-and-forget RPC prompt) to every live session.

```json
{"kind":"hv.rules","stage":"loaded","global":1,"workspaces":1}
{"kind":"hv.rules","stage":"error","message":"<parse error>"}
```

On parse error the bridge KEEPS the previous ruleset — a torn write never fails open.

### hv.dangerous — /hv-dangerous on|off acknowledgment

```json
{"kind":"hv.dangerous","on":true}
{"kind":"hv.dangerous","stage":"error","message":"Usage: /hv-dangerous on|off"}
```

Per-session, in-bridge-memory only — never persisted, always off after respawn. The renderer drives its warning banner from this notify (forwarded through `hv:ui-request` like all non-audit requests).

Contract coverage: `tests/rules-bridge.test.ts` (reload + dangerous need no model; the rule-deny hv.audit contract is DEEPSEEK-gated like bridge.test.ts).

## B5 wire shapes — hv.context / hv-context-marks (2026-07-07)

Context visibility rides the same fire-and-forget `ctx.ui.notify` channel as B4
(JSON in the `message` field; the renderer parses it and NEVER opens the modal).
Three `/hv-context*` bridge commands are driven from main via RPC `prompt`
(B3/B4 precedent); their replies stream back as `hv.context` notifies.

### Mark identity (the crux)

The panel identifies items by session-entry id (from `get_entries`), but the
`context` event — the only place removal takes effect — hands the bridge an
`AgentMessage[]` with NO entry ids. The only ids present in BOTH are:

- `toolCall.id` / `toolResult.toolCallId` → mark key `tool:<id>` (a pair)
- `message.timestamp` → mark key `msg:<ts>` (plain user/assistant messages)

So the renderer sends **mark keys**, not entry ids. Pairing is automatic:
`tool:<id>` drops the assistant's toolCall block AND the matching toolResult
(atomic), so selecting either half removes both.

### hv.context — snapshot (reply to /hv-context)

```json
{"kind":"hv.context","stage":"snapshot",
 "system":{"chars":8123,"estTokens":2031,"toolCount":4,
   "contextFiles":[{"path":"/ws/AGENTS.md","chars":512,"estTokens":128}],
   "toolDefs":[{"name":"read","chars":420},{"name":"bash","chars":880}]},
 "items":[
   {"markKey":"msg:1730000000000","entryId":"e1","group":"conversation","role":"user","preview":"turn one","chars":8,"estTokens":2,"removable":true},
   {"markKey":"tool:call-A","entryId":"e2","group":"tool","role":"assistant","toolName":"bash","toolCallId":"call-A","preview":"bash({\"command\":\"ls\"})","chars":21,"estTokens":6,"usage":{"input":100,"output":20,"total":120},"removable":true},
   {"markKey":"tool:call-A","entryId":"e3","group":"tool","role":"toolResult","toolName":"bash","toolCallId":"call-A","preview":"ok","chars":2,"estTokens":1,"removable":true}
 ],
 "marks":["tool:call-A"]}
```

- `system` is `null` until the first turn runs (captured at `before_agent_start`).
- Item sizes (`chars`/`estTokens`) are char-based ESTIMATES (≈ chars/4), labeled
  as such in the UI; `usage.total` on assistant items is MEASURED.
- `removable` reflects the completed-turn gate (s0.3): items at/after the last
  user message (the in-flight turn) are `false`. The bridge refuses to mark them.
- **v5:** `system.toolDefs[]` gives per-tool ESTIMATED sizes (from the LLM tool
  spec, ≈ chars/4) so the panel can drill into tool definitions with a total
  instead of "size not measured". `bashExecution` message entries now group as
  `tool` (not `other`), and the renderer hides any empty group — so "Other" only
  appears when a genuinely unknown entry type shows up.

### hv.context — removed / restored (reply to /hv-context-remove | -restore)

```json
{"kind":"hv.context","stage":"removed","accepted":["tool:call-A"],"refused":["tool:call-B"],"marks":["tool:call-A"]}
{"kind":"hv.context","stage":"restored","restored":["tool:call-A"],"marks":[]}
```

`refused` = keys rejected by the completed-turn gate (in-flight or unknown);
`notifyType` is `"warning"` when anything was refused. `marks` is the full
current kill-set after the operation.

### hv-context-marks — persistence (pi.appendEntry)

The kill-set is persisted as `custom` entries of type `hv-context-marks` with
`data: {marks: MarkKey[]}` — a full snapshot, newest wins. These do NOT enter
LLM context. Restored on `session_start` (survives reload + resume), and
re-appended in `session_before_compact` so they survive compaction. The bridge's
`context` handler applies the set via `filterMessages` (non-destructive — the
session file is never mutated, proven in s0.3).

### Compaction

Main fires the RPC `compact` command (`hv:compact-session`); Pi streams
`compaction_start` / `compaction_end` events (forwarded as `hv:pi-event`),
rendered as transcript notices. The red-zone auto-suggest banner and the
[Compact now] dialog are renderer-only; compaction is NEVER automatic.

Contract coverage: `tests/context-bridge.test.ts` (snapshot shape + in-flight
refusal need no model; the remove→resume-survival guarantee is DEEPSEEK-gated —
it proves the mark persists across a full Pi process restart from the session
file, i.e. the same `pi.appendEntry` + `restoreMarks` machinery
`session_before_compact` re-invokes; a manual `compact` refuses on a session
that small, so persistence is proven deterministically via resume). Pure logic
(marks/pairing/completed-turn gate + gauge/grouping/parsing) is unit-tested in
`tests/hv-context.test.ts` and `tests/context-renderer.test.ts`.

## B6 wire shapes — hv.agents / hv.tools + subagent trace (2026-07-07)

`pi-subagents@0.33.1` is loaded as a SECOND `-e` extension alongside the bridge
(`spawn.ts` PI_SUBAGENTS_RELPATH); the child Pi it spawns for delegation is
pointed at the embedded bin via `PI_SUBAGENT_PI_BINARY` (spawn env, always set —
`node_modules/.bin/pi`). Agents are discovered from `<PI_CODING_AGENT_DIR>/agents/*.md`
(→ source `builtin`, the app-owned dir) and `<cwd>/.pi/agents/*.md` (→ `project`).

`hv.agents` / `hv.tools` ride the fire-and-forget `ctx.ui.notify` channel (JSON
in `message`, like B4/B5); the renderer parses them and NEVER opens the modal.
Both are driven from main via RPC `prompt` (`/hv-agents`, `/hv-tools`).

### hv.agents — agent inventory (reply to /hv-agents)

```json
{"kind":"hv.agents","agents":[
  {"name":"code-explorer","description":"Read-only codebase investigator…","tools":["read","grep","glob","list","ls"],"source":"builtin","path":"/…/pi-agent/agents/code-explorer.md"},
  {"name":"agents-md-maker","description":"Drafts AGENTS.md files…","tools":["read","grep","glob","list","ls"],"source":"builtin","path":"/…/pi-agent/agents/agents-md-maker.md"}
]}
```

**v5 — two builtins.** The Summarizer agent was removed (compaction is Pi-native);
`installBuiltinAgents` uninstalls an unedited previously-installed copy. The
**agents-md-maker** is now read-only + structured: its final message is one fenced
block tagged ` ```json agents-md ` with `{"files": {"AGENTS.md": "...", "pkg/AGENTS.md": "..."}}`
(root + optional nested). The renderer parses it (`parseAgentsMdOutput`) and calls
`hv:write-agents-md-files`; **main** writes each file path-confined (basename must be
`AGENTS.md`, no escape) and appends an `agents_md.written` EventLog entry.
Unparseable output falls back to a single root draft.

The bridge enumerates the two agent dirs itself (readdir + flat-frontmatter
parse in `hv-agents.ts`) rather than the pi-subagents `subagent {action:"list"}`
result, so the shape is exactly `{name, description, tools?, model?, source, path}`.
`model?` is the agent-tier override (frontmatter `model:`) when present.

### hv.tools — tool inventory (reply to /hv-tools)

```json
{"kind":"hv.tools","tools":[
  {"name":"bash","description":"Run a shell command","source":"builtin"},
  {"name":"subagent","description":"Delegate to a subagent…","source":"pi-subagents"}
]}
```

Built from `pi.getAllTools()` (extension side — RPC `get_commands` does NOT list
built-in LLM tools). Permission state is joined RENDERER-side via `hv:eval-rules`
(one call per tool, empty input → tool-layer verdict) — the SAME `evaluate()` the
bridge's `tool_call` gate runs, so the rules logic is never forked.

### Subagent delegation trace (ordinary tool_execution_* events, toolName "subagent")

The subagent tool is a normal `tool_call` → the B4 bridge permission gate applies
to it (gate `subagent` via a tool-layer rule). Observed shapes on
pi-subagents 0.33.1 (differs from s0.3's prediction — the transcript is in the
UPDATE, the outcome in the END):

- `tool_execution_start` — `toolName: "subagent"`, `args: {agent, task}`.
- `tool_execution_update.partialResult.details.results[]` — **LIVE child transcript**:
  each result `{agent, task, exitCode, messages: AgentMessage[]}`. `messages` is
  the child's full turn-by-turn transcript (user/assistant/thinking/toolCall blocks).
- `tool_execution_end.result.details.results[]` — **FINAL outcome, NO transcript**:
  each result `{agent, exitCode, finalOutput, usage?, modelAttempts:[{model, usage:{input,output,cost,turns}, exitCode}], artifactPaths:{inputPath,outputPath,jsonlPath,transcriptPath,metadataPath}, progressSummary}`.
  Artifacts land under `<cwd>/.pi-subagents/artifacts/`.

The renderer keeps the update's `messages` and merges the end's model/usage/finalOutput
onto them (`mergeTrace`), rendering a nested collapsible mini-conversation
distinct from a normal tool card.

## queue_update — margin note (2026-07-12)

`queue_update` (`{type:"queue_update", steering: string[], followUp: string[]}`, mirrored by `src/renderer/src/queue.ts`) is read-only state: Pi 0.80.3 exposes no dequeue RPC, so queued messages can be displayed but never removed — upstream feature request is the path.

## B7 — hv:get-analytics (internal IPC shape, NOT a Pi wire contract) (2026-07-10)

Local-only analytics. Main reads the whole EventLog (`events.jsonl`) and returns
a pure aggregate computed by `src/main/analytics.ts` (`aggregate()`); NOTHING is
ever sent off the machine (PRD: local-only telemetry). Additive `hv.*` method,
no Pi involvement — this is a renderer↔main shape only.

`hv:get-analytics({workspaceId?, sinceTs?})` → `Analytics`:

```json
{"totalSessions":12,"openSessions":1,"crashes":0,
 "tokens":{"input":40213,"output":18022},"cost":0.83,
 "duration":{"avgMs":184000,"medianMs":120000,"count":11},
 "sessionsPerDay":[{"date":"2026-07-09","count":5},{"date":"2026-07-10","count":7}],
 "perWorkspace":[{"key":"/ws/app","sessions":9,"tokens":51000,"cost":0.7}],
 "perModel":[{"key":"deepseek-chat","sessions":9,"tokens":51000,"cost":0.7}],
 "permissions":{"total":30,"byDecision":{"allow":22,"deny":8},"bySource":{"rule":18,"user":12}}}
```

- Sources: `session.start`/`session.end`/`session.crash`/`permission.decision`.
- Tokens/cost/per-workspace/per-model come only from `session.end`'s `data.stats`
  (get_session_stats payload); a start with no matching end is `openSessions` and
  adds no duration. `perModel` is empty unless the stats payload carries `model`.
- Cost is an ESTIMATE (labeled as such in the UI). `duration` derives from
  start→end ts pairs by sessionId (skew/negatives dropped).
- Onboarding "seen the wow-flow" flag: `hv:get-onboarding-seen` /
  `hv:set-onboarding-seen(bool)`, persisted in `config.json` (userData) as
  `onboardingSeen`. Shown once on a brand-new user's first session, re-openable
  from the sidebar Help affordance.

Contract coverage: `tests/analytics.test.ts` (pure aggregation: duration pairing,
token/cost sums, workspace + model grouping, open-session handling, empty log,
filters, corrupt-line resilience over a real EventLog) and
`tests/analytics-format.test.ts` (duration/number/cost formatters).

## W1.4 wire shape — hv.sysprompt (2026-07-12)

Read-only resolved system prompt for Settings → System Prompt. Rides the same
fire-and-forget `ctx.ui.notify` channel as B4/B5 (JSON in `message`; the
renderer parses it and NEVER opens the modal). Driven from main via RPC
`prompt` (`/hv-sysprompt`) on the focused session's Pi, falling back to the
utility client.

### hv.sysprompt — reply to /hv-sysprompt

```json
{"kind":"hv.sysprompt","text":"You are a coding agent…<full resolved prompt>"}
{"kind":"hv.sysprompt","text":null}
```

`text` is the full `event.systemPrompt` string captured by the bridge at
`before_agent_start` (the same capture point as B5's `systemBlock`) — i.e. the
prompt Pi actually resolved for the LAST turn, including APPEND_SYSTEM.md
layers. `null` until a turn has run in that Pi process (the utility client
never runs one, so Settings shows a "start a session" note).

### APPEND_SYSTEM.md additions layer (no Pi wire — main-side confined writers)

Global `<agentDir>/APPEND_SYSTEM.md` and per-workspace
`<workspace>/.pi/APPEND_SYSTEM.md` are written by `src/main/appendSystem.ts`
(path-confined like agentsMd.ts; blank content deletes the file). Pi reads
them at session load only → edits apply to new/restarted sessions. The
workspace file (trusted project) REPLACES the global one — NOT concatenated —
and the UI copy states that. Per-workspace model override lives in
`workspaces.json` (`WorkspaceEntry.model`; legacy string[] migrates on read);
spawn resolution in ipc.ts is workspace-model → global default (session tier
reserved for Wave 2).

Contract coverage: `tests/append-system.test.ts` (confinement + blank-deletes)
and `tests/session-index.test.ts` (registry migration + model override).

## MCP permission tool-name convention (m1.md, 2026-07-13)

The `tool` field in an `hv.permission` select prompt may be a virtual name for MCP
proxy calls:

| Value | Meaning |
|-------|---------|
| `mcp:<serverName>_<toolName>` | MCP invoke — per-tool identity; rules can pattern-match (e.g. `mcp:github_*`) |
| `mcp` | MCP discovery (search/describe/connect) — safe-default-allowed, no prompt |

The actual format is `mcp:<prefix>_<toolName>` where `<prefix>` is the server name
from `.mcp.json` `mcpServers` key (the adapter's tool-prefix behavior).
Empirically confirmed: server `echo`, tool `echo` → `mcp:echo_echo`.

Discovery calls (`search`, `describe`, `connect`) set `ruleTool: "mcp"` (bare) and
are safe-default-allowed at the bridge gate — they never surface as `hv.permission`
prompts unless an explicit rule matches `mcp`.

## B6 wire coverage note

Contract coverage: `tests/agents-bridge.test.ts` (pi-subagents loads with
PI_SUBAGENT_PI_BINARY set + both `-e` extensions; `/hv-agents` lists the 2
builtins; `/hv-tools` shape — all need no model; a real DEEPSEEK-gated delegation
proves the update-transcript + end-outcome shapes above). Pure logic
(frontmatter parse/serialize/edit/duplicate + tool→permission join + trace
extraction/merge) is unit-tested in `tests/hv-agents.test.ts` and
`tests/agents-renderer.test.ts`.

## W2.3 wire shapes — hv.context-files / nested AGENTS.md injection (2026-07-12)

Pi discovers AGENTS.md/CLAUDE.md **upward from cwd at session start only**
(dist/core/resource-loader.js loadProjectContextFiles — no nested/downward
discovery, no per-tool context loading). Nested AGENTS.md support per the
agents.md standard is built ENTIRELY in the bridge (`hv-agents-md.ts` pure
module + `happyvibe-bridge.ts` wiring):

- On `tool_call` for file tools (`read`/`edit`/`write`; input `path` |
  `file_path`), the bridge resolves the NEAREST `AGENTS.md` walking UP from
  the target file's directory, stopping at the session cwd EXCLUSIVE — the
  root file is Pi's own context file, structurally never duplicated. Paths
  outside cwd resolve to nothing. Discoveries accumulate in a per-session
  in-bridge set (a respawn rediscovers on the next file-tool call).
- On `before_agent_start`, if the set is non-empty the bridge returns
  `{systemPrompt: base + "\n\n## Nested AGENTS.md (scoped instructions)\n…"}`
  — a SINGLE-TURN replacement (agent-session.js resets to the base prompt
  whenever the handler returns nothing). File content is re-read at injection
  time and capped at 8192 chars/file with a `[truncated at 8192 chars]` note.
  `/hv-sysprompt` returns the INJECTED prompt (honest resolved-prompt display).

### hv.context-files — fire-and-forget notify on every set change

```json
{"kind":"hv.context-files","nested":[{"dir":"sub","path":"/ws/sub/AGENTS.md","chars":42}]}
```

`dir` is cwd-relative. Renderer patches the live snapshot's system block
(`parseContextFiles`); the context panel shows each entry as an AGENTS.md row
with a "nested" tag and its subtree dir.

### hv.context snapshot extension

The snapshot's `system` block gains `nested` (same array shape, computed fresh
at snapshot time — the set can grow mid-turn):

```json
{"kind":"hv.context","stage":"snapshot","system":{"chars":9000,"estTokens":2250,"toolCount":4,
 "contextFiles":[{"path":"/ws/AGENTS.md","chars":512,"estTokens":128}],
 "nested":[{"dir":"sub","path":"/ws/sub/AGENTS.md","chars":42}]},"items":[],"marks":[]}
```

### Missing-file flow (renderer ↔ main only, no new Pi wire)

- `hv:has-claude-md(workspaceId)` / `hv:copy-claude-md(workspaceId)` — confined
  to `<workspace>/CLAUDE.md` → `<workspace>/AGENTS.md` (agentsMd.ts trust
  boundary; copy is the explicit user action and returns the copied content).
- "Draft with agents-md-maker" reuses EXISTING wire: `hv:prompt-session` asks
  the model to delegate to the bundled `agents-md-maker` agent (third built-in,
  installed by the same versioned idempotent installBuiltinAgents sweep); the
  renderer captures the delegation's `tool_execution_end` finalOutput
  (agents.ts traceFromEnd) into the editor. The draft is NEVER auto-written —
  Save is explicit.

Contract coverage: `tests/agents-md-bridge.test.ts` (bridge loads + empty
state need no model; the injection round-trip — discovery notify on turn 1's
read, turn 2's system prompt contains the nested section, root NOT duplicated,
snapshot `nested` list — is DEEPSEEK-gated because tool_call/before_agent_start
only fire on real turns; PROVEN live 2026-07-12). Pure logic (nearest-file
resolution, cwd-exclusive stop, outside-cwd refusal, cap+truncation, dedupe,
list shape) in `tests/hv-agents-md.test.ts`; CLAUDE.md-copy confinement in
`tests/agents-md.test.ts`.

## WS8 — file-tree mutations + native watching (renderer ↔ main only, no Pi wire)

All confined by `files.ts` `resolveInWorkspace` (the workspaceId trust boundary);
each refuses to clobber an existing path.

- `hv:fs-create-file` / `hv:fs-create-dir(workspaceId, relPath)` — create (mkdir parents).
- `hv:fs-move(workspaceId, srcRel, destDirRel)` — move within the workspace (rename); refuses overwrite / moving into itself. Returns the new rel path.
- `hv:fs-import(workspaceId, destDirRel, srcAbsPaths[])` — copy EXTERNAL OS paths (from an OS drag-drop) into a confined dest (`fs.cpSync` recursive). Sources are absolute + unconfined (they're external); only dest is confined. Returns written rel paths.
- `hv:watch-workspace` / `hv:unwatch-workspace(workspaceId)` — start/stop a recursive `fs.watch` (macOS/Windows; Linux recursive is unsupported → no watcher, renderer keeps working). Debounced changes push `hv:fs-changed {workspaceId, relDirs}`; the tree re-lists expanded dirs (replaces the manual refresh button).
- Drag-out: tree rows set the `application/x-hv-relpath` dataTransfer mime; the center panes drop-open it. Preload exposes `getPathForFile(file)` (`webUtils.getPathForFile`) for OS-drop imports.

Unit coverage: create/move/import confinement + no-clobber in `tests/files.test.ts`.

## §V2 addendum — hv.ask-user (AskUserQuestion tool, V2.B)

The registered `ask_user` tool (happyvibe-bridge.ts + pure hv-ask-user.ts)
surfaces a model-initiated decision as a BLOCKING `ctx.ui.input` — JSON
payload in `title` (hv.auth prompt precedent). NO timeout, NO auto-answer
(permission invariant). Routing is KIND-based: `method:"input"` +
`title.kind:"hv.ask-user"` (hv.auth inputs keep routing to the auth flow).
Input is clamped, never rejected: headers >12 chars truncated, >4
questions/options dropped, previews stripped off multi-select questions —
each adjustment rides back on the tool result as a note.

**Verbatim request (observed live in ask-user-bridge.test.ts, 2026-07-12):**

```json
{"type":"extension_ui_request","id":"…","method":"input","sessionId":"…",
 "title":"{\"kind\":\"hv.ask-user\",\"intent\":\"Choosing a greeting style\",\"questions\":[{\"question\":\"Which greeting should I use?\",\"header\":\"Greeting\",\"multiSelect\":false,\"options\":[{\"label\":\"Hello (Recommended)\",\"description\":\"Plain and friendly\"},{\"label\":\"Hi\",\"description\":\"Casual\"}]}]}",
 "placeholder":""}
```

**Working response — answered** (`value` is `JSON.stringify(AskAnswer[])`;
`answers` carries option labels and/or the renderer's free-text "Other"):

```json
{"type":"extension_ui_response","id":"…",
 "value":"[{\"question\":\"Which greeting should I use?\",\"header\":\"Greeting\",\"answers\":[\"Hello (Recommended)\"],\"note\":\"keep it simple\"}]"}
```

**Working response — dismissed** (renderer `respondInput(id, null)` → main
maps to `{cancelled:true}`; the tool result tells the model "User dismissed
the question — proceed with your best judgment."):

```json
{"type":"extension_ui_response","id":"…","cancelled":true}
```

Tool result to the model is readable markdown ("The user answered:\n-
**Greeting** — Which greeting should I use?: Hello (Recommended) _(note: keep
it simple)_"). Renderer always appends "Other"; the answer also echoes into
the transcript as a user-style item (`answersSummary`) and queues through the
same uiQueue as permission prompts (sidebar badges, headFor routing, dock
badge unchanged). `ask_user` is in SAFE_TOOLS — no permission prompt stacks
on top of the question modal.

Contract coverage: `tests/ask-user-bridge.test.ts` (DEEPSEEK-gated; request
shape + wire round-trip + tool result reaches the model — PROVEN live
2026-07-12). Pure logic (clamping, answers round-trip, markdown) in
`tests/hv-ask-user.test.ts`; renderer routing incl. hv.auth non-regression in
`tests/ask-user-renderer.test.ts`.

## Async subagents — hv.subagent + /hv-subagent-* + discoverability (2026-07-17)

PRD §12: delegations run async by default (`asyncByDefault` in the app-written
`<agentDir>/extensions/subagent/config.json`, `completionBatch.enabled:false`),
so a delegation returns immediately (detached runner) and the user keeps
chatting. pi-subagents emits lifecycle events on the **in-process `pi.events`
bus** (never on RPC stdout); the co-resident bridge subscribes and relays each
as a fire-and-forget `hv.subagent` notify (JSON in `message`, like B4/B5).
Completion itself is auto-delivered by pi-subagents as an injected
`subagent-notify` message + triggered assistant turn (final-output-only, the
same isolation contract as foreground) — the RPC stream serializes that turn;
the `hv.subagent complete` notify is the card-lifecycle signal alongside it.

### hv.subagent — lifecycle relays (bus → bridge → main/renderer)

```json
{"kind":"hv.subagent","stage":"started","runId":"<id>","agent":"code-explorer","task":"map the repo","asyncDir":"/…/pi-subagents-uid-501/async-subagent-runs/<id>"}
{"kind":"hv.subagent","stage":"control","runId":"<id>","activityState":"long-running"}
{"kind":"hv.subagent","stage":"control","runId":"<id>","activityState":"needs_attention"}
{"kind":"hv.subagent","stage":"complete","runId":"<id>","status":"success","summary":"<final output, ≤500 chars>"}
{"kind":"hv.subagent","stage":"complete","runId":"<id>","status":"error"}
{"kind":"hv.subagent","stage":"complete","runId":"<id>","status":"interrupted"}
```

- `started` maps from `subagent:async-started` (`{id, agent, task, asyncDir, …}`).
  **From 0.50 the event's `task` is redacted — the caption comes from the bridge's
  own store instead.** See §"pi-subagents 0.50" below.
- `control` maps from `subagent:control-event` (`{event:{type}, asyncDir}`); `runId = basename(asyncDir)` (the run dir is named by id). `activityState`: pi-subagents' `active_long_running` → `"long-running"`, `needs_attention` → `"needs_attention"`.
- `complete` maps from `subagent:async-complete` (`{runId, agent, success, summary, state}`); `status = success ? "success" : state==="paused" ? "interrupted" : "error"`.

### /hv-subagent-interrupt <runId> — stop a running async run (reply notify)

Drives pi-subagents' versioned event-bus RPC (`subagents:rpc:v1:request`, method
`interrupt`) and acknowledges:

```json
{"kind":"hv.subagent","stage":"interrupt-sent","runId":"<id>"}
{"kind":"hv.subagent","stage":"interrupt-error","runId":"<id>"}
```

Interrupt is file-based and resumable (drops `control/interrupt.json`). Driven
from main via RPC `prompt` like the other `/hv-*` commands.

### /hv-subagent-list — active-run resync (reply to a respawn)

`restoreActiveJobs` repopulates pi-subagents' internal job map on session_start
but does NOT re-emit `started`, so main fires `/hv-subagent-list` after every
(re)spawn to resync run cards + the activity counter. The bridge reads
`listAsyncRuns(ASYNC_DIR, {states:["queued","running"], sessionId})`:

```json
{"kind":"hv.subagent","stage":"active","runs":[{"runId":"<id>","agent":"code-explorer","asyncDir":"/…/<id>"}]}
```

**Why this reads pi-subagents' internals (re-audited against 0.40.0, 2026-08-02).**
`listAsyncRuns` + `ASYNC_DIR` are imported by RELATIVE path
(`../node_modules/pi-subagents/src/…`), because 0.35.0 added an `exports` map that
lists neither file and an exports map only gates *bare* specifiers. Three public
alternatives were measured against the real 0.40.0 tarball and all three fail:

| candidate | why it can't serve this command |
| --- | --- |
| `snapshotBackgroundWork()` (`./background-work`) | inverse API — it lets *other* extensions declare work *to* pi-subagents for idle/hibernation gating. pi-subagents never calls `registerBackgroundWorkProvider` on itself, so the snapshot is empty; a `BackgroundWorkItem` is only `{id, sessionId}`. |
| `status` RPC → `fleet` (new in 0.40.0, `{version:1,entries,totalActive,omitted}`) | genuinely structured, but `SubagentRpcFleetEntry.key` is documented "Opaque key for client-side reconciliation; **never a run or async identifier**" (`src/extension/rpc.ts:76`). No `runId` (so no interrupt), no `asyncDir` (so no `status.json` tail). It is a display fleet view. |
| `status` RPC prose | `src/runs/background/fleet-view.ts` renders display lines; scraping them is strictly more fragile than the import. |

`fleetStatus` is **not** an RPC method — `SUBAGENT_RPC_METHODS` is
`ping, status, spawn, steer, interrupt, stop, resume` (`src/extension/rpc.ts:29`);
`capabilities.fleetStatus:{version:1}` is a handshake flag for the TUI widget
(`SubagentFleetStatus`, gated on `ctx.hasUI`). `steer` and `resume` are new in
0.40.0 and currently unused by HappyVibe.

Gate: `tests/pi-subagents-contract.test.ts` (key-free) pins the relative form,
the exports map, the tmpdir-confined `ASYNC_DIR`, and the `{id, asyncDir,
steps[].agent}` triple against a fixture run dir.

### Subagent trace projections — `messages` is gone (0.40.0)

The child transcript used to ride `tool_execution_update.partialResult.details
.results[].messages`. As of 0.40.0 it rides **nothing**:

| projection | source | `messages` | `toolCalls` |
| --- | --- | --- | --- |
| streamed update | `snapshotStreamResult` (`execution.ts:266`) | `undefined` **(new in 0.40)** | present, bounded to 64 |
| terminal end | `compactForegroundResult` (`utils.ts:405`) | `undefined` (unchanged since 0.34) | present |

Upstream's reason is in the code: one `tool_execution_update` is serialized as a
single child-stdout line, read under `MAX_CHILD_PENDING_LINE_BYTES` (16 MiB), and
an unbounded running transcript could cross it and kill the child with
`protocol_output_limit`. So this is the same change as the deep-fan-out robustness
fix — the regression and the win are one commit.

`ToolCallSummary` is `{text, expandedText}` (display strings only). The renderer
maps it into the existing `SubagentMessage` rows (`role:"tool"`, `expandedText ??
text`) and still prefers real `messages` when a payload carries them, so a future
pin that restores the transcript needs no renderer change. **The child's prose is
genuinely lost from this path**; its ANSWER survives as `finalOutput`, which
`SubagentTraceView` now renders. Live activity for the DEFAULT (async) path is
unaffected — that comes from the `status.json` poller, not this projection.

Pinned by `tests/agents-bridge.test.ts` (wire: asserts `messages` is absent) plus
`tests/agents-renderer.test.ts` (the toolCalls→rows mapping).

### The parent-blocking wait tool is name-matched (`wait` → `subagent_wait`)

PRD §12's "never block on the result" is enforced by a tool-name match, so a
rename disarms it silently. 0.35.0 renamed `wait` → `subagent_wait` with **no
alias** (`src/runs/background/wait-tool.ts:8`). Both names now live in
`WAIT_TOOLS`/`isWaitTool` (`hv-rules.ts`) — the shared pure module the bridge
(blocks the call) and the renderer (hides the card, `App.tsx`) both import, so the
two can't drift. `tests/pi-subagents-contract.test.ts` asserts the name upstream
actually registers is in the set, and that neither call site uses a literal.
The upstream alternative is `"waitTool": false` in the subagent config (or
`PI_SUBAGENT_WAIT_TOOL_ENABLED=false`), which per its README **keeps the tool
registered but makes direct calls return immediately** instead of blocking. We do
not set it: the bridge's guard also *steers* ("end your turn now, the result will
arrive"), which a silent no-op does not, and the guard has to exist anyway for the
renderer to hide the card. Worth adding as defence-in-depth if the name ever
drifts again.

**Not a concern (checked 2026-08-02):** pi-subagents >=0.40 also ends every turn
with `if (ctx.hasUI) return; await drainOutstandingWork(...)`, which WOULD block a
turn on its own async delegation. It never runs for us — `ctx.hasUI` is **true**
under `--mode rpc` (rpc-mode.js binds a real `uiContext`; Pi's headless mode is
*print* mode). Measured with a probe extension, and the three links are pinned in
`tests/pi-subagents-contract.test.ts`. Do not read a `ctx.hasUI` gate in vendored
code as "excludes HappyVibe".

### hv:subagent-status — live status poll (internal IPC, NOT a Pi wire contract)

Main tails `<asyncDir>/status.json` (~500 ms; atomic writes → a torn read skips a
tick; asyncDir sanity-checked to start with `os.tmpdir()`) for each active run
and forwards changes to the renderer:

```json
{"sessionId":"…","runId":"<id>","status":{"currentTool":"read","activityState":"long-running","recentTools":[{"tool":"grep","args":"…"}],"turnCount":3,"state":"running"}}
```

### Discoverability — hv.context snapshot `system.agents`

The bridge injects an `## Available subagents` roster (name + description of every
enumerated agent, builtin + project, plus one guidance line) into the system
prompt each turn — the same per-turn replacement mechanism as nested AGENTS.md —
so the model delegates spontaneously instead of having to call
`subagent {action:"list"}`. It rides the `/hv-context` snapshot's `system` block:

```json
{"kind":"hv.context","stage":"snapshot","system":{"…":"…","agents":[{"name":"code-explorer","chars":72},{"name":"agents-md-maker","chars":95}]}}
```

Contract coverage: `tests/subagent-async-bridge.test.ts` (DEEPSEEK-gated: async
delegation returns immediately with `details.asyncId` + `started` notify, then a
`complete` notify + a triggered turn — the session-id-continuity delivery
contract; plus the `async:false` foreground regression). Discoverability:
`tests/subagent-discovery-bridge.test.ts` (`/hv-agents` enumerate + DEEPSEEK-gated
injection into `/hv-sysprompt` and `system.agents`). Pure logic:
`tests/hv-agents.test.ts` (`renderSubagentSection`), `tests/session-activity.test.ts`
(async-run activity gating), `tests/subagent-status.test.ts` (poller), and
renderer parsers in `tests/agents-renderer.test.ts`.

## Plan Mode — hv.plan / hv.plan-write / hv.plan-status / hv.plan.blocked + hv:plan-* (2026-07-19)

PRD §23. Plan Mode is per-session read-only mode; the plan is a workspace file
(`.agents/plans/NNN-<slug>.md`) with front-matter status + a Tasks checklist. The
bridge module `hv-plan.ts` holds `{enabled, planPath?}` (persisted via
`pi.appendEntry("hv-plan-state")`, restored on `session_start` like context marks
— but note: unlike dangerous mode this state SURVIVES respawn), and re-emits the
`hv.plan` notify on `session_start` so the renderer resyncs after a respawn.

### The plan gate (tool_call clamp, BEFORE dangerous + rules)

Inserted in the bridge `tool_call` handler after MCP unwrap (so it sees real tool
names) but **before** the dangerous/bypass check and the rule engine — Plan Mode
wins over bypass. `gatePlanCall(toolName, input)` (pure, in `hv-plan.ts`):

| Result | Tools | Bridge action |
|--------|-------|---------------|
| `block` | `edit`, `write`, `subagent` | `{block:true, reason}` + `hv.plan.blocked` notify + `hv.audit` (source `"plan"`) |
| `block` | `bash` failing the read-only allowlist | same |
| `pass` | `read`/`grep`/`glob`/`list`/`ls`/`ask_user`/`plan_complete`, allowlisted `bash` | fall through to normal gates |
| `floor-ask` | everything else (MCP, unknown) | run `evaluate()`, then clamp `allow → ask` (deny stays deny) |

The bash allowlist is ported from `@narumitw/pi-plan-mode` `tool-policy.ts` (MIT):
fail-closed — read-only commands + safe `git`/`gh`/`npm` subcommands; rejects
redirects, substitutions, subshells, background jobs, and any chain with an unsafe
segment. In-flight async subagent runs are untouched (the gate sees only new calls).

### hv.plan — mode notify (reply to /hv-plan on|off|implement + on session_start)

Fire-and-forget `ctx.ui.notify` (JSON in `message`, like B4/B5); renderer parses,
never opens the modal. Drives the banner + composer toggle + sidebar badge.

```json
{"kind":"hv.plan","enabled":true,"planPath":null}
{"kind":"hv.plan","enabled":true,"planPath":".agents/plans/001-add-auth.md"}
{"kind":"hv.plan","enabled":false,"planPath":".agents/plans/001-add-auth.md"}
```

`off` keeps `planPath` (the file stays on disk). `/hv-plan` is driven from main
via RPC `prompt` (B3/B4 precedent); enter is also reachable via the `plan_start`
registered tool.

### hv.plan-write — BLOCKING plan-file write (plan_complete → main → tool result)

The `plan_complete({intent, plan})` registered tool emits a **blocking**
`ctx.ui.input` (JSON in `title`, like `hv.ask-user`); main writes the file and
answers with the workspace-relative path, which becomes the tool result AND the
new `planPath`. Main MUST always respond (error string on failure) — never leave
the bridge blocked.

```json
{"type":"extension_ui_request","id":"…","method":"input",
 "title":"{\"kind\":\"hv.plan-write\",\"plan\":\"# Add auth\\n\\n## Tasks\\n- [ ] …\\n\\n## Verification\\n…\"}",
 "placeholder":""}
```

```json
{"type":"extension_ui_response","id":"…","value":".agents/plans/001-add-auth.md"}
```

A second `plan_complete` in the same planning phase (planPath already set)
**overwrites the same file** (revision, keeping front-matter `createdAt`); a fresh
mode entry starts a new NNN. The written file carries front-matter `status: draft`
+ timestamps; `parsePlanFile(md) → {status, createdAt?, done, total}` (shared by
bridge, main, tests) counts `- [ ]`/`- [x]`.

### hv.plan-status — terminal status (plan_status_update → main rewrites front-matter)

Fire-and-forget notify; `plan_status_update({intent, status, note?})` records a
terminal fact (the model may only set `implemented` | `cancelled`).

```json
{"kind":"hv.plan-status","status":"implemented","note":"all checks pass"}
{"kind":"hv.plan-status","status":"cancelled"}
```

Main rewrites the plan file's front-matter and audits `plan.status {who:"model"}`.
Human overrides (incl. `reopen` → `implementing`, which is human-only) go through
`hv:plan-status` IPC and audit `{who:"human"}`.

### hv.plan.blocked — skipped-tool notify (renderer renders the "skipped" card)

```json
{"kind":"hv.plan.blocked","toolName":"write","toolCallId":"call-A","reason":"Plan mode is read-only — file edits are blocked."}
```

### Renderer ↔ main IPC (no Pi wire)

- `hv:plan-set(sessionId, enabled)` — disabling while the session is busy aborts
  the turn first (`{type:"abort"}`), then fires `/hv-plan off`.
- `hv:plan-implement(sessionId, model?)` — sets front-matter `implementing`;
  applies the optional session model override (reasonable-model nudge, §23); fires
  `/hv-plan off` then the file-referencing implement message; audits `plan.implement`.
- `hv:plan-discard(sessionId)` — abort-if-busy + `/hv-plan off`; file stays `draft`.
- `hv:plan-status(sessionId, relPath, status)` — human status override / reopen.
- `hv:plan-changed {workspaceId, path, status, done, total}` — pushed from the
  `watch.ts` fs-watcher when a `.agents/plans/*.md` file changes (`.agents` added to
  `DOTFILE_ALLOW`); drives live PlanCard progress. Carries no `sessionId` — the
  renderer maps via `planPath`.

Audit events (EventLog, frozen envelope): `plan.enter`, `plan.exit`,
`plan.blocked`, `plan.ready`, `plan.implement`, `plan.status` (with `who:"model"|"human"`).

Note — `plan_start` mid-turn: the gate applies to the next `tool_call` immediately,
but the 3-phase plan prompt only takes effect on the next turn (`before_agent_start`).

Contract coverage: `tests/plan-bridge.test.ts` (DEEPSEEK-gated: enter Plan Mode →
a non-allowlisted bash call blocked + `hv.plan.blocked` + an audit stamped
`source:"plan"` → `plan_complete` → blocking `hv.plan-write` answered with a path →
`agent_end`; plan-file semantics). The blocked call is a **`curl`**, not a `touch`:
the plan-mode system prompt has already told the model it is read-only, so asking
for a mutation gets prose and no tool call at all (observed — no
`tool_execution_start`, nothing for the clamp to decide), while `curl` reads as
inspection and the model issues it freely. Same `isSafeCommand` block branch either
way, so the wire is proved on a call the model actually makes. Pure
logic (bash allowlist accept/reject table, gate ordering incl. plan-block wins over
dangerous, front-matter/checkbox parser, state round-trip) in `tests/hv-plan.test.ts`;
plan-file writer (numbering, revision-overwrite, confinement, status rewrite) in
`tests/plans.test.ts`; renderer `parsePlan` in `tests/plan-renderer.test.ts` (if added).

## Session restore reads the file, not `get_messages` (§17, round 10)

`get_messages` returns `agent.state.messages` — cheap in itself, but it is the
FIRST RPC a freshly spawned child answers, so it absorbs the whole boot: 1.35–2.86 s,
90%+ of a restore. `history.ts contextItems` rebuilds the same transcript from the
session file in 1–5 ms (measured on real sessions to 4 MB) and needs no child, so
`hv:open-session` paints before the spawn instead of after it.

Pi's rule (`session-manager.js buildContextEntries`) is
`[latest compaction] + entries from firstKeptEntryId onward`. The compaction entry
itself only yields a `role:"compactionSummary"` message, which `restoreItems` drops,
so the plain slice from `firstKeptEntryId` reproduces it exactly — and is the exact
complement of `earlierItems`.

Two consequences worth knowing:
- §9 removals are applied by the bridge at the `context` event only, so
  `agent.state.messages` still holds removed messages. The file path runs
  `filterMessages`, so a removed message no longer comes back on reopen.
- A session with no captured `piSessionFile` yet keeps the RPC (its client is
  already warm, so there is no boot to absorb).

Contract coverage: `tests/history-contract.test.ts` — key-free, part of the
**pin-bump gate**. It writes a compacted session file, resumes a real Pi on it, and
asserts `contextItems(file)` equals `restoreItems(get_messages)`. A Pi version that
changes the context rule fails here instead of silently truncating conversations.

## Prompt templates — hv.prompt-template (§24, 2026-08-02)

The only Pi wire this feature adds. Full Pi-side evidence is in `pt1.md`; this is
the envelope shape.

### hv.prompt-template — typed↔expanded pairing (fire-and-forget notify)

Pi expands a prompt template into the user message and keeps only the expansion
(`agent-session.js:867-875`), so the typed `/review src/foo.ts` exists on no event
and in no file. Two hooks straddle the expansion in the same `prompt()` call stack,
so the bridge pairs them:

```
input                (agent-session.js:810)  → original typed text
before_agent_start   (agent-session.js:882)  → expanded text (event.prompt)
```

Emitted from the bridge's `before_agent_start` handler, once per expanded
invocation, never for an ordinary prompt or an unexpanded `/hv-*`:

```jsonc
{ "kind": "hv.prompt-template",
  "name": "review",                       // basename of the template file
  "typed": "/review src/foo.ts",          // what the user wrote
  "expanded": "Review src/foo.ts for …" } // what the model received
```

Rides `ui.notify` (the ui-request channel, `method:"notify"`), like `hv.audit` and
`hv.skill` — never RPC stdout.

Main logs `{ type: "prompt-template.invoked", data: { sessionId, typed, expandedHash } }`
(sha256 of the expansion) and forwards the envelope to the renderer.
`applyPromptTemplatePair` folds it into the transcript; `restore.ts` re-pairs after a
reload by hashing each restored user message. **Hash, never ordinal** — ask-user
answers and queued messages also produce user messages.

**The `input` handler fails OPEN and must stay that way.** It sits in front of
every user prompt. Pi already wraps input handlers (`runner.js:933-955`) and a
handler returning `undefined` continues normally; only `{action:"handled"}`
swallows a prompt. The bridge wraps its own body too. `tests/prompt-templates-bridge.test.ts`
asserts a plain non-slash prompt still completes **before** it asserts anything
about the feature — if that goes red, revert the hook, not the test.

---

## Agent terminals — hv.terminal-* + hv.terminal (§26 part 2, 2026-08-07)

Three registered tools, each a thin shell over a **blocking** `ctx.ui.input`
envelope. Main owns the PTYs (part 1), so main owns the cap, the busy-reuse
refusal and the interleave hold; the bridge only carries the request and turns
the reply into a tool result.

### The three blocking inputs (bridge → main → tool result)

Sent as `ctx.ui.input(JSON.stringify(envelope), "")`, so the payload is in
**`title`** (a notify uses `message` — that asymmetry is the single easiest
thing to get wrong here, and it costs you a card that never appears).

```jsonc
{ "kind": "hv.terminal-run",  "command": "npm run dev", "terminalId": "t1?", "intent": "…" }
{ "kind": "hv.terminal-read", "terminalId": "t1", "lines": 200, "waitMs": 3000 }
{ "kind": "hv.terminal-kill", "terminalId": "t1" }
```

Main **always** answers, with a JSON string:

```jsonc
{ "ok": true,  "terminalId": "t1", "title": "zsh" }                                  // run
{ "ok": true,  "text": "…", "running": true, "exitCode": null, "userTyped": false }  // read
{ "ok": true }                                                                        // kill
{ "ok": false, "reason": "…" }                                                        // any of them
```

`terminalReply` (bridge) maps `ok:false` to the reason as the tool's text. **A
dropped response hangs the turn** — same contract, and same reason, as
`hv.plan-write`.

`intent` rides the run envelope only so main can echo it on the notify below;
the permission prompt was built from the factual `summarize()` long before, and
must never show it (§13's MCP rule).

### hv.terminal — card lifecycle (fire-and-forget notify, payload in `message`)

```jsonc
{ "kind": "hv.terminal", "stage": "started", "terminalId": "t1", "title": "zsh",
  "command": "npm run dev", "intent": "…", "workspaceId": "/abs/ws" }
{ "kind": "hv.terminal", "stage": "killed",  "terminalId": "t1" }
```

There is deliberately no `exited` stage: the renderer already learns that from
`hv:term-exit` on part 1's push channel, and the card stack is derived from the
live `terminals` map, so an exited terminal leaves it with no extra event.

### Gate placement (all three must be NAMED — nothing is automatic)

| Tool | `SAFE_TOOLS` | `gatePlanCall` |
|---|---|---|
| `terminal_run` | no (asks) | `BLOCKED_PLAN_TOOLS` |
| `terminal_read` | **yes** | `PLAN_PASS_TOOLS` |
| `terminal_kill` | no (asks) | `PLAN_PASS_TOOLS` |

`terminal_read` in `PLAN_PASS_TOOLS` is not cosmetic: the default is
`floor-ask`, which clamps `allow`→`ask`, so a planning agent polling a log would
raise a modal **on every poll**.

### Two guards run in `tool_call` BEFORE the permission prompt

The ordering is the mechanism, not an optimisation. A multi-line `command` is
refused (`describeCommand` labels the modal from the first segment only, so the
user would approve a string they are shown part of), and a `bash` command with a
genuine trailing `&` is blocked and redirected — **only while
`builtins.terminal` is true**, since with the group off there is no
`terminal_run` to redirect to. Both audit as `source:"terminal"`, which is also
the only way to observe them: `tool_execution_start` fires *before* tool_call
handlers, so it cannot distinguish a blocked call from an allowed one.

### ~~Registering three tools measurably dilutes a small model's tool choice~~ — RETRACTED

**This section previously claimed the terminal tools made the model stop calling
`bash`. That claim was wrong, and the retraction is kept in place because the way
it was wrong is more useful than the claim ever was.**

The original evidence (2026-08-07, deepseek-v4-flash, `touch forbidden.txt`,
counting `bash` calls) was five trials per configuration, run **sequentially**,
each in a different app state:

| Configuration | bash called |
|---|---|
| before the terminal tools existed | 3/5 |
| `builtins.terminal:false` | 2/5 |
| tools registered, no steer line | 1/5 |
| tools + steer line (first draft) | 0/5 |
| tools + steer line (bash-affirming rewrite) | 0/5 |

It reads as a clean dose-response curve. It is not one. **3/5 vs 0/5 is
p = 0.167 by a two-sided Fisher exact test** — never significant at any point,
and the monotone shape is what five-trial binomial noise looks like when you sort
the rows by the story you already believe.

Two controlled re-runs, **interleaved** so provider drift cannot masquerade as a
condition effect:

**A · No bridge, only a probe extension registering K tools (n=8 per cell).**
Isolates the tool list from everything else — no gate, no steer line, no MCP.

| Condition | bash called |
|---|---|
| 0 extra tools | 1/8 |
| 3 unrelated tools (calendar/contacts/notes, neutral wording) | 4/8 |
| 3 terminal tools, wording that never names bash | 4/8 |
| 3 terminal tools, the **shipped** wording | **7/8** |

**B · The shipped configuration — real bridge, only `builtins.terminal` toggled
(n=12 per arm).** `off` 4/12 · `on` 6/12.

So: the effect does not exist in the direction claimed, and the one significant
comparison runs the **opposite** way.

| Comparison | p (2-sided Fisher) |
|---|---|
| original claim, 3/5 vs 0/5 | 0.167 — not significant |
| 0 tools vs 3 shipped-wording tools | **0.010 — significant, more tools ⇒ MORE bash** |
| 0 tools vs 3 unrelated tools | 0.282 |
| neutral wording vs bash-mentioning wording | 0.282 |
| shipped config, group off vs on | 0.680 — no detectable effect |

There is also a confound the original never controlled for: `terminal_run`'s own
description says *"instead of backgrounding a bash command"* and `terminal_read`'s
says *"instead of sleeping in bash"*. The "tools, no steer line" row therefore
still disparaged bash — descriptions **are** wording. The "wording versus count"
question the original claimed to have answered was never actually asked.

**What stands.** The prompt changes in `tests/bridge.test.ts` and
`tests/rules-bridge.test.ts` stay, on their own merit rather than the retracted
one: at the shipped baseline the model calls `bash` on roughly half of attempts,
so `askUntil`'s three tries left those tests failing ~12% of the time. The
explicit wording measured 5/5. That is a flakiness fix, not a dilution fix.

**The transferable lesson is about method, not tools.** Five trials per cell,
run sequentially while other things changed, produced a table that looked
causal, survived my own review, and shipped into three documents. What broke it
was interleaving the conditions and computing a p-value — both cheap, neither
done the first time. Before recording a behavioural claim about a model here:
interleave the arms, hold everything else fixed, and if the result would not
survive a Fisher exact test, write down the uncertainty instead of the claim.

---

## Embedded browser — hv.browser-* + hv.browser (§28, 2026-08-15)

Ten registered tools, each a thin shell over a **blocking** `ctx.ui.input`
envelope, exactly like §26's terminals. Main owns the `WebContentsView`, so main
owns the cap (one pane per session), the egress gate and every lifecycle event;
the bridge carries the request and turns the reply into a tool result.

### The ten blocking inputs (bridge → main → tool result)

Payload in **`title`** (a notify uses `message`). Same asymmetry, same trap.

```jsonc
{ "kind": "hv.browser-open",        "url": "http://localhost:5173", "intent": "…" }
{ "kind": "hv.browser-navigate",    "url": "https://example.com",   "intent": "…" }
{ "kind": "hv.browser-screenshot",  "intent": "…" }
{ "kind": "hv.browser-get-text" }
{ "kind": "hv.browser-read-console","lines": 100 }
{ "kind": "hv.browser-read-network","limit": 50 }
{ "kind": "hv.browser-click",       "selector": "#save", "intent": "…" }
{ "kind": "hv.browser-type",        "selector": "#q", "text": "hello", "intent": "…" }
{ "kind": "hv.browser-evaluate",    "code": "document.title", "intent": "…" }
{ "kind": "hv.browser-close",       "intent": "…" }
```

Main **always** answers, with a JSON string:

```jsonc
{ "ok": true,  "browserId": "b1", "text": "The browser is open at …" }        // open
{ "ok": true,  "text": "…", "untrusted": true }                              // every read/act
{ "ok": true,  "text": "…", "imageBase64": "iVBOR…" }                        // screenshot, vision model
{ "ok": false, "reason": "This session has no browser open. …" }             // any of them
```

Two fields only this family has:

- **`untrusted: true`** ⇒ `browserReply` prefixes `UNTRUSTED_BANNER` (hv-browser.ts).
  Page bytes must never reach the model dressed as our own words. Prompt
  injection has no mechanical fix; labelling plus the permission gate on what the
  injected text talks the model into is the whole mitigation (§28).
- **`imageBase64`** ⇒ an `{type:"image"}` content block **in addition to** the
  text. Verified, not assumed: `AgentToolResult.content` is
  `(TextContent | ImageContent)[]` in
  `pi-runtime/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-agent-core/dist/types.d.ts:316-320`,
  so an extension tool CAN hand a model a picture. Main only sets it when the
  session's model advertises `input: ["image", …]` in Pi's registry — otherwise
  the result says so and points at `browser_get_text`. The **user sees the
  screenshot either way**, via the notify below.

### hv.browser — card lifecycle (fire-and-forget notify, payload in `message`)

```jsonc
{ "kind": "hv.browser", "stage": "opened",     "browserId": "b1", "url": "…", "intent": "…", "workspaceId": "/abs/ws" }
{ "kind": "hv.browser", "stage": "navigated",  "browserId": "b1", "url": "…", "intent": "…" }
{ "kind": "hv.browser", "stage": "screenshot", "browserId": "b1", "intent": "…" }
{ "kind": "hv.browser", "stage": "acted",      "browserId": "b1", "action": "click|type|evaluate", "detail": "…" }
{ "kind": "hv.browser", "stage": "closed",     "browserId": "b1" }
```

`opened` is what places the TAB (renderer, `openBrowserTab`) — main creates the
pane, but where it goes on screen is a layout decision and stays in the renderer.

The screenshot notify carries **no image**: §28's "the user always sees it" means
*in the pane*, and the live page is already on screen beside the transcript, so
shipping a multi-megabyte data URL to a renderer that has the real thing would be
waste. The PNG goes to the MODEL (as an image content block) or nowhere.

### Gate placement — navigation gates per HOST, not per tool

| Tool | rule name | `SAFE_TOOLS` | `gatePlanCall` |
|---|---|---|---|
| `browser_open` / `browser_navigate` | **`browser:<host>`** | no | *(unnamed)* → `floor-ask` |
| `browser_get_text` / `_read_console` / `_read_network` / `_screenshot` | own name | **yes** | `PLAN_PASS_TOOLS` |
| `browser_close` | own name | **yes** | `PLAN_PASS_TOOLS` |
| `browser_click` / `_type` / `_evaluate` | own name | no (asks) | `BLOCKED_PLAN_TOOLS` |

`browserRuleName(url)` (hv-browser.ts) builds `browser:<host>` — the same
virtual-name trick as `mcp:<server>_<tool>`, so a user's
`deny browser:*.stripe.com` is an ordinary tool-layer rule in the engine that
already exists. **Consequence worth knowing:** "Allow for session" grants the
HOST, so it says nothing about the next one.

`localhost` / `127.0.0.1` / `[::1]` / `0.0.0.0` are a **safe-default allow**
(exact hostname match — `localhost.evil.com` is a real remote host and does not
qualify), evaluated after the rule engine so an explicit `browser:localhost` rule
still wins. Without it the dev-preview loop would prompt on every page load,
which is the whole feature.

`browser_open`/`browser_navigate` are deliberately **absent from both plan sets**
so they land on `floor-ask`: opening documentation to read it is legitimate
planning, and a GET the user approves per call is not a mutation.

### The egress gate is on the PARTITION, not on the tool call

`session.fromPartition("persist:hv-browser").webRequest.onBeforeRequest`
(browsers.ts) is the enforcement point, because a tool-level gate is bypassable
by the page: one `location.href` from injected JS and `browser_navigate` never
ran. Decisions come from the pure `EgressState` (browserEgress.ts).

- **main-frame** → gated. Refused ⇒ `{cancel:true}` **and** the pane goes to
  `state:"blocked"` with the host, which the renderer renders as an Allow button.
  Chromium then fires `did-fail-load` with **ERR_BLOCKED_BY_CLIENT (-20)** (measured — *not*
  ERR_ABORTED/-3, which is what an ordinary interrupted navigation gives), so that handler skips
  both codes and re-checks `state === "blocked"` or it would repaint the one actionable state as a
  generic failure.
  Clicking it is user consent (§28) and needs no second prompt.
- **subresources of an allowed page** → run silently, but every one is recorded
  in the ring `browser_read_network` serves.
- **redirects inherit** the approval of the navigation that started them, or
  every IdP bounce would break.

Honest limit, stated in §28 and repeated here so nobody over-claims it: in-page
`fetch` reaches any domain the page can. The headline is "the agent's browser
only NAVIGATES where you allow", not "no byte leaves". `browser_evaluate`'s
stricter rule is the companion mitigation.

**One hook per partition.** Electron REPLACES `onBeforeRequest` rather than
stacking, so installing it per pane would silently disarm every pane but the
newest — hence `egressInstalled` + a `webContents.id → entry` map.

### The guest has no privileges, and the picker is why that matters

`sandbox: true`, `contextIsolation: true`, no `nodeIntegration`, **no preload**,
`setPermissionRequestHandler`/`setPermissionCheckHandler` deny everything,
`will-navigate` blocks non-http(s), `setWindowOpenHandler` denies popups (a
`new-window` disposition goes to the OS browser instead). The element picker is
therefore **injected** via `executeJavaScript` (browserPicker.ts) rather than
preloaded — it reads geometry and DOM, i.e. nothing the page did not already
have. Its payload enters the COMPOSER as a chip the user then sends, so it gets
no untrusted banner: the human authors that turn, which is exactly the line the
tool-result rule above draws.

### The composited view has no z-index — visibility IS the z-order

A `WebContentsView` paints over the whole renderer DOM, so anything drawn "above"
it is really drawn *under* it and swallows its own clicks.

**Round 1 correction (2026-08-15).** The rule was `document.querySelector(".hv-overlay")`,
on the belief that every overlay carried that class. Four do; the app has ~35
floating surfaces. Reported as "none of this menu is clickable" — and the menu in
question (the pane `+`) shares neither the class, nor the styling, nor the
dismissal idiom of the other menus, so no selector would have caught it.

`BrowserTab` now asks the question geometrically: a 3×3 grid of
`document.elementFromPoint` samples inside its own rect, rAF-coalesced off a
body `MutationObserver` plus scroll/resize. If the topmost element at any sample
is not the placeholder, something is over the page and the view hides. Nothing to
remember on the other 35 components, and it hides ONLY when the overlap is real —
a menu open in the other half leaves the page up.

The view also insets itself by `DIVIDER_INSET` (6px) on each side that touches
another pane: the divider's 10px drag strip straddles the boundary, so half of it
would otherwise sit under the page (ungrabbable), and a transparent strip at the
very edge would also read as "covered" and hide the page permanently.

### The picker payload (round 1)

`hv:browser-pick` resolves with `{ element, imageBase64 }`, not just the element:

```jsonc
{ "element": { "selector": "#save", "outerHTML": "…", "label": "<button#save> Save",
               "rect": { "x": 12, "y": 40, "width": 88, "height": 32 } },
  "imageBase64": "iVBOR…" }
```

`label` is **not shown in the popup** — the outline already says which element
this is, so printing its name again was one more thing to read for no
information. It survives as the input's accessible name, as the composer chip's
fallback when no comment is typed, and in the sent message. What it contains: what
the element SAYS, never what it is made of — its visible text,
else its accessible name (`aria-label`/`alt`/`placeholder`/`title`), else a plain
word for its kind ("link", "button", "image"). It used to read `<a.nav-play> PLAY`
— a tag and a class are the developer's handle on an element, and the person
pointing at a button means "PLAY". The selector still travels beside it, because
`browser_click` needs one; it just stopped being the label.

`rect` pins the comment popup to the thing being commented on. `imageBase64` is
captured by main **while the view is still visible** (in its own try — a failed
capture must never discard the picked element, which it did until measured) and shown as a still behind
the popup — a popup over the LIVE page cannot be seen at all (no z-index), so the
page has to go, and a frozen frame keeps the user looking at what they clicked
instead of at a blank pane. Both fields are page-controlled and validated:
a rect with a non-finite number degrades to "no rect" (centred popup) rather than
positioning something off-screen.

## pi-subagents 0.50 — what the bump moved (2026-08-17)

**Provenance.** The first half of this section is read out of the vendored 0.50.0
tree with `file:line` or proven by a key-free test; the §"Measured on the wire"
part below is captured from real delegations against
OpenRouter `deepseek/deepseek-v4-flash` and pinned by the re-timed live tests.
The distinction earned its keep: source told us what the code *contains*, and only
the wire told us that `subagent:async-started` never fires on the path we actually
take — a PRD §12 regression no amount of reading would have surfaced.

**A retraction first, because it is the more useful lesson — and note how it ends.**
Four live tests were run at 0.50 before the DeepSeek balance ran out and all four
failed, in a pattern matching the 0.49 audit's inventory: transcript gone from
`results[].messages`, `details.asyncId` missing, no `tool_execution_update`. Those
runs measured NOTHING — all four were a `402 Insufficient Balance`, and the tells
were in the assertions rather than the symptoms (`expected 0 to be greater than 0`
for `tool_execution_start` events; `a real delegation ran: expected undefined to be
truthy`). No delegation ever ran.

The instructive part is what happened when the same three claims were then measured
properly on a funded key: **two were right and one was wrong.**
`tool_execution_update` really is never emitted, and the transcript really is absent
from `results[].messages` (`toolCalls` carries it instead) — while `asyncId` is very
much present at both emit sites (`async-execution.ts:1288,1701`) and on the wire. So
invalid evidence is not the same thing as a wrong conclusion, and "that finding came
from a dead key" is not grounds to discard it — only grounds to re-measure it. The
0.49 audit's red inventory deserves exactly that treatment: re-run, don't trust and
don't dismiss. CLAUDE.md's standing warning ("a live test failing 'model never called
X' is usually NOISE") covers a flaky model; this is the harder version, where the
whole provider is down and every shape assertion fails plausibly at once.

### Prompt redaction — the one break that reaches the UI

`PROMPT_REDACTED = "[prompt redacted]"` (`shared/utils.ts:20`) replaces `task` and
`goal` on every surface an observer reads:

| Surface | Site |
|---|---|
| `subagent:async-started` (`task`, `goal`) | `async-execution.ts:1259-1260, 1682-1683` |
| foreground result (`result.task`, `result.progress.task`) | `foreground/execution.ts:126-129` |
| `status.json` `steps[].description` | `chain-append.ts:155-161` — `statusStepDescription` **ignores its argument** and returns the constant |
| run metadata | `foreground/execution.ts:154` |
| child input artifact + initial transcript message | `foreground/execution.ts:1690, 1704` |

Unconditional: no config key, no env var, nothing in `types.ts` mentioning
redaction. The child still receives the real task via its prompt, so delegation
itself is unaffected — only display is.

Consequence for us: **no surface can give a caption back**, which is why
`hv-subagent-tasks.ts` exists and why it PERSISTS (a respawn or app restart has no
args event and no on-disk task to re-derive from). Pairing is one slot per agent,
last-write-wins; the reasoning, including why a queue is wrong for denied
delegations, is in that file's header. `REDACTED_PROMPT` is duplicated in
`hv-rules.ts` because the renderer cannot import a vendored runtime package, and
`tests/pi-subagents-contract.test.ts` asserts the two still agree.

### Active-run inventory — the accepted upgrade hole

An active-state query (`states` all-active, no `runId`, no `entryLimit` — exactly
`/hv-subagent-list`) is `readActiveRunIndex(root) ?? []` with **no fallback scan**
(`async-status.ts:402`). The index is `<runs-root>/.active-runs/<runId>`, an empty
marker file (`active-run-index.ts:5,22,72`). Proven, not inferred: two contract
tests went red on the bump for exactly this, and both directions are now pinned.

Two facts make the hole cheap, and both are pinned so they cannot quietly stop
being true: the index self-prunes (an indexed entry that no longer resolves is
released, same function), and a **runId-targeted query still scans**
(`async-status.ts:390-399`), so `/hv-subagent-interrupt` reaches a 0.40-era run
even while the list cannot see it. Decision 2026-08-17: accept, no migrator.

### Unchanged, and now pinned because their absence would be silent

- `details.asyncId` on the async dispatch result — the renderer's async/foreground
  switch (`asyncResultInfo`). Checked at **both** emit sites; a bare grep would
  pass on the type declaration alone.
- `id`, `agent`, `asyncDir`, `sessionId`, `lifecycleArtifactVersion` on
  `async-started` — the SHAPE is unchanged and the bridge still maps `id → runId`.
  Read this bullet with the wire section below: the payload is intact but **the event
  does not fire for a top-level delegation at 0.50**, so nothing on our main path
  consumes it. It is kept for nested/single runs and against a pin that restores it.
  This pair — a correct shape on a dead path — is exactly what source-only auditing
  cannot tell you.
- `subagent_wait` name (`wait-tool.ts:9`) — `WAIT_TOOLS` still covers it.
- `ASYNC_DIR` absent from all 11 public subpaths. `./shared-types` looked like its
  home but re-exports **types only** (`api/shared-types.ts`), so the deep relative
  import stays and upstream ask A (export `./async-status`) stays open. Scanned
  ~43.7k chars across every subpath target; the test carries a non-vacuity arm,
  because a negative assertion over source you failed to read is a tautology.
- `agent_end` drain still gated on `hasUI` (true under `--mode rpc`), so PRD §12
  survives.

### New at 0.50 that we adopt

`waitTool: {enabled: false}` (`types.ts`, `wait-tool.ts`) — written by
`writeSubagentConfig` beside `asyncByDefault`. Kept ALONGSIDE the `WAIT_TOOLS`
name guard: a config key upstream renames fails silent, a renamed tool name fails
the contract test loudly, and the guard's guidance text is what the model actually
reads. PRD §12 decision, 2026-08-17.

### Measured on the wire (2026-08-17, OpenRouter deepseek/deepseek-v4-flash)

The rows below replace the "still to measure" list. Captured with
`scripts/probe-050.ts` (same spawn path as the live tests, dumps every RPC event
plus every `status.json`) and then pinned by the re-timed live tests.

**Every top-level delegation is a WORKFLOW now.** `details.mode` is `"workflow"`
with a `missionId`, even for one child — 0.50 removed the legacy entry points
outright (`public-execution.ts`: *"action='single' is not supported"*, *"Legacy
top-level chain and parallel inputs were removed; use workflowScript"*). Three
consequences, each of which broke something:

1. **`subagent:async-started` is NEVER emitted for a top-level delegation.** The
   workflow path emits only `async-complete`. Measured twice with a
   `console.error` inside the bridge's own handler: it does not fire. This was a
   PRD §12 regression with no test covering it — the sticky run card was raised by
   that notify, so an async delegation showed the user NOTHING for its whole life
   and then dropped a result in. Fixed renderer-side: `tool_execution_end` carries
   `details.asyncId`, and the foreground card (which already holds the real agent
   and task from the call's own args) is RE-KEYED to it instead of deleted.
   Measured `asyncId === details.runId === the complete notify's runId`, so the
   completion and the `/hv-subagent-list` resync both find that card.
2. **`agent` on the completion event is the literal `"workflow"`.** The bridge
   drops it, so the hand-off notice cannot name a pipeline the user never chose.
3. **Async is upstream's own default now.** A run with no `asyncByDefault` config
   still detached. Our config stays (explicit beats inherited) but is no longer
   what makes it true.

**`tool_execution_update` is not emitted at all.** Zero updates on a blocking run
and zero on an async one. There is no live child transcript any more: expanding a
run card shows nothing until it finishes, and `traceFromUpdate` in the renderer is
dead weight kept only against a pin that brings streaming back.
`tests/agents-bridge.test.ts` asserts the ABSENCE, so that day is loud.

**`tool_execution_end` carries no `args`.** Args live on `tool_execution_start`
only, so a delegation must be found by correlating START→END on `toolCallId`. This
is not cosmetic: `subagent-async-bridge`'s foreground assertion had been passing
VACUOUSLY (`undefined?.result?.details?.asyncId` is falsy, so "no asyncId" held for
a delegation it never found). It also confirms the caption design — the bridge
takes the task from its `tool_call` handler and the renderer from
`tool_execution_start`, both of which do carry it.

**The redaction is real on the wire**, not just in source: `details.results[].task`
came back as the literal `[prompt redacted]` while
`tool_execution_start.args.task` still held the text the user typed.

**⚠ Context cost regressed, and this is the one to decide about.** What enters the
main agent's context per delegation:

| Path | `toolResult.content` | contains the answer? |
|---|---|---|
| async (shipped default) | **1,255 chars** — a `Run fan-out: n/64 used` receipt | no; it arrives on the triggered turn |
| blocking (`async:false`) | **4,947–5,532 chars** | yes |

The blocking figure is the whole workflow return JSON inlined: launch-contract
digest, resolved-extension hashes, artifact paths, usage, acceptance scaffolding,
`childReport`, `toolCalls` — around 5 KB to carry a one-word answer. No `"role"`
scaffolding, so the child TRANSCRIPT still stays out and the isolation contract
holds; but PRD §12's "only the call and the final result enter the main agent's
context" now costs ~5 KB a delegation on that path. `subagent-context.test.ts`
branches on the path (the model picks `async` itself and picked differently on
consecutive identical runs) and pins both bounds, with the blocking one marked as a
regression watch rather than an endorsement.


### The delivery repair — giving the parent its child's whole answer (2026-08-19)

Upstream truncates the completion payload at a hardcoded 1,000 chars
(`subagent-executor.ts`, `formatWorkflowValue(v).slice(0, 1_000)`; still present at
0.51). Measured: a 4,107-char child report arrives as 1,108 chars of JSON cut
mid-string, and the model responds by announcing the truncation, calling
`subagent_wait`, polling `subagent {action:"status"}` and reading the artifact —
**four tool calls for one delegation**, three of them visible as plumbing.

`hv-subagent-delivery.ts` repairs it: the bridge remembers `results[].output` from
`subagent:async-complete` and substitutes it into the injected message from the
`context` hook. Measured after: **one** tool call, the model reporting from the full
text, no truncation narration.

Three measured facts the design rests on — each would have broken it silently:

| Fact | Why it matters |
|---|---|
| The notify IS in the `context` hook's `messages`: `{role:"custom", customType:"subagent-notify", content:<string>, …}` | The whole approach depends on it being rewritable. Probed BEFORE building. |
| The id in the message is the **child's** run id (`"runId": "65227ea7"`), not the workflow's async UUID (`a2df09d4-…`) | Keying by the workflow id — the one we track everywhere else — would never match. `results[].runId` is the join. |
| `details` on the persisted message is EMPTY | The full output cannot be recovered from the message; it must be captured at completion. |

It is cheaper than what it replaces: the model reads the artifact anyway, so the
full text enters context either way; the tool call, its result envelope and the
narration are what disappear.

Deliberately NOT persisted, and deliberately not applied to the session file: a
completion and its delivery share a turn, multi-KB outputs through `appendEntry`
would bloat every session, and the file should keep a faithful record of what
upstream actually sent. The repair applies to what the model SEES, per request.

Refusals (all tested): unrecognised header → untouched (a future rewording must
arrive unmangled); more than one child named → untouched (a parallel fan-out would
otherwise get every message replaced by one child's answer); redacted, absent, or
over 32 KB → defer to the artifact, where `describeSubagentArtifact` already labels
the read legibly.

## pi-subagents 0.51 + pi-mcp-adapter 2.26.1 — what the bump moved (2026-08-19)

Method, in the order it was actually done: diff the packed tarballs
(`npm pack pi-subagents@0.50.0 pi-subagents@0.51.0`, same for the adapter), move the
pins, run the non-live suite for a red inventory, then measure the wire with
`scripts/probe-050.ts`. Source reading came first and was necessary; every claim
below that could be settled on the wire was.

### The red inventory was EMPTY, and that is not the same as "nothing changed"

`npm test` at the new pins: 188 files / 2017 tests, exit 0, before any repair. Two
reasons, both worth knowing:

* pi-subagents' exports map grew 11 → 12 subpaths (`./external-job-provider` added),
  and the contract case asserts the *property* — neither internal is exposed by any
  subpath — instead of counting them. So it correctly did not care.
* the three pi-mcp-adapter files our pin-bump gates read (`mcp-auth.ts`,
  `mcp-auth-flow.ts`, `utils.ts`) are **byte-identical** 2.25.0 → 2.26.1 (verified by
  `md5` over the unpacked trees). `supportsOAuth`'s "any custom header disables
  OAuth" heuristic is unchanged, so the Miro workaround in `plugins/mcpImport.ts`
  still applies and still cannot be deleted.

The one regression 0.51 carries for HappyVibe is invisible to every test we had.

### #1225: completion delivery is scoped to a PROCESS

`shared/completion-owner.ts` (verbatim, installed tree):

```ts
const COMPLETION_OWNER_KEY = Symbol.for("pi-subagents.completion-owner-id");
/** Stable for one parent Pi process across extension reloads. */
export function currentCompletionOwnerId(): string {
	const runtime = globalThis as CompletionOwnerGlobal;
	runtime[COMPLETION_OWNER_KEY] ??= randomUUID();
	return runtime[COMPLETION_OWNER_KEY];
}
```

`runs/background/notify.ts:279`, inside `deliver` (verbatim):

```ts
if (result.source !== "foreground" && (!state.completionOwnerId || result.completionOwnerId !== state.completionOwnerId)) return Promise.resolve(false);
```

Read at `extension/index.ts:422`, once, during registration:
`completionOwnerId: currentCompletionOwnerId()`.

There is no config key that disables the scoping (grepped for
`completionOwnerScoping|disableCompletionOwner|ignoreCompletionOwner`: nothing).
`result-watcher.ts:291` DOES have a fallback when deciding whether to *read* a
result file — `identity.asyncDir && existsSync(<asyncDir>/mission.json)` — but
`deliver()` has none, so the mission file cannot rescue delivery.

At 0.50 the guard was session-id only. HappyVibe respawns Pi deliberately and
resumes the same session file (`startClient(meta, true)`) precisely so a detached
run is never orphaned, so at 0.51 the resumed parent is refused its own child's
result: the artifact lands on disk, `/hv-subagent-list` resyncs the card, and the
answer never reaches the model.

**Measured deterministically, key-free, in 5 ms** (a behavioural harness over
upstream's real `notify.ts` — `tests/pi-subagents-contract.test.ts`, not a source
scan). `state.completionOwnerId` vs the completion's:

| completion's owner | state's owner | `deliver()` | messages sent |
|---|---|---|---|
| `hv-session-42` | `hv-session-42` | `true` | 1 (`subagent-notify`) |
| `the-dead-process-uuid` | `a-fresh-process-uuid` | **`false`** | **0** |
| absent | `hv-session-42` | `false` | 0 |
| any, `source:"foreground"` | `hv-session-42` | `true` | 1 |

The foreground row is why `async:false` was never affected, and why the fix only
has to cover the async path.

### The fix, and why it is its own extension

`pi-runtime/extensions/hv-owner-seed.ts` claims the registry symbol from
`HV_SUBAGENT_OWNER` (= `hv-<HappyVibe session id>`) at module load. `??=` is the
whole mechanism: a value already in the registry wins and upstream never overwrites
it.

It cannot live in `happyvibe-bridge.ts`. Pi loads `-e` extensions strictly
sequentially — import, then factory, one at a time in argv order
(`core/extensions/loader.js:440`) — and the id is minted inside pi-subagents'
factory, while the bridge is pinned LAST so the permission gate sees post-mutation
tool input. So the seed is a separate extension loaded FIRST. It registers no tools
and no `tool_call` handler, so it cannot change what the gate sees; the invariant is
that the *gate* is last, not that nothing precedes it. It is also import-free, and
`loadExtension` wraps load in try/catch and returns an error string rather than
crashing the session.

Measured end of that chain: with `sessionId` passed, a real delegation's
`status.json` reads `ownerId=hv-probe-session` where the previous runs read
`3ab22fb3-…` and `7200a18a-…`, and the run still reaches `state=complete`.

**RESOLVED 2026-08-19 in the running app — an async run does NOT survive its
parent, and never could have.** A live delegation's `status.json` recorded
`pid: 57451`, and 57451 was one of exactly two `pi` processes on the machine, both
direct children of the Electron main (`ppid 56697`) in the app's own process group
(`pgid 56652`), both started with the app. The delegation's child agent was an
ordinary non-detached child of it (same pgid), and there was **no process group
anywhere on the machine other than the app's** — i.e. no detached runner exists.
`async-execution.ts:521` does spawn with `detached: true`, so that path exists; it is
simply not the one a top-level workflow-mode delegation takes.

That fully explains both probe deaths below, which were never a mystery: killing the
parent Pi kills the process that OWNS the run. It also promotes `activity.asyncRuns`
from belt-and-braces to LOAD-BEARING: it is the only thing preventing hibernation or
MCP live-reload from destroying an in-flight delegation.

**Correction (2026-08-20): this does NOT make the owner-id refusal unreachable, and
saying so here was wrong.** No live *process* survives a respawn — but a result is a
FILE. A child that finished and wrote its result before its parent went away leaves it
on disk, and the next session start scans it: `extension/index.ts` calls
`primeExistingResults({ triggerTurn: !recovering })`, which routes through
`scheduleResult → handleResult → notifier.deliver` — the same owner-scoped guard the
table above exercises. So the reachable sequence is:

1. the sub-agent finishes and its result file is written, stamped with the launching
   process's `completionOwnerId`;
2. the session respawns before delivery (app quit, or an MCP live-reload landing in
   that window);
3. the resumed session primes the file, the ids do not match, and delivery is refused.

And it is refused *permanently*, not transiently: `if (!accepted) scheduleResult(file,
triggerTurn, RETRY_DELAY_MS)` retries without marking the file delivered, so it is
retried forever by a process whose id can never match, and by every process after it.
The answer is lost silently and the result file never clears. With the seed the id is
the HappyVibe session id, which is stable across exactly that respawn, so it delivers.

The window is narrow — seconds between the result landing and the notify — but it is
precisely the case `startClient(meta, true)` exists to protect, and the failure is
invisible. Pinned as a ROUTE assertion in `tests/pi-subagents-contract.test.ts`
(session-start priming, the shared handler, and the retry-on-refusal), because the
behavioural refusal test alone would stay green if upstream ever stopped priming.

The original, now-explained observation: Two `scripts/probe-050.ts respawn` attempts both lost the child on
the parent's SIGTERM — the first killed it ~0 s after dispatch (`state: failed`,
*"Async runner process 12506 exited or disappeared before writing a result"*, 2.7 s
in), the second after a 25 s settle with `aliveBeforeKill=true` confirmed by
`process.kill(pid, 0)` and then `aliveAfterParentKill=false`. The runner is spawned
`detached: true` with `stdio: ["ignore", …]` (`async-execution.ts:521`) and nothing
in pi-subagents kills a runner on shutdown (`terminateRunnerBeforeProceed` is only
on the preflight path), which is consistent with the topology above rather than
mysterious. CLAUDE.md's "detached runs SURVIVE a parent respawn" was simply WRONG and
has been corrected at its source.

The probe's `respawn` mode now settles the child, proves it alive before the kill,
re-checks after, and prints the run's own final state — so "not delivered" can never
again be confused with "the child failed". That confound cost two runs here.

### Four shape facts, re-measured and all UNCHANGED at 0.51

From `/tmp/probe-050-{async,fg}.json` (real delegations, real model):

| fact | async | foreground |
|---|---|---|
| `subagent:async-started` emitted | **no** | n/a |
| `tool_execution_update` count | **0** | **0** |
| `details.asyncId` on the dispatch result | `2250388a-…` | `undefined` |
| `details.mode` | `workflow` | `workflow` |
| `args` present on `tool_execution_start` / `_end` | yes / **no** | yes / **no** |
| `results[0].messages` / `.toolCalls` | n/a | `undefined` / 1 item |

So the card re-key, the caption store, the START→END correlation and the absence of a
live child transcript all stay exactly as 0.50 left them.

### The delivery repair still fires

Instrumented in the bridge's own handlers (then reverted), on a real async run:

```
[HVPROBE] async-complete: results=1 keys=[["workflowKey","agent","runId","output",
          "outputState","success","artifactPaths","status","summary","index",
          "artifactPath","sessionPath","children"]]
[HVPROBE] runIds=["cac04cb8-…"] outputLens=[4098] summaryLens=[4098]
[HVPROBE] store size after remember=1 keys=["cac04cb8-…"]
[HVPROBE] context: 1 notify msg(s), store size=1
[HVPROBE]   notify len=1108 head="Background task completed: **workflow**\n\nWorkflow completed with 1 chi"
            runIdsInMsg=["cac04cb8-…"]
[HVPROBE] substitution fired=true
```

`output` and `summary` are still byte-identical (4,098 each), and the id in the
message is still the CHILD's run id — the join `hv-subagent-delivery.ts` keys on.

Worth recording because it nearly produced a false finding: the message Pi
*records* is still the truncated 1,108-char one, cut mid-string at section 6 with
`**workflow**` as the agent. That is expected — the repair rewrites what the model
receives via the `context` hook and deliberately does not rewrite the session file.
Reading the recorded transcript therefore looks exactly like a broken repair. The
model's answer looks fine either way, too, because a truncated report of a patterned
file lets it extrapolate the tail: it wrote all 40 sections with correct values from
the first 6 plus "an arithmetic progression increasing by 7". Only the
instrumentation settles it.

### A blocking delegation roughly doubled its envelope

`toolResult` content text, same probe task as 0.50's measurement:

| | 0.50 | 0.51 |
|---|---|---|
| async (receipt only) | 1,255 | **1,214** (unchanged; 1,255 was the `JSON.stringify` figure) |
| blocking (`async:false`) | 4,947–5,532 | **14,785** |
| └ of which the child's own `finalOutput` | — | 4,569 |
| └ envelope (upstream scaffolding) | — | **~10,216** |

Envelope pieces, none of which scale with the answer: `acceptance` 988,
`artifactPaths` 842, `launchResolvedExtensions` 220, `toolCalls` 211,
`modelAttempts` 187, `usage` 99, `launchContractDigest` 66, `effects` 79,
`progressSummary` 48. The rest is the formatted workflow-return prose around them.

`tests/subagent-context.test.ts` enforced `< 8_000`, so it would have failed the
moment the model chose to block — it chooses on roughly half of runs and took the
async path (1,214) on the run that surfaced this, so the failure was latent. The
bound is now 20,000 on the total AND 13,000 on the envelope, and the test's log line
prints the two separately, because a chattier child legitimately costs more while a
fatter wrapper does not.

### Adapter 2.26: a second execution surface, and upstream agreeing with us

* **`requestHeadersCommand`** (#353) `spawn`s a command per outbound
  Streamable-HTTP/SSE request and uses the output as headers. Per-server config is
  readable from a workspace `.mcp.json`, so it is the same class as
  `resolveCommandSecret`'s `!` prefix. Pinned, along with an assertion that neither
  the curated catalog nor the plugin importer ever authors the key.
* **#365** fixed MCP panel commands hanging in RPC by adding
  `ctx.hasUI && ctx.mode === "tui"` (`isTuiMode`, `canRenderPanel`). Its docstring
  independently states this repo's own `hasUI` finding, and Pi's dist confirms the
  mechanism: `rpc-mode.js:152` is `async custom() { return undefined; }`, so the RPC
  uiContext is real per METHOD — `input`/`select` ride `extension_ui_request`,
  `custom()` no-ops. This makes the pi-subagents hazard sharper: its turn-end drain
  still gates on bare `ctx.hasUI` (`index.ts:689`), the only reason it stays dormant
  for us.

### Not adopted

`repairScan: true` (new on `listAsyncRuns`) would close the accepted `.active-runs`
upgrade hole and sweep the stale markers, and is deliberately declined: the 0.50
decision stands (no migrator, no release note, no users), the markers are filtered by
state today, and #1162 exists to stop scanning. Measured on this machine while
probing: ~140 run dirs under one `pi-subagents-uid-*` root, so a scan is not free.
30-day retention (`ASYNC_RETENTION_DAYS = 30`) should now bound that growth on its
own; it runs in a `worker_threads` worker, unref'd, 60 s after activation — an
in-process thread, not a child process, so it carries no Dock-icon hazard.

### The GUI pass, done in the running app (2026-08-19)

Driven over CDP against the live dev app on this branch (dev server started 11:16:45,
after the last commit, so it was running this code — its two boot-time `pi` processes
both carried the seed). One delegation, `code-explorer`, in a real workspace.

Observed on the CHAT surface, sticky card while running:

* names the agent — **`code-explorer`**, not `workflow`
* shows the real task text, not `[prompt redacted]`
* elapsed time advancing (`working · 3m 18s`), STOP control present, expand chevron
* the composer switches to "Subagents are working in the background — keep chatting;
  results drop in when…", i.e. the turn ended at dispatch

Observed after completion:

* the sticky card **slid away** (`stickyCardStillPresent: false`)
* the answer landed in full in one turn — 77 files counted, plus a paragraph on the
  biggest file
* **absence assertions, all confirmed by scanning the rendered transcript**: no
  `subagent_wait`, no status poll, no `_output.md` artifact read, and none of the
  strings `truncat`, `let me fetch`, `incomplete`, `prompt redacted` — nor the word
  `workflow` anywhere on the surface

And the seed, end to end in the real app rather than in a probe: the run's
`status.json` read `ownerId=hv-76c2fa9a-d24a-458d-984d-41ec7b016088` — the `hv-`
prefix plus the HappyVibe session id — where an unseeded run reads a bare randomUUID.
The maintainer's own earlier test in the same session carried the same owner id and
completed, so both runs exercised it.

This is also the measurement that resolved the child-survival question above: the same
`status.json` named the session's Pi as the run's owner, and no detached process group
existed on the machine.

## Subagent delegation concurrency (2026-08-21, pi-subagents 0.53.0 / Pi 0.84.2)

**Question:** can the bridge safely widen a session-scoped capability ceiling for
one approved delegation and restore it afterwards? That design needs our
`tool_call` gate and the child's spawn to be serialized with respect to each
other. **They are not**, and the answer came out of Pi's own agent loop rather
than a behavioural probe — which is the stronger result, because a probe can only
ever show that concurrency *was not observed*.

**Measured in `@earendil-works/pi-agent-core/dist/agent-loop.js`:**

- `executeToolCalls` (`:287-293`) picks between two paths. It takes the sequential
  one only when `config.toolExecution === "sequential"` **or** some tool in the
  batch declares `executionMode === "sequential"`.
- `Agent` defaults `toolExecution` to **`"parallel"`** (`agent.js:134`,
  `runtimeOptions.toolExecution ?? "parallel"`), and pi-coding-agent never passes
  the option at all — zero hits for `toolExecution` in its whole `dist/`. So the
  parallel path is what HappyVibe runs.
- pi-subagents declares `executionMode: "sequential"` on exactly three tools —
  the watchdog permission arbiter, watchdog review, and the LLM intent arbiter —
  and **not** on `subagent`. No Pi builtin declares it either. So a batch of two
  delegations does not opt into the sequential path.
- In `executeToolCallsParallel` (`:331-365`) the `for` loop awaits
  `prepareToolCall` for every call **in order** — and `prepareToolCall` (`:393`,
  hook at `:405`) is where `config.beforeToolCall` fires, i.e. our gate
  (`agent-session.js:223`, `_installAgentToolHooks`). Each non-immediate call is
  then pushed as a **deferred thunk**, and line `:365` runs the lot under one
  `Promise.all`.

**Consequence.** Gates are strictly serialized; executions are concurrent. Since
the child is spawned during *execution*, two delegations approved in one turn
spawn against whatever the session ceiling holds at that moment. A window opened
on A's approval and closed at A's `tool_execution_end` therefore overlaps B's
spawn, and B — if its agent declares no `tools:` — would inherit A's widened set.
`emitToolExecutionEnd` living inside the thunk does not help: the windows still
overlap.

**Design conclusion.** A per-delegation ceiling is not implementable through the
capability-ceiling registry, because that registry is keyed by **session id**
(`capability-ceiling.ts:158-166`) with no per-call key, and nothing in the public
surface lets a caller scope a ceiling to one delegation. The ceiling is therefore
**monotonic within a turn**: it rests at read-only, widens when a human approves
more, and is restored to read-only at a point where nothing can be spawning.

Two things keep that sound rather than merely convenient:

1. **The approval prompt shows the preflight-RESOLVED toolset**, which is computed
   against the live ceiling. So a delegation running under a widened ceiling is
   shown as such — the prompt can never understate what a child will get, which
   is the invariant that actually matters. Honesty replaces isolation here.
2. **An agent declaring no `tools:` is refused while the ceiling is wider than
   read-only.** That is FR3 stated as an enforcement rather than a side effect: an
   undeclared agent's reach must not depend on what some other agent was approved
   for earlier in the same turn.

Restoring at `agent_end` with no async runs in flight is safe for the same reason
the window was not: at that point there is no pending thunk left to spawn.

## The child guard (2026-08-21, FR4 measured end to end)

`hv-child-guard.ts`, injected by `pi-runtime/bin/pi-node.sh` (which IS
`PI_SUBAGENT_PI_BINARY`, i.e. children only). Proven against a real child Pi, not
a mock — `tests/child-guard-bridge.test.ts`, 10.2 s.

The test is built so that one pass exercises four separate mechanisms, because
each is useless without the others:

1. a fixture agent declares `tools: read, write`, so approving it must **widen the
   session capability ceiling** — otherwise the child has no `write` to attempt;
2. the child therefore launches with `write` in its `--tools`;
3. the wrapper injected the guard into it;
4. the guard clamped the write's `ask` to a **deny** and recorded it.

**The row it wrote, verbatim** (`<HV_CHILD_AUDIT_DIR>/<runId>.jsonl`):

```json
{"decision":"deny","tool":"write","wouldHave":"ask","source":"child",
 "summary":"{\"path\":\"note.txt\",\"content\":\"HI\"}"}
```

Four things that row establishes, none of which a unit test could:

- **The wrapper injection works.** The file existing at all means
  `hv-child-guard.ts` loaded inside a child. A wrong path would leave the
  directory empty forever, silently — which is why the test's first assertion is
  simply "a row exists".
- **`PI_SUBAGENT_RUN_ID` is set and is a UUID** — the filename was
  `0381633a-80c6-423d-b51a-c1bfd3bdc69b.jsonl`. (The env var is
  `PI_SUBAGENT_RUN_ID`, not `SUBAGENT_RUN_ID`; set at `pi-args.ts:783`.)
- **The clamp is what fired, not a rule.** `wouldHave: "ask"` says the parent
  would have prompted; there was no `write` rule in the fixture. A deny that
  merely echoed a configured rule would prove nothing about the clamp.
- **Exactly ONE row.** The child attempted the write once and stopped rather than
  retrying or working around it, which is what the block reason asks for in words
  ("Do not retry it and do not work around it"). A model looping on a denied tool
  burns the whole run and the user sees only a timeout, so this is worth noticing.

The test also asserts `note.txt` does not exist afterwards. The audit row could in
principle lie about what happened; the filesystem cannot.

**Why the audit file and not a Pi event:** `tool_execution_start` fires BEFORE
tool_call handlers, so it can never prove a call was blocked — a test asserting on
it passes whether the guard works or not. The audit row is written by the code path
that made the decision, so it is the only honest witness available from outside.

## The child session file (sub-agent cost, 2026-08-22, pi-subagents 0.53.0)

Where sub-agent money comes from, and the retraction of the mechanism first
proposed for it.

**A sub-agent child writes an ordinary Pi session file**, at:

```
<sessionsDir>/<parentSessionBasename>/<runId>/run-<idx>/session.jsonl
```

Measured in the installed tree:

| Claim | Evidence (0.53.0) |
| --- | --- |
| the root sits beside the PARENT's own session file | `src/extension/index.ts:267-273` — `path.join(path.dirname(parentSessionFile), path.basename(parentSessionFile, ".jsonl"))` |
| one directory per run | `src/runs/foreground/subagent-executor.ts:5440` — `sessionRoot = path.join(baseSessionRoot, runId)` |
| one directory per step, leaf `session.jsonl` | `:5454`, `:5470` |
| children always get one | `:3365` passes `sessionDir` unconditionally → `subagent-runner.ts:2333` `sessionEnabled` |
| per-turn cost is recorded | `subagent-runner.ts:728` — `usage.cost += eventUsage.cost?.total ?? 0` |

Two consequences. It is **not** in a tmpdir, so it lives as long as the parent
session — which is what lets a reopened session show the same numbers it showed
live. And it is the same format `src/main/calls.ts` already parses, so a child
needs no new parser, no new price table, and gets the same three-state billing
as the session's own calls (PRD §19).

`status.json` also exposes the live child's path on a public `sessionFile` field
(`shared/types.ts:575`, `:588`; written `subagent-runner.ts:2531`). We do **not**
use it: a completed run's `status.json` is in a tmpdir that is gone by the time a
session is reopened, so the layout resolver has to exist anyway, and reading the
status field for the live case only would be a second mechanism buying nothing.
`store.ts childSessionFiles` is the one resolver; `tests/pi-subagents-contract.test.ts`
pins the layout so a pin bump fails loudly rather than silently returning nothing.

**RETRACTED — `<asyncDir>/events.jsonl` carries no cost data.** The first draft of
the cost page proposed tailing it and summing per-turn `message.usage.cost.total`.
Every append to that file in 0.53 is lifecycle, control, steering, chain-append or
nested-interrupt **diagnostics**; grepping all of them for `cost` returns **zero**
hits, and there is no assistant-usage line in it at all. The claim had been reasoned
from the fact that the runner accumulates cost in memory (`:728`), not measured at
the file — the same failure mode as the 0.50 bump audit. Recorded here so nobody
re-proposes the tailer.

**Known ceiling, stated rather than discovered later:** the `subagent` tool schema
accepts a caller-supplied `sessionDir` (`src/extension/schemas.ts:330`). A
delegation for which the MODEL passes one relocates the child session outside the
resolver's root and contributes no rows. HappyVibe never passes it; the contract
test is what makes a layout change loud.

### The two ids (measured 2026-08-22, in the running app)

A delegation has **two** identifiers, and confusing them is silent.

| id | what it names | where it appears |
| --- | --- | --- |
| workflow **async id** | the `asyncDir` under `pi-subagents-uid-<uid>/async-subagent-runs/` | `details.asyncId`, the status poller's key, `subagent.async_started` / `async_complete` rows |
| run **inner id** | the child session DIRECTORY under the parent's session dir | `steps[].sessionFile`, and the `subagent.audit_rollup` row |

Three real runs:

| async id | child directory |
| --- | --- |
| `72e6fd2e-cc28-4de3-8b36-0d682851079d` | `8a2f9f62-d468-4120-880b-97738d611854` |
| `4d79e293-a2b0-4356-b318-2c39da7e374e` | `439ff3d5-24aa-470f-a153-fdf0271671ab` |
| `1dcd9ad3-aea8-43a3-bbcb-f4e4590d7cf8` | `e3752c5d-3b25-4f3a-ae03-80c6610de84a` |

So a child path **cannot** be derived from any id the app holds. The link is
`steps[].sessionFile` on `status.json` — note the RUN level has **no**
`sessionFile` field at all (`sessionRoot` is there, the parent's directory, and
is not enough). `steps[].agent` carries the agent name, which no path contains.

Verified against a real run: upstream's own `totalCost.costUsd` (0.00096917)
equals what `calls.ts` computes from the child's session file, so parsing the
child file and trusting upstream's total are the same number two ways.

**Why this matters beyond this feature:** the failure mode is a lookup that
returns nothing rather than something wrong, so nothing throws and no test goes
red. The unit fixtures used one id on both sides and passed; only running the
real app caught it. Fixtures for anything keyed on a run id should use two
different ids on purpose.

## pi-subagents 0.58 — what the bump moved (2026-08-28)

Pin `0.53.0 → 0.58.0`, five releases. Pi stays `0.84.2`, `pi-mcp-adapter` `2.26.1`,
`typebox` `1.3.7` — 0.58's peer range is still `"*"`, so nothing was forced, and one pin
moving means one blame surface. Measured against the real installed package and, for
everything behavioural, on the wire via `scripts/probe-050.ts` — not read from the
changelog. Provider: OpenRouter `deepseek/deepseek-v4-flash`.

**Balance checked first, and it mattered.** `curl` against both providers before believing
anything: OpenRouter `200`, **DeepSeek `402 Insufficient Balance`**. The resolver
(`tests/liveModel.ts`) prefers OpenRouter when both keys are present, which is the only
reason this batch measured anything at all. This is the second time that 402 has been sitting
there waiting to be misread as a regression.

### The red inventory: two non-live, one live, and all three were honest

**1. The owner-scoping source scan (`pi-subagents-contract.test.ts`).** 0.58 EXTRACTED the
comparison `result.completionOwnerId !== state.completionOwnerId` out of `notify.ts` into a new
`src/runs/background/result-delivery-ownership.ts` (`owns(sessionId, completionOwnerId)`),
leaving the same check as `notify.ts`'s inline default (`notify.ts:342`). The behavioural
half of that group — a harness over the real `notify.ts` — stayed green throughout, including
"REFUSES when the owner differs", so nothing about the guarantee moved. The scan now tracks
the code to its new home.

0.58 also adds the first thing on the delivery path that looks like a FALLBACK, and it is worth
being precise about because it could have made `hv-owner-seed.ts` redundant: `claimPredecessor`
(#1531, "deliver async results from an explicitly replaced predecessor session without
accepting unrelated session results"). It does not. The claim is keyed BY the current owner id
(`claimed.set(previousRuntimeSessionId, owner)`) and `owns` still demands
`completionOwnerId === owner` before it ever looks at a session id. A respawn that mints a
fresh `randomUUID()` is refused exactly as before. The seed stays load-bearing, and the
relationship is now pinned so a future relaxation is loud.

**2. The malformed-`pi-permission-system` control INVERTED (`subagent-adversarial.test.ts`).**
That test asserted that without a ceiling, a stray `<agentDir>/extensions/pi-permission-system`
directory lacking a `package.json` makes argv build THROW — a robustness property found by
accident, where our ceiling's `denyExtensions` was the one thing standing between a stray dir
and every delegation failing. At 0.58 it no longer throws, and 0.54 (#1339) is why: the
resolver is now reached only when the launch carries explicit native permission rules
(`hasPermissionRules(input.permissionRules)`, `pi-args.ts:541-544`).

HappyVibe writes **no `permissions` key at all** (FR12, built-measured-and-removed 2026-08-21,
absence-pinned), so that second gate is shut for us too. The fragility therefore has TWO
independent gates now rather than one. The test was rewritten to assert the new shape AND to
prove the fragility is still real — a launch WITH permission rules and WITHOUT a ceiling still
throws — so it keeps measuring something instead of asserting a tautology. This also means the
FR12 "write no permissions key" decision is load-bearing for a robustness property nobody knew
it touched.

**3. `tool_execution_update` is BACK (`agents-bridge.test.ts`) — the headline.** See below.

### The live child transcript came back, and the wait-for-a-pin bet paid off

At 0.50-0.53 `tool_execution_update` was not emitted at all for a subagent call: zero on a
blocking delegation, zero on an async one (measured 2026-08-17). Expanding a run card showed
nothing until the run ended, `traceFromUpdate` in the renderer was dead weight, and
`tests/agents-bridge.test.ts` asserted the ABSENCE with a comment saying it should fail loudly
the day upstream restored streaming.

It did. **38 updates** on the first 0.58 live run where 0 were expected; **53** on the probe.

Measured shape, blocking delegation (`probe-050.ts fg`):

```
tool_execution_update  ×53      keys: [args, partialResult, toolCallId, toolName, type]
  partialResult.details            keys: [context, mode, progress, results, runId]
  partialResult.details.mode       "single"          ← not "workflow" (see below)
  partialResult.details.results    length 1
    [0].agent                      "code-explorer"
    [0].messages                   ABSENT            ← unchanged since 0.40
    [0].toolCalls                  [{text, expandedText}]  ← still the transcript source
    [0].finalOutput                set on 1 of 53 updates (the last)
    [0].progress                   {index, agent, status, task, recentTools[]}   ← NEW
    [0].progressSummary            {toolCount: 1, tokens: 5231, durationMs: 45274}  ← NEW
    [0].transcriptPath             …/subagent-artifacts/<runId>_<agent>_0_transcript.jsonl  ← NEW
    [0].usage                      {input, output, cacheRead, cacheWrite, cost, turns}
tool_execution_end     ×1        details.mode "single", results[0].messages ABSENT
```

**Only the DELIVERY was restored, not the shape** — which is why the renderer needed no edit at
all. `traceFromUpdate → toResults` already reads `partialResult.details.results[]` and already
falls back from absent `messages` to `toolCalls`. Streaming is genuinely progressive: the
`toolCalls` JSON grows from 2 bytes (empty) to 214 (one call) across the 53 updates, and
`finalOutput` lands only on the last, so a card expanded mid-run fills in as the child works.

Two test consequences. The live assertion is now a PRESENCE (`toBeGreaterThan(0)`), never a
count — the number tracks how chatty the child is, which is the model's business. And the
measured payload is pinned as a verbatim fixture in `tests/agents-renderer.test.ts`, including
the new sibling fields, so an over-eager mapping change trips a test rather than an expanded card.

Also corrected in `CLAUDE.md`: the 0.51 note read #1229/#1230 ("async workflows do not have
inline `live-card` projection", documented as intended) as making the missing live transcript a
**permanent property**. That conclusion was wrong. Those issues are scoped to ASYNC workflows;
the blocking path streams again at 0.58.

### The four verify-first probes

**Probe 4 — the 0.55 single-child unwrap, confirmed on the wire.** 0.55 stopped wrapping public
single-child launches in a workflow. At 0.58 an async `{agent, task}` delegation returns
`details: {mode: "single", runId, asyncId, asyncDir, …}` (`async-execution.ts:1967`) and only a
real multi-child `workflowScript` is `mode: "workflow"` with a `missionId`. On the blocking
path, measured: `details.mode === "single"` on both update and end, and `asyncId` absent —
consistent with the standing note that `asyncId` rides the async dispatch result only.

**The run card was unaffected, and the reason is the interesting part.** It is keyed off
`details.asyncId`, not off the mode — a decision made when 0.50 removed the card. The mode has
now changed twice underneath that key and the card never moved. `subagent:async-started` **is**
emitted at 0.58 (`async-execution.ts:1413`, `:1940`) — an earlier line in this section said
nothing emits it, which was a bad measurement: the grep looked for the literal string and only
matched the constant's own definition, while every emitter references
`SUBAGENT_ASYNC_STARTED_EVENT`. The 0.50 measurement it was generalised from was narrower and
still holds — the WORKFLOW path never emitted it — and 0.55's unwrap put delegations back on the
direct async path, which does. The re-key from `tool_execution_end` stays load-bearing anyway,
for a different reason: the notify carries no `toolCallId` and so cannot caption a card correctly
when two same-agent delegations are in flight.
`CLAUDE.md`'s "every delegation is a WORKFLOW from 0.50" is corrected at source.

**Probe 1 — upstream's builtins ARE discoverable, and the hole was real.** Two probe runs,
identical except for whether `<agentDir>/settings.json` carries our overrides.

| | agents the model was offered |
|---|---|
| **no settings.json** (upstream default) | all 13 upstream builtins — including `claude-code`, `claude-code-writer`, `codex-exec`, `codex-exec-writer`, `cursor-agent`, `cursor-agent-writer` — plus our two |
| **with `writeSubagentSettings()`** | the five other external names GONE; `claude-code` appears only in the refusal text, because the probe prompt names it explicitly |

So without the fix the model can see and pick an external CLI agent, `-writer` variants
included. With it, they are not in the roster.

**And the refusal fires ahead of the rule engine, which is the design claim.** The probe's rules
file allows `subagent:*` outright, and a delegation to `claude-code` was still refused verbatim:

```
HappyVibe does not run 'claude-code': it launches a separate claude-code CLI process, so this
app's permission boundary, its capability ceiling and its audit log cannot see or govern
anything it does. Delegate to one of this session's own sub-agents instead, or do the work in
this session where every tool call goes through the gate.
```

Two caveats recorded rather than tidied away. `subagent({action:"detail"})` does not exist —
the valid action is `get` — so the probe's third question returned upstream's action list
instead of an answer. And the roster in both runs contained a stray user agent, `10x`, traced to
`~/.agents/commands/superset/10x.md`: user-scope agent discovery reaches beyond
`PI_CODING_AGENT_DIR`, by a mechanism this pass did not chase. Both runs shared it, so the
comparison above is unaffected — but it means the Agents page under-reports by MORE than
upstream's builtins, which belongs to the fleet round.

**Probe 2 — the child transcript DOES carry thinking blocks, so P4 item 6 is feasible.**
Answered off `results[].transcriptPath`, a field 0.58 newly exposes. The child's own JSONL
(7 records for a 2-turn run) carries real `type: "thinking"` content blocks, and the text is
unmistakably the child's own reasoning rather than a copy of the parent's:

```
line 3  role=assistant  agent=code-explorer  recordType=message
  THINKING[152]: "The task is to read notes.md and write a detailed report listing every
                  section heading and both of its facts verbatim. Let me start by reading the file."
line 7  THINKING[219]: "The file has 40 sections, each with a heading and two facts (fact A and
                  fact B). Let me now produce the detailed report…"
```

The parent's event stream carries its OWN thinking too (`thinking_start` / `thinking_delta` /
`thinking_end`, plus `thinkingSignature: "reasoning"`), which is a separate thing and easy to
mistake for the child's — the child's is only in the transcript file. Note the `toolCalls`
projection does NOT include thinking, so surfacing it means reading `transcriptPath` (path-confined,
the same shape as `subagentStatus.ts` tailing `status.json`). No upstream ask needed.

**Probe 3 — the identity pin is already true; closed by measurement, not by building it.**
`defaultSystemPromptMode(name)` returns `"replace"` for every agent except the builtin
`delegate`, which is `"append"` (`agents.ts:41-43`). Neither bundled agent sets the key, so both
resolve to `"replace"`, and `"replace"` is what selects `--system-prompt` over
`--append-system-prompt` at launch (`pi-args.ts:746`) — the child receives ONLY its own prompt,
never Pi's. So "the child never thinks it is Pi" holds today. Pinned in
`pi-subagents-contract.test.ts` (the default, our agents not opting into `append`, and the argv
consequence) because the symptom of a silent flip would be a sub-agent introducing itself as Pi.

### Unchanged, and pinned because their absence would be silent

- `PROMPT_REDACTED` is still `"[prompt redacted]"` (`shared/utils.ts:12`), and `progress.task`
  in the streamed update reads `"[prompt redacted]"` too — the caption workaround stays.
- The **1,000-char completion truncation** survives (`subagent-executor.ts:4956`,
  `formatWorkflowValue(v).slice(0, 1_000)`, no config), so the delivery repair stays load-bearing.
- `subagent_wait` keeps its name; `waitTool.enabled` is still the sanctioned off-switch.
- The auto-drain still gates on **bare `ctx.hasUI`** (`extension/index.ts:738`) — still the only
  thing keeping it dormant for us, and still not `ctx.mode`.
- Both relative-path imports resolve. The `exports` map is now **13** subpaths (11 at 0.50) and
  lists neither file, so the relative form is still required.
- `Symbol.for("pi-subagents.completion-owner-id")` unchanged; the seed still claims it.

### The six external-CLI builtins — the substantive decision

0.53 shipped 7 builtin agents, all native Pi children the capability ceiling governs. 0.58 ships
**13** (12 bundled files; `advisor` is a name-only alias 0.57 resolves through `oracle`), and the
six new ones declare `runner: {type: "external-cli"}`. They are opaque processes: the ceiling
cannot bound one, the child guard cannot run inside one, their tool calls never reach the audit
log, and upstream refuses ask/deny rules for external runners by design while withholding
extension authority from them (0.56). PRD §12's three layers do not reach inside them.

Enforcement is `EXTERNAL_CLI_AGENTS` / `isExternalCliAgent` (`hv-rules.ts`), checked by the bridge
**before `resolveBoundary`** — that call also widens the session ceiling, and a grant for an agent
nothing can hold to it is worse than no grant. It cannot live in upstream's settings file instead,
and this is measured, not assumed: a PROJECT-scope `.pi/settings.json` override beats the user
scope OUTRIGHT (`agents.ts:1340` returns on the project override before it ever reads the user
one), so a cloned repo could re-enable one.

`writeSubagentSettings()` is hygiene only. Its key is **`agentOverrides`, not `overrides`** —
pi-subagents parses the former INTO an internal field it calls the latter (`agents.ts:1129`), so
the obvious spelling is a silent no-op with no symptom anywhere. Not `disableBuiltins: true`,
which is all-or-nothing and would also remove `worker` and `reviewer`. The refusal set is DERIVED
from upstream's own frontmatter in the contract test, so a seventh adapter fails there.

### A pre-existing bug the bump surfaced, and the blind spot behind it

`const summary` in `happyvibe-bridge.ts` sat AFTER three §12 refusal paths that audit with it —
the ceiling-error, no-boundary and undeclared-agent-inheriting-a-widened-boundary refusals. Each
was a temporal-dead-zone `ReferenceError`.

It failed **CLOSED**, so the boundary held: Pi's `beforeToolCall` re-throws as *"Extension failed,
blocking execution"* (`agent-session.js:236-241`), and `emitToolCall` has **no try/catch of its
own** — unlike `emitUserBash` and `emitContext` immediately beside it, which both wrap handlers
and swallow. But the refusal surfaced as an extension crash with **no `hv.audit` row** instead of
a clean denial naming the boundary, so a denial the audit log exists to record was invisible.

Nothing caught it and nothing would have: **`happyvibe-bridge.ts` is in NEITHER typecheck include
list.** `tsconfig.node.json` names only the pure `hv-*.ts` modules, so TS never saw a
use-before-declaration it would normally reject outright, and no test invoked the handler on those
three paths. Fixed by hoisting one line; pinned by source order in
`tests/subagent-external-agents.test.ts`. When editing the bridge, assume the compiler is not
watching.

### Also fixed: the probe itself had been broken since the boundary round

`scripts/probe-050.ts` wrote a rules file allowing the bare tool name `subagent`. §12 (2026-08-21)
gates a delegation under the per-agent rule name `subagent:<agent>`, so that rule stopped matching,
the permission prompt was raised, and **nothing in the probe answers a `ui-request`** — prompts
never time out by design. The probe hung on `[ui] select` and measured nothing, silently, while
still writing a dump that looked like a result. Now allows `subagent:*` as well (tool patterns are
globs). That deliberately permits `subagent:claude-code`, which makes the roster probe stronger:
the refusal has to fire ahead of the rule engine to pass.

### Found during the GUI pass: the ceiling silently rescues a STALE agent file

The GUI pass repaired this developer's installed builtin agents towards the new bundle and left
the documented one-time `<agent>.md.bak`. The backup showed what had been on disk: an agent
declaring `tools: read, grep, glob, list, ls` — the pre-2026-08-02 bundle, i.e. asking for `glob`
and `list`, which **Pi has never registered** and which pi-subagents >=0.40 treats as a FATAL
strict-allowlist error ("Agent 'x' requested unavailable child tools: glob, list").

The first conclusion drawn from that was **wrong and is retracted**: that delegations to
`code-explorer` must therefore have been failing since the 0.40 bump. The Audit log disproved it
on sight — `SUB-AGENT CODE-EXPLORER · RULES WOULD HAVE ALLOWED` rows for `read` on 2026-08-23,
i.e. children that ran fine.

The reason is §12's capability ceiling, and it is worth writing down because nobody designed it
for this. The ceiling is INTERSECTED with the agent's declared list before the launch plan is
built, so the dead names are gone before upstream's strict check ever sees them. Measured
directly against upstream's own `resolvePiLaunchToolPlan`:

```
declared (the stale agent):  read, grep, glob, list, ls
ceiling (read-only):         find, grep, ls, read
effectiveToolAllowlist:      [read, grep, ls]      ← glob and list dropped, not rejected
```

So a bundled-agent file that goes stale across a Pi upgrade degrades to "the tools that still
exist" instead of failing the run. Two consequences worth keeping: the contract test's
"no bundled agent asks for a tool that does not exist" guards the BUNDLE and cannot see a user's
installed copy, and it does not need to — the ceiling is the backstop. And when a future report
says "my sub-agent is missing a tool", the tool list to compare is
`effectiveToolAllowlist`, not the agent file's `tools:` line.

Unrelated but recorded from the same pass: this developer's `pi-agent/agents` held pre-2026-08-02
content until today even though the content-hash install scheme landed on 2026-08-02 itself
(`9b45d1a`), so the repair fires the first time a build containing it actually runs. Which build
they normally launch was not determined and is not inferred here.

### The run-card caption bug: two same-agent delegations swapped (2026-08-29)

Reported from a real session: two sub-agents spawned, and the sticky cards' captions did not
match the runs — inverted. Confirmed from the session file, and it was worse than a swap.

The model emitted **two `subagent` toolCall blocks in ONE assistant message**, both
`code-explorer`:

```
call_1547cde07cdb4ba6a8f460d2   task = "Explain how the game Beat Saber works…"
call_27de2d17418446e1b8502a81   task = "Explain how the Minesweeper implementation…"
```

What each child actually did, from its own transcript: `753c611a` read `minesweeper.html`
(Minesweeper ×6, Beat Saber ×0); `f94ac707` read `beat-saber.html`. And the caption map persisted
in the session file held exactly one entry:

```json
{"runs": {"f94ac707-…": "Explain how the Minesweeper implementation … works."}}
```

**The Beat Saber run was captioned with the Minesweeper task, and the Minesweeper run had no
caption of its own.**

Two independent causes, both fixed.

**1. `hv-subagent-tasks.ts` keyed pending captions by AGENT, one slot, last-write-wins.** Both
calls were `code-explorer`, so the second stash overwrote the first before either run announced.
The module's own header justified the design with *"Pi runs tool calls one at a time, so the slot
is claimed before the next call writes it"* — **that is false**, and this session is the
counter-example: two toolCall blocks in one assistant message means both stashes land first. The
header also framed the risk as needing "started events in the opposite order"; it does not, and
same-agent fan-out is the normal shape rather than a corner.

Pending is now keyed by **`toolCallId`**, and the authoritative pairing is `bindRun(toolCallId,
runId)` called from the bridge's new `tool_result` handler — the one event carrying BOTH the tool
call id and `details.asyncId`. `claimTask`, which serves the `subagent:async-started` notify and
has no tool call id available, now **refuses when more than one dispatch matches** instead of
guessing. Keying by tool call id is what lets it see the ambiguity at all; a per-agent slot had
already destroyed the evidence.

**2. The renderer let the notify's caption win.** `App.tsx`'s re-key read
`next[detached.asyncId] ?? { ...fg }` — so an already-raised async card kept the notify's
best-effort caption and DISCARDED `fg`, whose `label`/`agent` came from `tool_execution_start`'s
own args and are exact per call. It now merges, preferring the exact values.

Note the interaction that made this reachable at 0.58 specifically: `subagent:async-started` fires
again for single-child delegations (0.55's unwrap put them back on the direct async path), so the
notify is raising cards again — with a caption it cannot pair. Under 0.50-0.53's workflow path the
notify never fired and the card was always built from `fg`, which is exact.

Pinned by `tests/hv-subagent-tasks.test.ts` (the real session's two ids and tasks are the
fixture), plus source-scan assertions that the bridge stashes by `event.toolCallId`, subscribes to
`tool_result`, and that Pi's own `ToolResultEventBase` still carries `toolCallId` — if a pin drops
that, the exact pairing degrades silently back to the guess.

## Two things the 0.58 branch found in USE, not in the diff (2026-08-29)

Both were reported by the user after the bump landed, and neither was caused by it. Recording
them here because each is a class of bug the bump audit could not have caught: one needed two
delegations in one turn, the other needed a session to be deleted.

### The run-card captions swapped — see §The run-card caption bug above

Root cause and fix are written up in that section. The measurement worth carrying forward is the
one about IDs: `details.asyncId`, the `<stem>/` child-directory name, and the
`subagent-artifacts/` filename prefix are three id spaces that only sometimes coincide. Anything
that needs to associate a run with its data must collect ids from every available source rather
than deriving one from another.

### A model the app was silently not using

`[pi:stderr] [pi-subagents] Skipping model 'openrouter/qwen/qwen3.8-flash' due to a cached
exclusion (reason: Subagent produced no output …; expires: 2026-08-30T10:35:51.349Z).`

The store, captured verbatim before it was cleared:

```json
{"version": 1, "exclusions": [{
  "modelId": "qwen/qwen3.8-flash", "provider": "openrouter",
  "reason": "Subagent produced no output (possible model cold-start or empty response).",
  "recordedAt": 1787999751349, "expiresAt": 1788086151349 }]}
```

`expiresAt - recordedAt` = exactly **86,400,000 ms**, i.e. upstream's
`DEFAULT_MODEL_EXCLUSION_TTL_MS = 24 * 60 * 60_000`. The file lives at
`<TEMP_ROOT_DIR>/model-exclusions.json` where `TEMP_ROOT_DIR` is
`os.tmpdir()/pi-subagents-<scopeId>` — **per-UID, so one flaky child disables that model for every
session and every workspace on the machine**, and the only signal is `model-fallback.ts:413`'s
`console.warn`, which `PiClient.ts:61` prints to a dev terminal and nowhere else.

Reachability at 0.58 is worse than at 0.57: #1556 fails an *explicitly* requested model closed
rather than falling back, so a per-agent model override plus one empty response makes that agent's
delegations fail outright for a day.

Two mitigations shipped. `modelExclusions.defaultTtlMs = 5 * 60_000` — upstream applies a
configured value with `shortenExisting`, so setting it also caps records already on disk and a
stale 24-hour entry self-heals at the next start rather than needing the file deleted. And a
`model.excluded` audit row per live exclusion, reported at delegation start and deduped by
model+expiry.

**The path is ours, and that is a deliberate reversal of the obvious approach.** Reading upstream's
default location would mean re-implementing `os.tmpdir()/pi-subagents-<resolveTempScopeId()>`,
which is the same move that produced the MCP keychain drift. `PI_MODEL_EXCLUSIONS_PATH` is
upstream's own documented hook (`EXCLUSIONS_PATH_ENV`), so spawn.ts sets it to
`<agentDir>/model-exclusions.json` and main reads a file it chose. `tests/model-exclusions.test.ts`
pins the env name against upstream's source, so a rename fails there rather than leaving main
reading a file nobody writes.

**The audit row is a third TYPE, and that mattered.** The first design put it through the bridge's
existing `hv.audit` envelope — wrong, because `AuditDecision` is `allow | allow-session | deny`, a
permission vocabulary. Forcing "your model was substituted" into it is the same shape as the
`wouldHave` misreport §6 exists to prevent. It follows `assistant.oneshot` instead: main writes it,
`toAuditRow` discriminates on the event TYPE (not on `data.kind`, which is the bug that misfiled
every permission decision once already), and it carries no cost or token figure because it has
none.

### Session delete left every sub-agent trace behind

Full decision in PRD §5. The disk facts, measured before and after on a real install:

| | before | after the sweep |
|---|---|---|
| orphaned `<stem>/` dirs | 18 | 0 |
| `subagent-artifacts/` files | 80 (2.4 MB) | 36 (752 KB) |
| a named still-referenced run's files | 4 | 4 |

`[hv] swept 18 orphaned sub-agent dir(s), 44 artifact file(s)`

Then one session was deleted through the real dialog: its `.jsonl`, its child directory and its 4
artifacts all went, a different session's 4 stayed, and reopening a *third* session still rendered
its per-child cost (`715 tok · ~$0.0002`) — the regression the sweep risks, since §19's ledger
re-parses exactly those child session files.

**The real-data catch:** `subagent-artifacts/` also holds `.last-cleanup`, pi-subagents' own
retention timestamp, which has **no underscore**. `slice(0, indexOf("_"))` on it returns
`".last-cleanu"` — a plausible-looking id that would have deleted a file we do not own. Every unit
fixture had an underscore, so only running against the real directory surfaced it.
