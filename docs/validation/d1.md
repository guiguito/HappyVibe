# D1 Validation — extension_ui_request over RPC (Gate V4)

> **Re-audited 2026-08-02 against `@earendil-works/pi-coding-agent@0.83.0` — NO DRIFT.**
> Checked field-by-field against `dist/modes/rpc/rpc-types.d.ts`:
> `RpcExtensionUIRequest` still has `select {title, options[]}`, `confirm {title, message}`,
> `input {title, placeholder?}` (all with an optional `timeout?` we deliberately never set —
> permission prompts do not time out); `RpcExtensionUIResponse` still has all three variants
> `{value}` / `{confirmed}` / `{cancelled:true}`. `method:"notify"` still present (:415), and
> every event the bridge relies on still exists: `tool_call`, `tool_execution_start|update|end`,
> `session_start`, `turn_start`, `agent_end`, `queue_update`.
> Nothing in this document needed changing. Re-stamp on the next pin bump.

## Verdict: YES — Path A confirmed

`ctx.ui.confirm()` surfaces over RPC as `extension_ui_request` on stdout and is answered by writing `extension_ui_response` to stdin. The full round-trip was proven empirically.

## Verbatim `extension_ui_request` observed on Pi's stdout

```json
{"type":"extension_ui_request","id":"2a300a28-3aaf-4cae-ba55-ffe8de8401f6","method":"confirm","title":"D1-PROBE","message":"Reply to prove ui-over-rpc works"}
```

## Verbatim working `extension_ui_response` written to Pi's stdin

```json
{"type":"extension_ui_response","id":"2a300a28-3aaf-4cae-ba55-ffe8de8401f6","confirmed":true}
```

## Extension stderr output proving round-trip completion

```
D1-PROBE-RESULT: true
```

## Wire shape summary

| Direction | Key fields |
|-----------|-----------|
| stdout (Pi → client) | `type: "extension_ui_request"`, `id: string`, `method: "confirm"`, `title: string`, `message: string` |
| stdin (client → Pi) | `type: "extension_ui_response"`, `id: string`, `confirmed: boolean` |

**Critical**: For `confirm` dialogs, the response field is `confirmed: boolean` (NOT `value`). This is per `RpcExtensionUIResponse` in `rpc-types.d.ts`.

## select shape — PROVEN empirically by Task 6 (V5-core)

**Verbatim `extension_ui_request` for select observed in bridge.test.ts (2026-07-03):**

```json
{"type":"extension_ui_request","id":"43c31d65-5e14-48b5-a437-c585f3249c69","method":"select","title":"{\"kind\":\"hv.permission\",\"tool\":\"bash\",\"summary\":\"touch forbidden.txt\"}","options":["Allow","Allow for session","Deny"]}
```

**Working `extension_ui_response` for select:**

```json
{"type":"extension_ui_response","id":"43c31d65-5e14-48b5-a437-c585f3249c69","value":"Deny"}
```

**v5 — workspace confinement.** The pure engine (`hv-rules.ts`) gained a
`Verdict.source` value `"outside-workspace"` (action `"ask"`, plus `outsidePath`):
a FILE tool (`FILE_TOOLS` = read/write/edit/multi_edit/grep/glob/ls/list; **not**
bash) whose path arg escapes the workspace root asks by default — even a read that
`SAFE_TOOLS` would otherwise allow. Explicit rules still win (checked first);
`escapesWorkspace(p, ws)` is posix-only and does not realpath (symlink caveat).
When this fires, the `hv.permission` title carries `reason:"outside-workspace"`
and `path:<offending path>` so the modal shows the factual path; an in-memory
"Allow for session" grant covers it just like a default ask.

The response field for `select` is `value: string` — the string value of the chosen option (e.g., `"Deny"`, `"Allow"`, `"Allow for session"`). This matches `rpc-types.d.ts` and is now empirically confirmed. Deny blocked the bash tool call (forbidden.txt was NOT created) and `agent_end` still arrived (agent continued gracefully).

## Files

- Probe extension: `scripts/d1-probe-ext.ts`
- Probe driver: `scripts/d1-probe.mjs`

## Adaptation note — JSONL reader timing

During `session_start`, Pi's JSONL stdin reader is not yet attached (it's set up synchronously after `rebindSession()` returns). Calling `ctx.ui.confirm()` directly inside the `session_start` handler creates a pending promise with no active stdin reader — the Node.js event loop empties and Pi exits with code 0 before processing any response.

**Fix**: The probe extension fires the confirm asynchronously (detached via `void (async () => {...})()`) and returns synchronously from `session_start`. A 50ms delay ensures `rebindSession()` has completed and the JSONL reader is attached before `confirm()` emits `extension_ui_request`. Task 6/10 implementations MUST use the same async-detached pattern (or hook a later lifecycle event) when calling `ctx.ui.*` from RPC mode.

## Date

2026-07-03

## B4 wire shapes — hv.audit / hv.rules / hv.dangerous (2026-07-05)

All three ride `ctx.ui.notify`, which surfaces over RPC as a **fire-and-forget** `extension_ui_request` with `method: "notify"` — no `extension_ui_response` is ever written (responding is a protocol error; main must only parse). The JSON payload rides the `message` field (notify carries JSON in `message`; input/select carry it in `title` — same convention as `hv.auth`).

### hv.audit — one notify per permission decision (bridge → main → EventLog)

**Verbatim `extension_ui_request` observed in rules-bridge.test.ts (real RPC, rule-deny of `touch forbidden.txt`):**

```json
{"type":"extension_ui_request","id":"…","method":"notify","message":"{\"kind\":\"hv.audit\",\"ts\":\"2026-07-05T…Z\",\"tool\":\"bash\",\"summary\":\"touch forbidden.txt\",\"decision\":\"deny\",\"source\":\"rule\",\"rule\":{\"layer\":\"command\",\"pattern\":\"touch *\",\"action\":\"deny\",\"scope\":\"global\"}}","notifyType":"info"}
```

Payload fields:

| field | values |
|-------|--------|
| `kind` | `"hv.audit"` (constant) |
| `ts` | ISO 8601, written by the bridge at decision time |
| `tool` | Pi tool name (`bash`, `write`, …) |
| `summary` | bash command or JSON'd input, ≤300 chars |
| `decision` | `"allow"` \| `"allow-session"` \| `"deny"` |
| `source` | `"rule"` \| `"user"` \| `"dangerous"` \| `"safe-default"` |
| `rule?` | `{layer, pattern, action, scope: "global"\|"workspace"}` — only when `source: "rule"` |
| `grant?` | `"session"` — user allow riding an earlier "Allow for session" grant |

Main (`src/main/ipc.ts`) intercepts these before renderer forwarding and appends them to the EventLog as `type: "permission.decision"` with `sessionId`/`workspaceId` — the payload above becomes the event's `data`.

### hv.rules — /hv-rules-reload acknowledgment

Rules file (`permission-rules.json` in userData, `{global: Rule[], workspaces: Record<absWorkspacePath, Rule[]>}`) reaches the bridge via the `HV_RULES_FILE` spawn env var. Main rewrites the file on UI edits then broadcasts the `/hv-rules-reload` slash command (fire-and-forget RPC prompt) to every live session.

```json
{"kind":"hv.rules","stage":"loaded","global":1,"workspaces":1}
{"kind":"hv.rules","stage":"error","message":"<parse error>"}
```

On parse error the bridge KEEPS the previous ruleset — a torn write never fails open.

### hv.dangerous — /hv-dangerous on|off acknowledgment

```json
{"kind":"hv.dangerous","on":true}
{"kind":"hv.dangerous","stage":"error","message":"Usage: /hv-dangerous on|off"}
```

Per-session, in-bridge-memory only — never persisted, always off after respawn. The renderer drives its warning banner from this notify (forwarded through `hv:ui-request` like all non-audit requests).

Contract coverage: `tests/rules-bridge.test.ts` (reload + dangerous need no model; the rule-deny hv.audit contract is DEEPSEEK-gated like bridge.test.ts).

## B5 wire shapes — hv.context / hv-context-marks (2026-07-07)

Context visibility rides the same fire-and-forget `ctx.ui.notify` channel as B4
(JSON in the `message` field; the renderer parses it and NEVER opens the modal).
Three `/hv-context*` bridge commands are driven from main via RPC `prompt`
(B3/B4 precedent); their replies stream back as `hv.context` notifies.

### Mark identity (the crux)

The panel identifies items by session-entry id (from `get_entries`), but the
`context` event — the only place removal takes effect — hands the bridge an
`AgentMessage[]` with NO entry ids. The only ids present in BOTH are:

- `toolCall.id` / `toolResult.toolCallId` → mark key `tool:<id>` (a pair)
- `message.timestamp` → mark key `msg:<ts>` (plain user/assistant messages)

So the renderer sends **mark keys**, not entry ids. Pairing is automatic:
`tool:<id>` drops the assistant's toolCall block AND the matching toolResult
(atomic), so selecting either half removes both.

### hv.context — snapshot (reply to /hv-context)

```json
{"kind":"hv.context","stage":"snapshot",
 "system":{"chars":8123,"estTokens":2031,"toolCount":4,
   "contextFiles":[{"path":"/ws/AGENTS.md","chars":512,"estTokens":128}],
   "toolDefs":[{"name":"read","chars":420},{"name":"bash","chars":880}]},
 "items":[
   {"markKey":"msg:1730000000000","entryId":"e1","group":"conversation","role":"user","preview":"turn one","chars":8,"estTokens":2,"removable":true},
   {"markKey":"tool:call-A","entryId":"e2","group":"tool","role":"assistant","toolName":"bash","toolCallId":"call-A","preview":"bash({\"command\":\"ls\"})","chars":21,"estTokens":6,"usage":{"input":100,"output":20,"total":120},"removable":true},
   {"markKey":"tool:call-A","entryId":"e3","group":"tool","role":"toolResult","toolName":"bash","toolCallId":"call-A","preview":"ok","chars":2,"estTokens":1,"removable":true}
 ],
 "marks":["tool:call-A"]}
```

- `system` is `null` until the first turn runs (captured at `before_agent_start`).
- Item sizes (`chars`/`estTokens`) are char-based ESTIMATES (≈ chars/4), labeled
  as such in the UI; `usage.total` on assistant items is MEASURED.
- `removable` reflects the completed-turn gate (s0.3): items at/after the last
  user message (the in-flight turn) are `false`. The bridge refuses to mark them.
- **v5:** `system.toolDefs[]` gives per-tool ESTIMATED sizes (from the LLM tool
  spec, ≈ chars/4) so the panel can drill into tool definitions with a total
  instead of "size not measured". `bashExecution` message entries now group as
  `tool` (not `other`), and the renderer hides any empty group — so "Other" only
  appears when a genuinely unknown entry type shows up.

### hv.context — removed / restored (reply to /hv-context-remove | -restore)

```json
{"kind":"hv.context","stage":"removed","accepted":["tool:call-A"],"refused":["tool:call-B"],"marks":["tool:call-A"]}
{"kind":"hv.context","stage":"restored","restored":["tool:call-A"],"marks":[]}
```

`refused` = keys rejected by the completed-turn gate (in-flight or unknown);
`notifyType` is `"warning"` when anything was refused. `marks` is the full
current kill-set after the operation.

### hv-context-marks — persistence (pi.appendEntry)

The kill-set is persisted as `custom` entries of type `hv-context-marks` with
`data: {marks: MarkKey[]}` — a full snapshot, newest wins. These do NOT enter
LLM context. Restored on `session_start` (survives reload + resume), and
re-appended in `session_before_compact` so they survive compaction. The bridge's
`context` handler applies the set via `filterMessages` (non-destructive — the
session file is never mutated, proven in s0.3).

### Compaction

Main fires the RPC `compact` command (`hv:compact-session`); Pi streams
`compaction_start` / `compaction_end` events (forwarded as `hv:pi-event`),
rendered as transcript notices. The red-zone auto-suggest banner and the
[Compact now] dialog are renderer-only; compaction is NEVER automatic.

Contract coverage: `tests/context-bridge.test.ts` (snapshot shape + in-flight
refusal need no model; the remove→resume-survival guarantee is DEEPSEEK-gated —
it proves the mark persists across a full Pi process restart from the session
file, i.e. the same `pi.appendEntry` + `restoreMarks` machinery
`session_before_compact` re-invokes; a manual `compact` refuses on a session
that small, so persistence is proven deterministically via resume). Pure logic
(marks/pairing/completed-turn gate + gauge/grouping/parsing) is unit-tested in
`tests/hv-context.test.ts` and `tests/context-renderer.test.ts`.

## B6 wire shapes — hv.agents / hv.tools + subagent trace (2026-07-07)

`pi-subagents@0.33.1` is loaded as a SECOND `-e` extension alongside the bridge
(`spawn.ts` PI_SUBAGENTS_RELPATH); the child Pi it spawns for delegation is
pointed at the embedded bin via `PI_SUBAGENT_PI_BINARY` (spawn env, always set —
`node_modules/.bin/pi`). Agents are discovered from `<PI_CODING_AGENT_DIR>/agents/*.md`
(→ source `builtin`, the app-owned dir) and `<cwd>/.pi/agents/*.md` (→ `project`).

`hv.agents` / `hv.tools` ride the fire-and-forget `ctx.ui.notify` channel (JSON
in `message`, like B4/B5); the renderer parses them and NEVER opens the modal.
Both are driven from main via RPC `prompt` (`/hv-agents`, `/hv-tools`).

### hv.agents — agent inventory (reply to /hv-agents)

```json
{"kind":"hv.agents","agents":[
  {"name":"code-explorer","description":"Read-only codebase investigator…","tools":["read","grep","glob","list","ls"],"source":"builtin","path":"/…/pi-agent/agents/code-explorer.md"},
  {"name":"agents-md-maker","description":"Drafts AGENTS.md files…","tools":["read","grep","glob","list","ls"],"source":"builtin","path":"/…/pi-agent/agents/agents-md-maker.md"}
]}
```

**v5 — two builtins.** The Summarizer agent was removed (compaction is Pi-native);
`installBuiltinAgents` uninstalls an unedited previously-installed copy. The
**agents-md-maker** is now read-only + structured: its final message is one fenced
block tagged ` ```json agents-md ` with `{"files": {"AGENTS.md": "...", "pkg/AGENTS.md": "..."}}`
(root + optional nested). The renderer parses it (`parseAgentsMdOutput`) and calls
`hv:write-agents-md-files`; **main** writes each file path-confined (basename must be
`AGENTS.md`, no escape) and appends an `agents_md.written` EventLog entry.
Unparseable output falls back to a single root draft.

The bridge enumerates the two agent dirs itself (readdir + flat-frontmatter
parse in `hv-agents.ts`) rather than the pi-subagents `subagent {action:"list"}`
result, so the shape is exactly `{name, description, tools?, model?, source, path}`.
`model?` is the agent-tier override (frontmatter `model:`) when present.

### hv.tools — tool inventory (reply to /hv-tools)

```json
{"kind":"hv.tools","tools":[
  {"name":"bash","description":"Run a shell command","source":"builtin"},
  {"name":"subagent","description":"Delegate to a subagent…","source":"pi-subagents"}
]}
```

Built from `pi.getAllTools()` (extension side — RPC `get_commands` does NOT list
built-in LLM tools). Permission state is joined RENDERER-side via `hv:eval-rules`
(one call per tool, empty input → tool-layer verdict) — the SAME `evaluate()` the
bridge's `tool_call` gate runs, so the rules logic is never forked.

### Subagent delegation trace (ordinary tool_execution_* events, toolName "subagent")

The subagent tool is a normal `tool_call` → the B4 bridge permission gate applies
to it (gate `subagent` via a tool-layer rule). Observed shapes on
pi-subagents 0.33.1 (differs from s0.3's prediction — the transcript is in the
UPDATE, the outcome in the END):

- `tool_execution_start` — `toolName: "subagent"`, `args: {agent, task}`.
- `tool_execution_update.partialResult.details.results[]` — **LIVE child transcript**:
  each result `{agent, task, exitCode, messages: AgentMessage[]}`. `messages` is
  the child's full turn-by-turn transcript (user/assistant/thinking/toolCall blocks).
- `tool_execution_end.result.details.results[]` — **FINAL outcome, NO transcript**:
  each result `{agent, exitCode, finalOutput, usage?, modelAttempts:[{model, usage:{input,output,cost,turns}, exitCode}], artifactPaths:{inputPath,outputPath,jsonlPath,transcriptPath,metadataPath}, progressSummary}`.
  Artifacts land under `<cwd>/.pi-subagents/artifacts/`.

The renderer keeps the update's `messages` and merges the end's model/usage/finalOutput
onto them (`mergeTrace`), rendering a nested collapsible mini-conversation
distinct from a normal tool card.

## queue_update — margin note (2026-07-12)

`queue_update` (`{type:"queue_update", steering: string[], followUp: string[]}`, mirrored by `src/renderer/src/queue.ts`) is read-only state: Pi 0.80.3 exposes no dequeue RPC, so queued messages can be displayed but never removed — upstream feature request is the path.

## B7 — hv:get-analytics (internal IPC shape, NOT a Pi wire contract) (2026-07-10)

Local-only analytics. Main reads the whole EventLog (`events.jsonl`) and returns
a pure aggregate computed by `src/main/analytics.ts` (`aggregate()`); NOTHING is
ever sent off the machine (PRD: local-only telemetry). Additive `hv.*` method,
no Pi involvement — this is a renderer↔main shape only.

`hv:get-analytics({workspaceId?, sinceTs?})` → `Analytics`:

```json
{"totalSessions":12,"openSessions":1,"crashes":0,
 "tokens":{"input":40213,"output":18022},"cost":0.83,
 "duration":{"avgMs":184000,"medianMs":120000,"count":11},
 "sessionsPerDay":[{"date":"2026-07-09","count":5},{"date":"2026-07-10","count":7}],
 "perWorkspace":[{"key":"/ws/app","sessions":9,"tokens":51000,"cost":0.7}],
 "perModel":[{"key":"deepseek-chat","sessions":9,"tokens":51000,"cost":0.7}],
 "permissions":{"total":30,"byDecision":{"allow":22,"deny":8},"bySource":{"rule":18,"user":12}}}
```

- Sources: `session.start`/`session.end`/`session.crash`/`permission.decision`.
- Tokens/cost/per-workspace/per-model come only from `session.end`'s `data.stats`
  (get_session_stats payload); a start with no matching end is `openSessions` and
  adds no duration. `perModel` is empty unless the stats payload carries `model`.
- Cost is an ESTIMATE (labeled as such in the UI). `duration` derives from
  start→end ts pairs by sessionId (skew/negatives dropped).
- Onboarding "seen the wow-flow" flag: `hv:get-onboarding-seen` /
  `hv:set-onboarding-seen(bool)`, persisted in `config.json` (userData) as
  `onboardingSeen`. Shown once on a brand-new user's first session, re-openable
  from the sidebar Help affordance.

Contract coverage: `tests/analytics.test.ts` (pure aggregation: duration pairing,
token/cost sums, workspace + model grouping, open-session handling, empty log,
filters, corrupt-line resilience over a real EventLog) and
`tests/analytics-format.test.ts` (duration/number/cost formatters).

## W1.4 wire shape — hv.sysprompt (2026-07-12)

Read-only resolved system prompt for Settings → System Prompt. Rides the same
fire-and-forget `ctx.ui.notify` channel as B4/B5 (JSON in `message`; the
renderer parses it and NEVER opens the modal). Driven from main via RPC
`prompt` (`/hv-sysprompt`) on the focused session's Pi, falling back to the
utility client.

### hv.sysprompt — reply to /hv-sysprompt

```json
{"kind":"hv.sysprompt","text":"You are a coding agent…<full resolved prompt>"}
{"kind":"hv.sysprompt","text":null}
```

`text` is the full `event.systemPrompt` string captured by the bridge at
`before_agent_start` (the same capture point as B5's `systemBlock`) — i.e. the
prompt Pi actually resolved for the LAST turn, including APPEND_SYSTEM.md
layers. `null` until a turn has run in that Pi process (the utility client
never runs one, so Settings shows a "start a session" note).

### APPEND_SYSTEM.md additions layer (no Pi wire — main-side confined writers)

Global `<agentDir>/APPEND_SYSTEM.md` and per-workspace
`<workspace>/.pi/APPEND_SYSTEM.md` are written by `src/main/appendSystem.ts`
(path-confined like agentsMd.ts; blank content deletes the file). Pi reads
them at session load only → edits apply to new/restarted sessions. The
workspace file (trusted project) REPLACES the global one — NOT concatenated —
and the UI copy states that. Per-workspace model override lives in
`workspaces.json` (`WorkspaceEntry.model`; legacy string[] migrates on read);
spawn resolution in ipc.ts is workspace-model → global default (session tier
reserved for Wave 2).

Contract coverage: `tests/append-system.test.ts` (confinement + blank-deletes)
and `tests/session-index.test.ts` (registry migration + model override).

## MCP permission tool-name convention (m1.md, 2026-07-13)

The `tool` field in an `hv.permission` select prompt may be a virtual name for MCP
proxy calls:

| Value | Meaning |
|-------|---------|
| `mcp:<serverName>_<toolName>` | MCP invoke — per-tool identity; rules can pattern-match (e.g. `mcp:github_*`) |
| `mcp` | MCP discovery (search/describe/connect) — safe-default-allowed, no prompt |

The actual format is `mcp:<prefix>_<toolName>` where `<prefix>` is the server name
from `.mcp.json` `mcpServers` key (the adapter's tool-prefix behavior).
Empirically confirmed: server `echo`, tool `echo` → `mcp:echo_echo`.

Discovery calls (`search`, `describe`, `connect`) set `ruleTool: "mcp"` (bare) and
are safe-default-allowed at the bridge gate — they never surface as `hv.permission`
prompts unless an explicit rule matches `mcp`.

## B6 wire coverage note

Contract coverage: `tests/agents-bridge.test.ts` (pi-subagents loads with
PI_SUBAGENT_PI_BINARY set + both `-e` extensions; `/hv-agents` lists the 2
builtins; `/hv-tools` shape — all need no model; a real DEEPSEEK-gated delegation
proves the update-transcript + end-outcome shapes above). Pure logic
(frontmatter parse/serialize/edit/duplicate + tool→permission join + trace
extraction/merge) is unit-tested in `tests/hv-agents.test.ts` and
`tests/agents-renderer.test.ts`.

## W2.3 wire shapes — hv.context-files / nested AGENTS.md injection (2026-07-12)

Pi discovers AGENTS.md/CLAUDE.md **upward from cwd at session start only**
(dist/core/resource-loader.js loadProjectContextFiles — no nested/downward
discovery, no per-tool context loading). Nested AGENTS.md support per the
agents.md standard is built ENTIRELY in the bridge (`hv-agents-md.ts` pure
module + `happyvibe-bridge.ts` wiring):

- On `tool_call` for file tools (`read`/`edit`/`write`; input `path` |
  `file_path`), the bridge resolves the NEAREST `AGENTS.md` walking UP from
  the target file's directory, stopping at the session cwd EXCLUSIVE — the
  root file is Pi's own context file, structurally never duplicated. Paths
  outside cwd resolve to nothing. Discoveries accumulate in a per-session
  in-bridge set (a respawn rediscovers on the next file-tool call).
- On `before_agent_start`, if the set is non-empty the bridge returns
  `{systemPrompt: base + "\n\n## Nested AGENTS.md (scoped instructions)\n…"}`
  — a SINGLE-TURN replacement (agent-session.js resets to the base prompt
  whenever the handler returns nothing). File content is re-read at injection
  time and capped at 8192 chars/file with a `[truncated at 8192 chars]` note.
  `/hv-sysprompt` returns the INJECTED prompt (honest resolved-prompt display).

### hv.context-files — fire-and-forget notify on every set change

```json
{"kind":"hv.context-files","nested":[{"dir":"sub","path":"/ws/sub/AGENTS.md","chars":42}]}
```

`dir` is cwd-relative. Renderer patches the live snapshot's system block
(`parseContextFiles`); the context panel shows each entry as an AGENTS.md row
with a "nested" tag and its subtree dir.

### hv.context snapshot extension

The snapshot's `system` block gains `nested` (same array shape, computed fresh
at snapshot time — the set can grow mid-turn):

```json
{"kind":"hv.context","stage":"snapshot","system":{"chars":9000,"estTokens":2250,"toolCount":4,
 "contextFiles":[{"path":"/ws/AGENTS.md","chars":512,"estTokens":128}],
 "nested":[{"dir":"sub","path":"/ws/sub/AGENTS.md","chars":42}]},"items":[],"marks":[]}
```

### Missing-file flow (renderer ↔ main only, no new Pi wire)

- `hv:has-claude-md(workspaceId)` / `hv:copy-claude-md(workspaceId)` — confined
  to `<workspace>/CLAUDE.md` → `<workspace>/AGENTS.md` (agentsMd.ts trust
  boundary; copy is the explicit user action and returns the copied content).
- "Draft with agents-md-maker" reuses EXISTING wire: `hv:prompt-session` asks
  the model to delegate to the bundled `agents-md-maker` agent (third built-in,
  installed by the same versioned idempotent installBuiltinAgents sweep); the
  renderer captures the delegation's `tool_execution_end` finalOutput
  (agents.ts traceFromEnd) into the editor. The draft is NEVER auto-written —
  Save is explicit.

Contract coverage: `tests/agents-md-bridge.test.ts` (bridge loads + empty
state need no model; the injection round-trip — discovery notify on turn 1's
read, turn 2's system prompt contains the nested section, root NOT duplicated,
snapshot `nested` list — is DEEPSEEK-gated because tool_call/before_agent_start
only fire on real turns; PROVEN live 2026-07-12). Pure logic (nearest-file
resolution, cwd-exclusive stop, outside-cwd refusal, cap+truncation, dedupe,
list shape) in `tests/hv-agents-md.test.ts`; CLAUDE.md-copy confinement in
`tests/agents-md.test.ts`.

## WS8 — file-tree mutations + native watching (renderer ↔ main only, no Pi wire)

All confined by `files.ts` `resolveInWorkspace` (the workspaceId trust boundary);
each refuses to clobber an existing path.

- `hv:fs-create-file` / `hv:fs-create-dir(workspaceId, relPath)` — create (mkdir parents).
- `hv:fs-move(workspaceId, srcRel, destDirRel)` — move within the workspace (rename); refuses overwrite / moving into itself. Returns the new rel path.
- `hv:fs-import(workspaceId, destDirRel, srcAbsPaths[])` — copy EXTERNAL OS paths (from an OS drag-drop) into a confined dest (`fs.cpSync` recursive). Sources are absolute + unconfined (they're external); only dest is confined. Returns written rel paths.
- `hv:watch-workspace` / `hv:unwatch-workspace(workspaceId)` — start/stop a recursive `fs.watch` (macOS/Windows; Linux recursive is unsupported → no watcher, renderer keeps working). Debounced changes push `hv:fs-changed {workspaceId, relDirs}`; the tree re-lists expanded dirs (replaces the manual refresh button).
- Drag-out: tree rows set the `application/x-hv-relpath` dataTransfer mime; the center panes drop-open it. Preload exposes `getPathForFile(file)` (`webUtils.getPathForFile`) for OS-drop imports.

Unit coverage: create/move/import confinement + no-clobber in `tests/files.test.ts`.

## §V2 addendum — hv.ask-user (AskUserQuestion tool, V2.B)

The registered `ask_user` tool (happyvibe-bridge.ts + pure hv-ask-user.ts)
surfaces a model-initiated decision as a BLOCKING `ctx.ui.input` — JSON
payload in `title` (hv.auth prompt precedent). NO timeout, NO auto-answer
(permission invariant). Routing is KIND-based: `method:"input"` +
`title.kind:"hv.ask-user"` (hv.auth inputs keep routing to the auth flow).
Input is clamped, never rejected: headers >12 chars truncated, >4
questions/options dropped, previews stripped off multi-select questions —
each adjustment rides back on the tool result as a note.

**Verbatim request (observed live in ask-user-bridge.test.ts, 2026-07-12):**

```json
{"type":"extension_ui_request","id":"…","method":"input","sessionId":"…",
 "title":"{\"kind\":\"hv.ask-user\",\"intent\":\"Choosing a greeting style\",\"questions\":[{\"question\":\"Which greeting should I use?\",\"header\":\"Greeting\",\"multiSelect\":false,\"options\":[{\"label\":\"Hello (Recommended)\",\"description\":\"Plain and friendly\"},{\"label\":\"Hi\",\"description\":\"Casual\"}]}]}",
 "placeholder":""}
```

**Working response — answered** (`value` is `JSON.stringify(AskAnswer[])`;
`answers` carries option labels and/or the renderer's free-text "Other"):

```json
{"type":"extension_ui_response","id":"…",
 "value":"[{\"question\":\"Which greeting should I use?\",\"header\":\"Greeting\",\"answers\":[\"Hello (Recommended)\"],\"note\":\"keep it simple\"}]"}
```

**Working response — dismissed** (renderer `respondInput(id, null)` → main
maps to `{cancelled:true}`; the tool result tells the model "User dismissed
the question — proceed with your best judgment."):

```json
{"type":"extension_ui_response","id":"…","cancelled":true}
```

Tool result to the model is readable markdown ("The user answered:\n-
**Greeting** — Which greeting should I use?: Hello (Recommended) _(note: keep
it simple)_"). Renderer always appends "Other"; the answer also echoes into
the transcript as a user-style item (`answersSummary`) and queues through the
same uiQueue as permission prompts (sidebar badges, headFor routing, dock
badge unchanged). `ask_user` is in SAFE_TOOLS — no permission prompt stacks
on top of the question modal.

Contract coverage: `tests/ask-user-bridge.test.ts` (DEEPSEEK-gated; request
shape + wire round-trip + tool result reaches the model — PROVEN live
2026-07-12). Pure logic (clamping, answers round-trip, markdown) in
`tests/hv-ask-user.test.ts`; renderer routing incl. hv.auth non-regression in
`tests/ask-user-renderer.test.ts`.

## Async subagents — hv.subagent + /hv-subagent-* + discoverability (2026-07-17)

PRD §12: delegations run async by default (`asyncByDefault` in the app-written
`<agentDir>/extensions/subagent/config.json`, `completionBatch.enabled:false`),
so a delegation returns immediately (detached runner) and the user keeps
chatting. pi-subagents emits lifecycle events on the **in-process `pi.events`
bus** (never on RPC stdout); the co-resident bridge subscribes and relays each
as a fire-and-forget `hv.subagent` notify (JSON in `message`, like B4/B5).
Completion itself is auto-delivered by pi-subagents as an injected
`subagent-notify` message + triggered assistant turn (final-output-only, the
same isolation contract as foreground) — the RPC stream serializes that turn;
the `hv.subagent complete` notify is the card-lifecycle signal alongside it.

### hv.subagent — lifecycle relays (bus → bridge → main/renderer)

```json
{"kind":"hv.subagent","stage":"started","runId":"<id>","agent":"code-explorer","task":"map the repo","asyncDir":"/…/pi-subagents-uid-501/async-subagent-runs/<id>"}
{"kind":"hv.subagent","stage":"control","runId":"<id>","activityState":"long-running"}
{"kind":"hv.subagent","stage":"control","runId":"<id>","activityState":"needs_attention"}
{"kind":"hv.subagent","stage":"complete","runId":"<id>","status":"success","summary":"<final output, ≤500 chars>"}
{"kind":"hv.subagent","stage":"complete","runId":"<id>","status":"error"}
{"kind":"hv.subagent","stage":"complete","runId":"<id>","status":"interrupted"}
```

- `started` maps from `subagent:async-started` (`{id, agent, task, asyncDir, …}`).
- `control` maps from `subagent:control-event` (`{event:{type}, asyncDir}`); `runId = basename(asyncDir)` (the run dir is named by id). `activityState`: pi-subagents' `active_long_running` → `"long-running"`, `needs_attention` → `"needs_attention"`.
- `complete` maps from `subagent:async-complete` (`{runId, agent, success, summary, state}`); `status = success ? "success" : state==="paused" ? "interrupted" : "error"`.

### /hv-subagent-interrupt <runId> — stop a running async run (reply notify)

Drives pi-subagents' versioned event-bus RPC (`subagents:rpc:v1:request`, method
`interrupt`) and acknowledges:

```json
{"kind":"hv.subagent","stage":"interrupt-sent","runId":"<id>"}
{"kind":"hv.subagent","stage":"interrupt-error","runId":"<id>"}
```

Interrupt is file-based and resumable (drops `control/interrupt.json`). Driven
from main via RPC `prompt` like the other `/hv-*` commands.

### /hv-subagent-list — active-run resync (reply to a respawn)

`restoreActiveJobs` repopulates pi-subagents' internal job map on session_start
but does NOT re-emit `started`, so main fires `/hv-subagent-list` after every
(re)spawn to resync run cards + the activity counter. The bridge reads
`listAsyncRuns(ASYNC_DIR, {states:["queued","running"], sessionId})`:

```json
{"kind":"hv.subagent","stage":"active","runs":[{"runId":"<id>","agent":"code-explorer","asyncDir":"/…/<id>"}]}
```

**Why this reads pi-subagents' internals (re-audited against 0.40.0, 2026-08-02).**
`listAsyncRuns` + `ASYNC_DIR` are imported by RELATIVE path
(`../node_modules/pi-subagents/src/…`), because 0.35.0 added an `exports` map that
lists neither file and an exports map only gates *bare* specifiers. Three public
alternatives were measured against the real 0.40.0 tarball and all three fail:

| candidate | why it can't serve this command |
| --- | --- |
| `snapshotBackgroundWork()` (`./background-work`) | inverse API — it lets *other* extensions declare work *to* pi-subagents for idle/hibernation gating. pi-subagents never calls `registerBackgroundWorkProvider` on itself, so the snapshot is empty; a `BackgroundWorkItem` is only `{id, sessionId}`. |
| `status` RPC → `fleet` (new in 0.40.0, `{version:1,entries,totalActive,omitted}`) | genuinely structured, but `SubagentRpcFleetEntry.key` is documented "Opaque key for client-side reconciliation; **never a run or async identifier**" (`src/extension/rpc.ts:76`). No `runId` (so no interrupt), no `asyncDir` (so no `status.json` tail). It is a display fleet view. |
| `status` RPC prose | `src/runs/background/fleet-view.ts` renders display lines; scraping them is strictly more fragile than the import. |

`fleetStatus` is **not** an RPC method — `SUBAGENT_RPC_METHODS` is
`ping, status, spawn, steer, interrupt, stop, resume` (`src/extension/rpc.ts:29`);
`capabilities.fleetStatus:{version:1}` is a handshake flag for the TUI widget
(`SubagentFleetStatus`, gated on `ctx.hasUI`). `steer` and `resume` are new in
0.40.0 and currently unused by HappyVibe.

Gate: `tests/pi-subagents-contract.test.ts` (key-free) pins the relative form,
the exports map, the tmpdir-confined `ASYNC_DIR`, and the `{id, asyncDir,
steps[].agent}` triple against a fixture run dir.

### Subagent trace projections — `messages` is gone (0.40.0)

The child transcript used to ride `tool_execution_update.partialResult.details
.results[].messages`. As of 0.40.0 it rides **nothing**:

| projection | source | `messages` | `toolCalls` |
| --- | --- | --- | --- |
| streamed update | `snapshotStreamResult` (`execution.ts:266`) | `undefined` **(new in 0.40)** | present, bounded to 64 |
| terminal end | `compactForegroundResult` (`utils.ts:405`) | `undefined` (unchanged since 0.34) | present |

Upstream's reason is in the code: one `tool_execution_update` is serialized as a
single child-stdout line, read under `MAX_CHILD_PENDING_LINE_BYTES` (16 MiB), and
an unbounded running transcript could cross it and kill the child with
`protocol_output_limit`. So this is the same change as the deep-fan-out robustness
fix — the regression and the win are one commit.

`ToolCallSummary` is `{text, expandedText}` (display strings only). The renderer
maps it into the existing `SubagentMessage` rows (`role:"tool"`, `expandedText ??
text`) and still prefers real `messages` when a payload carries them, so a future
pin that restores the transcript needs no renderer change. **The child's prose is
genuinely lost from this path**; its ANSWER survives as `finalOutput`, which
`SubagentTraceView` now renders. Live activity for the DEFAULT (async) path is
unaffected — that comes from the `status.json` poller, not this projection.

Pinned by `tests/agents-bridge.test.ts` (wire: asserts `messages` is absent) plus
`tests/agents-renderer.test.ts` (the toolCalls→rows mapping).

### The parent-blocking wait tool is name-matched (`wait` → `subagent_wait`)

PRD §12's "never block on the result" is enforced by a tool-name match, so a
rename disarms it silently. 0.35.0 renamed `wait` → `subagent_wait` with **no
alias** (`src/runs/background/wait-tool.ts:8`). Both names now live in
`WAIT_TOOLS`/`isWaitTool` (`hv-rules.ts`) — the shared pure module the bridge
(blocks the call) and the renderer (hides the card, `App.tsx`) both import, so the
two can't drift. `tests/pi-subagents-contract.test.ts` asserts the name upstream
actually registers is in the set, and that neither call site uses a literal.
The upstream alternative is `"waitTool": false` in the subagent config (or
`PI_SUBAGENT_WAIT_TOOL_ENABLED=false`), which per its README **keeps the tool
registered but makes direct calls return immediately** instead of blocking. We do
not set it: the bridge's guard also *steers* ("end your turn now, the result will
arrive"), which a silent no-op does not, and the guard has to exist anyway for the
renderer to hide the card. Worth adding as defence-in-depth if the name ever
drifts again.

**Not a concern (checked 2026-08-02):** pi-subagents >=0.40 also ends every turn
with `if (ctx.hasUI) return; await drainOutstandingWork(...)`, which WOULD block a
turn on its own async delegation. It never runs for us — `ctx.hasUI` is **true**
under `--mode rpc` (rpc-mode.js binds a real `uiContext`; Pi's headless mode is
*print* mode). Measured with a probe extension, and the three links are pinned in
`tests/pi-subagents-contract.test.ts`. Do not read a `ctx.hasUI` gate in vendored
code as "excludes HappyVibe".

### hv:subagent-status — live status poll (internal IPC, NOT a Pi wire contract)

Main tails `<asyncDir>/status.json` (~500 ms; atomic writes → a torn read skips a
tick; asyncDir sanity-checked to start with `os.tmpdir()`) for each active run
and forwards changes to the renderer:

```json
{"sessionId":"…","runId":"<id>","status":{"currentTool":"read","activityState":"long-running","recentTools":[{"tool":"grep","args":"…"}],"turnCount":3,"state":"running"}}
```

### Discoverability — hv.context snapshot `system.agents`

The bridge injects an `## Available subagents` roster (name + description of every
enumerated agent, builtin + project, plus one guidance line) into the system
prompt each turn — the same per-turn replacement mechanism as nested AGENTS.md —
so the model delegates spontaneously instead of having to call
`subagent {action:"list"}`. It rides the `/hv-context` snapshot's `system` block:

```json
{"kind":"hv.context","stage":"snapshot","system":{"…":"…","agents":[{"name":"code-explorer","chars":72},{"name":"agents-md-maker","chars":95}]}}
```

Contract coverage: `tests/subagent-async-bridge.test.ts` (DEEPSEEK-gated: async
delegation returns immediately with `details.asyncId` + `started` notify, then a
`complete` notify + a triggered turn — the session-id-continuity delivery
contract; plus the `async:false` foreground regression). Discoverability:
`tests/subagent-discovery-bridge.test.ts` (`/hv-agents` enumerate + DEEPSEEK-gated
injection into `/hv-sysprompt` and `system.agents`). Pure logic:
`tests/hv-agents.test.ts` (`renderSubagentSection`), `tests/session-activity.test.ts`
(async-run activity gating), `tests/subagent-status.test.ts` (poller), and
renderer parsers in `tests/agents-renderer.test.ts`.

## Plan Mode — hv.plan / hv.plan-write / hv.plan-status / hv.plan.blocked + hv:plan-* (2026-07-19)

PRD §23. Plan Mode is per-session read-only mode; the plan is a workspace file
(`.agents/plans/NNN-<slug>.md`) with front-matter status + a Tasks checklist. The
bridge module `hv-plan.ts` holds `{enabled, planPath?}` (persisted via
`pi.appendEntry("hv-plan-state")`, restored on `session_start` like context marks
— but note: unlike dangerous mode this state SURVIVES respawn), and re-emits the
`hv.plan` notify on `session_start` so the renderer resyncs after a respawn.

### The plan gate (tool_call clamp, BEFORE dangerous + rules)

Inserted in the bridge `tool_call` handler after MCP unwrap (so it sees real tool
names) but **before** the dangerous/bypass check and the rule engine — Plan Mode
wins over bypass. `gatePlanCall(toolName, input)` (pure, in `hv-plan.ts`):

| Result | Tools | Bridge action |
|--------|-------|---------------|
| `block` | `edit`, `write`, `subagent` | `{block:true, reason}` + `hv.plan.blocked` notify + `hv.audit` (source `"plan"`) |
| `block` | `bash` failing the read-only allowlist | same |
| `pass` | `read`/`grep`/`glob`/`list`/`ls`/`ask_user`/`plan_complete`, allowlisted `bash` | fall through to normal gates |
| `floor-ask` | everything else (MCP, unknown) | run `evaluate()`, then clamp `allow → ask` (deny stays deny) |

The bash allowlist is ported from `@narumitw/pi-plan-mode` `tool-policy.ts` (MIT):
fail-closed — read-only commands + safe `git`/`gh`/`npm` subcommands; rejects
redirects, substitutions, subshells, background jobs, and any chain with an unsafe
segment. In-flight async subagent runs are untouched (the gate sees only new calls).

### hv.plan — mode notify (reply to /hv-plan on|off|implement + on session_start)

Fire-and-forget `ctx.ui.notify` (JSON in `message`, like B4/B5); renderer parses,
never opens the modal. Drives the banner + composer toggle + sidebar badge.

```json
{"kind":"hv.plan","enabled":true,"planPath":null}
{"kind":"hv.plan","enabled":true,"planPath":".agents/plans/001-add-auth.md"}
{"kind":"hv.plan","enabled":false,"planPath":".agents/plans/001-add-auth.md"}
```

`off` keeps `planPath` (the file stays on disk). `/hv-plan` is driven from main
via RPC `prompt` (B3/B4 precedent); enter is also reachable via the `plan_start`
registered tool.

### hv.plan-write — BLOCKING plan-file write (plan_complete → main → tool result)

The `plan_complete({intent, plan})` registered tool emits a **blocking**
`ctx.ui.input` (JSON in `title`, like `hv.ask-user`); main writes the file and
answers with the workspace-relative path, which becomes the tool result AND the
new `planPath`. Main MUST always respond (error string on failure) — never leave
the bridge blocked.

```json
{"type":"extension_ui_request","id":"…","method":"input",
 "title":"{\"kind\":\"hv.plan-write\",\"plan\":\"# Add auth\\n\\n## Tasks\\n- [ ] …\\n\\n## Verification\\n…\"}",
 "placeholder":""}
```

```json
{"type":"extension_ui_response","id":"…","value":".agents/plans/001-add-auth.md"}
```

A second `plan_complete` in the same planning phase (planPath already set)
**overwrites the same file** (revision, keeping front-matter `createdAt`); a fresh
mode entry starts a new NNN. The written file carries front-matter `status: draft`
+ timestamps; `parsePlanFile(md) → {status, createdAt?, done, total}` (shared by
bridge, main, tests) counts `- [ ]`/`- [x]`.

### hv.plan-status — terminal status (plan_status_update → main rewrites front-matter)

Fire-and-forget notify; `plan_status_update({intent, status, note?})` records a
terminal fact (the model may only set `implemented` | `cancelled`).

```json
{"kind":"hv.plan-status","status":"implemented","note":"all checks pass"}
{"kind":"hv.plan-status","status":"cancelled"}
```

Main rewrites the plan file's front-matter and audits `plan.status {who:"model"}`.
Human overrides (incl. `reopen` → `implementing`, which is human-only) go through
`hv:plan-status` IPC and audit `{who:"human"}`.

### hv.plan.blocked — skipped-tool notify (renderer renders the "skipped" card)

```json
{"kind":"hv.plan.blocked","toolName":"write","toolCallId":"call-A","reason":"Plan mode is read-only — file edits are blocked."}
```

### Renderer ↔ main IPC (no Pi wire)

- `hv:plan-set(sessionId, enabled)` — disabling while the session is busy aborts
  the turn first (`{type:"abort"}`), then fires `/hv-plan off`.
- `hv:plan-implement(sessionId, model?)` — sets front-matter `implementing`;
  applies the optional session model override (reasonable-model nudge, §23); fires
  `/hv-plan off` then the file-referencing implement message; audits `plan.implement`.
- `hv:plan-discard(sessionId)` — abort-if-busy + `/hv-plan off`; file stays `draft`.
- `hv:plan-status(sessionId, relPath, status)` — human status override / reopen.
- `hv:plan-changed {workspaceId, path, status, done, total}` — pushed from the
  `watch.ts` fs-watcher when a `.agents/plans/*.md` file changes (`.agents` added to
  `DOTFILE_ALLOW`); drives live PlanCard progress. Carries no `sessionId` — the
  renderer maps via `planPath`.

Audit events (EventLog, frozen envelope): `plan.enter`, `plan.exit`,
`plan.blocked`, `plan.ready`, `plan.implement`, `plan.status` (with `who:"model"|"human"`).

Note — `plan_start` mid-turn: the gate applies to the next `tool_call` immediately,
but the 3-phase plan prompt only takes effect on the next turn (`before_agent_start`).

Contract coverage: `tests/plan-bridge.test.ts` (DEEPSEEK-gated: enter Plan Mode →
a non-allowlisted bash call blocked + `hv.plan.blocked` + an audit stamped
`source:"plan"` → `plan_complete` → blocking `hv.plan-write` answered with a path →
`agent_end`; plan-file semantics). The blocked call is a **`curl`**, not a `touch`:
the plan-mode system prompt has already told the model it is read-only, so asking
for a mutation gets prose and no tool call at all (observed — no
`tool_execution_start`, nothing for the clamp to decide), while `curl` reads as
inspection and the model issues it freely. Same `isSafeCommand` block branch either
way, so the wire is proved on a call the model actually makes. Pure
logic (bash allowlist accept/reject table, gate ordering incl. plan-block wins over
dangerous, front-matter/checkbox parser, state round-trip) in `tests/hv-plan.test.ts`;
plan-file writer (numbering, revision-overwrite, confinement, status rewrite) in
`tests/plans.test.ts`; renderer `parsePlan` in `tests/plan-renderer.test.ts` (if added).

## Session restore reads the file, not `get_messages` (§17, round 10)

`get_messages` returns `agent.state.messages` — cheap in itself, but it is the
FIRST RPC a freshly spawned child answers, so it absorbs the whole boot: 1.35–2.86 s,
90%+ of a restore. `history.ts contextItems` rebuilds the same transcript from the
session file in 1–5 ms (measured on real sessions to 4 MB) and needs no child, so
`hv:open-session` paints before the spawn instead of after it.

Pi's rule (`session-manager.js buildContextEntries`) is
`[latest compaction] + entries from firstKeptEntryId onward`. The compaction entry
itself only yields a `role:"compactionSummary"` message, which `restoreItems` drops,
so the plain slice from `firstKeptEntryId` reproduces it exactly — and is the exact
complement of `earlierItems`.

Two consequences worth knowing:
- §9 removals are applied by the bridge at the `context` event only, so
  `agent.state.messages` still holds removed messages. The file path runs
  `filterMessages`, so a removed message no longer comes back on reopen.
- A session with no captured `piSessionFile` yet keeps the RPC (its client is
  already warm, so there is no boot to absorb).

Contract coverage: `tests/history-contract.test.ts` — key-free, part of the
**pin-bump gate**. It writes a compacted session file, resumes a real Pi on it, and
asserts `contextItems(file)` equals `restoreItems(get_messages)`. A Pi version that
changes the context rule fails here instead of silently truncating conversations.

## Prompt templates — hv.prompt-template (§24, 2026-08-02)

The only Pi wire this feature adds. Full Pi-side evidence is in `pt1.md`; this is
the envelope shape.

### hv.prompt-template — typed↔expanded pairing (fire-and-forget notify)

Pi expands a prompt template into the user message and keeps only the expansion
(`agent-session.js:867-875`), so the typed `/review src/foo.ts` exists on no event
and in no file. Two hooks straddle the expansion in the same `prompt()` call stack,
so the bridge pairs them:

```
input                (agent-session.js:810)  → original typed text
before_agent_start   (agent-session.js:882)  → expanded text (event.prompt)
```

Emitted from the bridge's `before_agent_start` handler, once per expanded
invocation, never for an ordinary prompt or an unexpanded `/hv-*`:

```jsonc
{ "kind": "hv.prompt-template",
  "name": "review",                       // basename of the template file
  "typed": "/review src/foo.ts",          // what the user wrote
  "expanded": "Review src/foo.ts for …" } // what the model received
```

Rides `ui.notify` (the ui-request channel, `method:"notify"`), like `hv.audit` and
`hv.skill` — never RPC stdout.

Main logs `{ type: "prompt-template.invoked", data: { sessionId, typed, expandedHash } }`
(sha256 of the expansion) and forwards the envelope to the renderer.
`applyPromptTemplatePair` folds it into the transcript; `restore.ts` re-pairs after a
reload by hashing each restored user message. **Hash, never ordinal** — ask-user
answers and queued messages also produce user messages.

**The `input` handler fails OPEN and must stay that way.** It sits in front of
every user prompt. Pi already wraps input handlers (`runner.js:933-955`) and a
handler returning `undefined` continues normally; only `{action:"handled"}`
swallows a prompt. The bridge wraps its own body too. `tests/prompt-templates-bridge.test.ts`
asserts a plain non-slash prompt still completes **before** it asserts anything
about the feature — if that goes red, revert the hook, not the test.

---

## Agent terminals — hv.terminal-* + hv.terminal (§26 part 2, 2026-08-07)

Three registered tools, each a thin shell over a **blocking** `ctx.ui.input`
envelope. Main owns the PTYs (part 1), so main owns the cap, the busy-reuse
refusal and the interleave hold; the bridge only carries the request and turns
the reply into a tool result.

### The three blocking inputs (bridge → main → tool result)

Sent as `ctx.ui.input(JSON.stringify(envelope), "")`, so the payload is in
**`title`** (a notify uses `message` — that asymmetry is the single easiest
thing to get wrong here, and it costs you a card that never appears).

```jsonc
{ "kind": "hv.terminal-run",  "command": "npm run dev", "terminalId": "t1?", "intent": "…" }
{ "kind": "hv.terminal-read", "terminalId": "t1", "lines": 200, "waitMs": 3000 }
{ "kind": "hv.terminal-kill", "terminalId": "t1" }
```

Main **always** answers, with a JSON string:

```jsonc
{ "ok": true,  "terminalId": "t1", "title": "zsh" }                                  // run
{ "ok": true,  "text": "…", "running": true, "exitCode": null, "userTyped": false }  // read
{ "ok": true }                                                                        // kill
{ "ok": false, "reason": "…" }                                                        // any of them
```

`terminalReply` (bridge) maps `ok:false` to the reason as the tool's text. **A
dropped response hangs the turn** — same contract, and same reason, as
`hv.plan-write`.

`intent` rides the run envelope only so main can echo it on the notify below;
the permission prompt was built from the factual `summarize()` long before, and
must never show it (§13's MCP rule).

### hv.terminal — card lifecycle (fire-and-forget notify, payload in `message`)

```jsonc
{ "kind": "hv.terminal", "stage": "started", "terminalId": "t1", "title": "zsh",
  "command": "npm run dev", "intent": "…", "workspaceId": "/abs/ws" }
{ "kind": "hv.terminal", "stage": "killed",  "terminalId": "t1" }
```

There is deliberately no `exited` stage: the renderer already learns that from
`hv:term-exit` on part 1's push channel, and the card stack is derived from the
live `terminals` map, so an exited terminal leaves it with no extra event.

### Gate placement (all three must be NAMED — nothing is automatic)

| Tool | `SAFE_TOOLS` | `gatePlanCall` |
|---|---|---|
| `terminal_run` | no (asks) | `BLOCKED_PLAN_TOOLS` |
| `terminal_read` | **yes** | `PLAN_PASS_TOOLS` |
| `terminal_kill` | no (asks) | `PLAN_PASS_TOOLS` |

`terminal_read` in `PLAN_PASS_TOOLS` is not cosmetic: the default is
`floor-ask`, which clamps `allow`→`ask`, so a planning agent polling a log would
raise a modal **on every poll**.

### Two guards run in `tool_call` BEFORE the permission prompt

The ordering is the mechanism, not an optimisation. A multi-line `command` is
refused (`describeCommand` labels the modal from the first segment only, so the
user would approve a string they are shown part of), and a `bash` command with a
genuine trailing `&` is blocked and redirected — **only while
`builtins.terminal` is true**, since with the group off there is no
`terminal_run` to redirect to. Both audit as `source:"terminal"`, which is also
the only way to observe them: `tool_execution_start` fires *before* tool_call
handlers, so it cannot distinguish a blocked call from an allowed one.

### ~~Registering three tools measurably dilutes a small model's tool choice~~ — RETRACTED

**This section previously claimed the terminal tools made the model stop calling
`bash`. That claim was wrong, and the retraction is kept in place because the way
it was wrong is more useful than the claim ever was.**

The original evidence (2026-08-07, deepseek-v4-flash, `touch forbidden.txt`,
counting `bash` calls) was five trials per configuration, run **sequentially**,
each in a different app state:

| Configuration | bash called |
|---|---|
| before the terminal tools existed | 3/5 |
| `builtins.terminal:false` | 2/5 |
| tools registered, no steer line | 1/5 |
| tools + steer line (first draft) | 0/5 |
| tools + steer line (bash-affirming rewrite) | 0/5 |

It reads as a clean dose-response curve. It is not one. **3/5 vs 0/5 is
p = 0.167 by a two-sided Fisher exact test** — never significant at any point,
and the monotone shape is what five-trial binomial noise looks like when you sort
the rows by the story you already believe.

Two controlled re-runs, **interleaved** so provider drift cannot masquerade as a
condition effect:

**A · No bridge, only a probe extension registering K tools (n=8 per cell).**
Isolates the tool list from everything else — no gate, no steer line, no MCP.

| Condition | bash called |
|---|---|
| 0 extra tools | 1/8 |
| 3 unrelated tools (calendar/contacts/notes, neutral wording) | 4/8 |
| 3 terminal tools, wording that never names bash | 4/8 |
| 3 terminal tools, the **shipped** wording | **7/8** |

**B · The shipped configuration — real bridge, only `builtins.terminal` toggled
(n=12 per arm).** `off` 4/12 · `on` 6/12.

So: the effect does not exist in the direction claimed, and the one significant
comparison runs the **opposite** way.

| Comparison | p (2-sided Fisher) |
|---|---|
| original claim, 3/5 vs 0/5 | 0.167 — not significant |
| 0 tools vs 3 shipped-wording tools | **0.010 — significant, more tools ⇒ MORE bash** |
| 0 tools vs 3 unrelated tools | 0.282 |
| neutral wording vs bash-mentioning wording | 0.282 |
| shipped config, group off vs on | 0.680 — no detectable effect |

There is also a confound the original never controlled for: `terminal_run`'s own
description says *"instead of backgrounding a bash command"* and `terminal_read`'s
says *"instead of sleeping in bash"*. The "tools, no steer line" row therefore
still disparaged bash — descriptions **are** wording. The "wording versus count"
question the original claimed to have answered was never actually asked.

**What stands.** The prompt changes in `tests/bridge.test.ts` and
`tests/rules-bridge.test.ts` stay, on their own merit rather than the retracted
one: at the shipped baseline the model calls `bash` on roughly half of attempts,
so `askUntil`'s three tries left those tests failing ~12% of the time. The
explicit wording measured 5/5. That is a flakiness fix, not a dilution fix.

**The transferable lesson is about method, not tools.** Five trials per cell,
run sequentially while other things changed, produced a table that looked
causal, survived my own review, and shipped into three documents. What broke it
was interleaving the conditions and computing a p-value — both cheap, neither
done the first time. Before recording a behavioural claim about a model here:
interleave the arms, hold everything else fixed, and if the result would not
survive a Fisher exact test, write down the uncertainty instead of the claim.

---

## Embedded browser — hv.browser-* + hv.browser (§28, 2026-08-15)

Ten registered tools, each a thin shell over a **blocking** `ctx.ui.input`
envelope, exactly like §26's terminals. Main owns the `WebContentsView`, so main
owns the cap (one pane per session), the egress gate and every lifecycle event;
the bridge carries the request and turns the reply into a tool result.

### The ten blocking inputs (bridge → main → tool result)

Payload in **`title`** (a notify uses `message`). Same asymmetry, same trap.

```jsonc
{ "kind": "hv.browser-open",        "url": "http://localhost:5173", "intent": "…" }
{ "kind": "hv.browser-navigate",    "url": "https://example.com",   "intent": "…" }
{ "kind": "hv.browser-screenshot",  "intent": "…" }
{ "kind": "hv.browser-get-text" }
{ "kind": "hv.browser-read-console","lines": 100 }
{ "kind": "hv.browser-read-network","limit": 50 }
{ "kind": "hv.browser-click",       "selector": "#save", "intent": "…" }
{ "kind": "hv.browser-type",        "selector": "#q", "text": "hello", "intent": "…" }
{ "kind": "hv.browser-evaluate",    "code": "document.title", "intent": "…" }
{ "kind": "hv.browser-close",       "intent": "…" }
```

Main **always** answers, with a JSON string:

```jsonc
{ "ok": true,  "browserId": "b1", "text": "The browser is open at …" }        // open
{ "ok": true,  "text": "…", "untrusted": true }                              // every read/act
{ "ok": true,  "text": "…", "imageBase64": "iVBOR…" }                        // screenshot, vision model
{ "ok": false, "reason": "This session has no browser open. …" }             // any of them
```

Two fields only this family has:

- **`untrusted: true`** ⇒ `browserReply` prefixes `UNTRUSTED_BANNER` (hv-browser.ts).
  Page bytes must never reach the model dressed as our own words. Prompt
  injection has no mechanical fix; labelling plus the permission gate on what the
  injected text talks the model into is the whole mitigation (§28).
- **`imageBase64`** ⇒ an `{type:"image"}` content block **in addition to** the
  text. Verified, not assumed: `AgentToolResult.content` is
  `(TextContent | ImageContent)[]` in
  `pi-runtime/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-agent-core/dist/types.d.ts:316-320`,
  so an extension tool CAN hand a model a picture. Main only sets it when the
  session's model advertises `input: ["image", …]` in Pi's registry — otherwise
  the result says so and points at `browser_get_text`. The **user sees the
  screenshot either way**, via the notify below.

### hv.browser — card lifecycle (fire-and-forget notify, payload in `message`)

```jsonc
{ "kind": "hv.browser", "stage": "opened",     "browserId": "b1", "url": "…", "intent": "…", "workspaceId": "/abs/ws" }
{ "kind": "hv.browser", "stage": "navigated",  "browserId": "b1", "url": "…", "intent": "…" }
{ "kind": "hv.browser", "stage": "screenshot", "browserId": "b1", "dataUrl": "data:image/png;base64,…" }
{ "kind": "hv.browser", "stage": "acted",      "browserId": "b1", "action": "click|type|evaluate", "detail": "…" }
{ "kind": "hv.browser", "stage": "closed",     "browserId": "b1" }
```

`opened` is what places the TAB (renderer, `openBrowserTab`) — main creates the
pane, but where it goes on screen is a layout decision and stays in the renderer.

### Gate placement — navigation gates per HOST, not per tool

| Tool | rule name | `SAFE_TOOLS` | `gatePlanCall` |
|---|---|---|---|
| `browser_open` / `browser_navigate` | **`browser:<host>`** | no | *(unnamed)* → `floor-ask` |
| `browser_get_text` / `_read_console` / `_read_network` / `_screenshot` | own name | **yes** | `PLAN_PASS_TOOLS` |
| `browser_close` | own name | **yes** | `PLAN_PASS_TOOLS` |
| `browser_click` / `_type` / `_evaluate` | own name | no (asks) | `BLOCKED_PLAN_TOOLS` |

`browserRuleName(url)` (hv-browser.ts) builds `browser:<host>` — the same
virtual-name trick as `mcp:<server>_<tool>`, so a user's
`deny browser:*.stripe.com` is an ordinary tool-layer rule in the engine that
already exists. **Consequence worth knowing:** "Allow for session" grants the
HOST, so it says nothing about the next one.

`localhost` / `127.0.0.1` / `[::1]` / `0.0.0.0` are a **safe-default allow**
(exact hostname match — `localhost.evil.com` is a real remote host and does not
qualify), evaluated after the rule engine so an explicit `browser:localhost` rule
still wins. Without it the dev-preview loop would prompt on every page load,
which is the whole feature.

`browser_open`/`browser_navigate` are deliberately **absent from both plan sets**
so they land on `floor-ask`: opening documentation to read it is legitimate
planning, and a GET the user approves per call is not a mutation.

### The egress gate is on the PARTITION, not on the tool call

`session.fromPartition("persist:hv-browser").webRequest.onBeforeRequest`
(browsers.ts) is the enforcement point, because a tool-level gate is bypassable
by the page: one `location.href` from injected JS and `browser_navigate` never
ran. Decisions come from the pure `EgressState` (browserEgress.ts).

- **main-frame** → gated. Refused ⇒ `{cancel:true}` **and** the pane goes to
  `state:"blocked"` with the host, which the renderer renders as an Allow button.
  Clicking it is user consent (§28) and needs no second prompt.
- **subresources of an allowed page** → run silently, but every one is recorded
  in the ring `browser_read_network` serves.
- **redirects inherit** the approval of the navigation that started them, or
  every IdP bounce would break.

Honest limit, stated in §28 and repeated here so nobody over-claims it: in-page
`fetch` reaches any domain the page can. The headline is "the agent's browser
only NAVIGATES where you allow", not "no byte leaves". `browser_evaluate`'s
stricter rule is the companion mitigation.

**One hook per partition.** Electron REPLACES `onBeforeRequest` rather than
stacking, so installing it per pane would silently disarm every pane but the
newest — hence `egressInstalled` + a `webContents.id → entry` map.

### The guest has no privileges, and the picker is why that matters

`sandbox: true`, `contextIsolation: true`, no `nodeIntegration`, **no preload**,
`setPermissionRequestHandler`/`setPermissionCheckHandler` deny everything,
`will-navigate` blocks non-http(s), `setWindowOpenHandler` denies popups (a
`new-window` disposition goes to the OS browser instead). The element picker is
therefore **injected** via `executeJavaScript` (browserPicker.ts) rather than
preloaded — it reads geometry and DOM, i.e. nothing the page did not already
have. Its payload enters the COMPOSER as a chip the user then sends, so it gets
no untrusted banner: the human authors that turn, which is exactly the line the
tool-result rule above draws.

### The composited view has no z-index — visibility IS the z-order

A `WebContentsView` paints over the whole renderer DOM. `BrowserTab` therefore
hides it whenever `document.querySelector(".hv-overlay")` matches (one
MutationObserver on `document.body`), which is what keeps the **permission modal
on top of the page it is asking about**. Every Radix overlay in the app already
carries that class, so a dialog written next year inherits the behaviour.
