# Feedbacks v11 (PRD round 15) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the 16 locked decisions of feedback round 15 (Notion "Feedbacks v11", mirrored in `docs/prd.md` §7/§11/§17/§19/§21 as "Decision (Feedback round 15, 2026-08-16)").

**Architecture:** Renderer-heavy round: tool-card/chat-surface polish, transcript timestamps + scroll behaviour, sidebar session rows, in-pane cost/context panels. Three main-side pieces (empty-session delete, one-shot audit entries, audit read) and one bridge change (bypass audit source + would-have verdict — touches `pi-runtime/extensions/`, so the live-Pi batch runs).

**Tech Stack:** React/TS renderer, Electron main, vitest, the HappyVibe bridge extension.

**Spec:** `docs/prd.md` round-15 decisions (§7 ×6, §11, §17 ×3, §19, §21) + the rewritten Notion "Feedbacks v11" page (3bcd33dfffca803f8cb1d0de5f43bde8).

## Global Constraints

- Streaming text stays OUT of the transcripts array; tool cards update via toolIndex map (CLAUDE.md renderer perf invariants).
- No new dependencies. No virtualization library — tail-render only.
- Icons are inline SVG (strict `script-src 'self'` CSP; no icon lib).
- Every icon that replaces a word carries `title` (tooltip) + `aria-label`.
- The permission modal NEVER shows model intent (factual summaries only) — untouched by this round.
- The §19 ledger keeps meaning "what sessions cost": one-shot estimates are tokens-only, labelled estimated, never dollars (no price table exists in main and none may be built).
- Gate: `npm run gate` (build → non-live suite). Task 14 touches `pi-runtime/extensions/` ⇒ `npm run live:why` will print ⇒ run `npm run test:live` once at the end (background it; ~6 min; only alongside non-tree work).
- Never pipe a test run to tail/grep — redirect to a log file and grep the file.

---

### Task 1: Model chip reads active

**Files:**
- Modify: `src/renderer/src/components/ChatView.tsx:616` (the ModelSelect `renderTrigger` button)

**Interfaces:** none (className-only).

- [ ] **Step 1: Restyle the trigger.** Replace the trigger button's className so the chip matches its top-bar neighbours (the search `⌕` button two siblings down is the reference):

```tsx
className="max-w-44 text-left font-mono text-[11px] rounded-full border-2 border-line bg-card px-2.5 py-1 text-ink hover:border-honey cursor-pointer transition-colors"
```

- [ ] **Step 2: Verify.** `npm run typecheck` passes. GUI assertion in Task 16.
- [ ] **Step 3: Commit** `fix(chat): the model chip reads as a control, not a disabled label`

### Task 2: Pane `+` menu gets icons

**Files:**
- Modify: `src/renderer/src/components/TabStrip.tsx:346-431` (the `+` menu `Item` component and its four call sites)

**Interfaces:** `Item` gains `icon: React.ReactNode`.

- [ ] **Step 1:** Add an `icon` prop to `Item`, rendered as a `size-4 shrink-0 text-ink-soft` leading slot. Reuse the glyphs TabStrip already draws on tabs of each kind (chat/terminal/browser/file — they exist in this file for tab labels; if a kind has none, copy the matching `ICON_PATHS` path from `ToolCard.tsx` as an inline SVG). Wire: New session → chat glyph, New terminal → terminal, New browser → globe, Open file… → file.
- [ ] **Step 2:** `npm run typecheck`. GUI assertion in Task 16.
- [ ] **Step 3: Commit** `feat(ui): the pane + menu shows what each row opens`

### Task 3: Browser and terminal panes draw the pane divider

**Files:**
- Modify: `src/renderer/src/App.tsx` (~2270 terminal map, ~2302 browser map — pass a `dividerClass` computed by the existing `paneDivider(area)` at `App.tsx:1985`)
- Modify: `src/renderer/src/components/TerminalTab.tsx` / `TerminalView.tsx` (whichever owns the grid-area root), `src/renderer/src/components/BrowserTab.tsx` — accept `dividerClass?: string`, append to the root container className.

**Interfaces:** Produces: `dividerClass?: string` prop on both components.

- [ ] **Step 1:** Thread `paneDivider(area)` into both components' roots. Do NOT re-derive edges inside the components — `BrowserTab` already has `edges` for the view inset, but the drawn border must come from the same `paneDivider` the other three pane kinds use, or the five drift.
- [ ] **Step 2:** `npm test -- tests/tabs.test.ts` still green (no tabs.ts change expected); `npm run typecheck`.
- [ ] **Step 3: Commit** `fix(ui): browser and terminal panes draw the divider the other panes always had`

### Task 4: Sidebar settings-split handle matches the pane divider

**Files:**
- Modify: `src/renderer/src/components/Sidebar.tsx:713-716` (the `h-1.5 cursor-row-resize` handle)

- [ ] **Step 1:** Replace the thin bar with the PaneDividers treatment (`App.tsx:2697-2710` is the reference): a 10px hit strip (`h-2.5 group cursor-row-resize`) containing a centred 5px tint child (`h-[5px] bg-transparent group-hover:bg-tangerine/40 transition-colors`). Keep the existing mouse handlers on the outer strip.
- [ ] **Step 2:** `npm run typecheck`. GUI assertion in Task 16.
- [ ] **Step 3: Commit** `fix(sidebar): the settings handle is the same divider as the pane one`

### Task 5: File panel loses its close icon

**Files:**
- Modify: `src/renderer/src/components/FileTree.tsx:309` (delete the `⇥` HeaderBtn), plus its now-unused `onClose` prop if nothing else consumes it — check callers in `App.tsx` first; the drawer's outside behaviour (rail toggle, ⌘⇧E) is untouched.

- [ ] **Step 1:** Remove the button. If `onClose` becomes dead, remove the prop and its App call site.
- [ ] **Step 2:** `npm run typecheck`; `npm test -- tests/` files touching FileTree if any (grep `FileTree` under tests/ first).
- [ ] **Step 3: Commit** `fix(files): one entry point, one exit — the rail button, twice`

### Task 6: Tool cards go icon-first (every word → icon, text on hover)

**Files:**
- Modify: `src/renderer/src/components/ToolCard.tsx` (STATUS map, DetailsToggle, approval/denied/skipped/destructive badges; same treatment in `SubagentCard`'s status word)
- Test: `tests/tool-card-icons.test.ts` (new, jsdom — the repo's renderer tests show the pattern; see `tests/agents-renderer.test.ts`)

**Interfaces:** none exported; visual contract only.

- [ ] **Step 1: Write the failing test** — render a done card and assert no visible status WORD, but an element with `aria-label="done"`; render the details toggle and assert `aria-label="Show details"`; render a denied card and assert `aria-label="denied"` exists and the text "DENIED"/"denied" is not rendered as visible text.

```tsx
import { render, screen } from "@testing-library/react";
import { ToolCard } from "../src/renderer/src/components/ToolCard";
test("status and badges are icons with hover text, not words", () => {
  render(<ToolCard card={{ toolCallId: "t1", toolName: "read", args: { path: "a.ts" }, status: "done" }} />);
  expect(screen.queryByText(/^done$/i)).toBeNull();
  expect(screen.getByLabelText("done")).toBeTruthy();
  expect(screen.getByLabelText("Show details")).toBeTruthy();
});
```

- [ ] **Step 2:** Run `npx vitest run tests/tool-card-icons.test.ts > /tmp/v.log 2>&1; echo EXIT=$?` — expect FAIL.
- [ ] **Step 3: Implement.**
  - `STATUS` map: keep the coloured dot; drop the trailing `{s.label}` word-span; give the DOT the `title={s.label}` + `aria-label={s.label}` (running keeps `animate-pulse`). Add per-status glyph inside/beside the dot where colour alone is ambiguous: done ✓ (check path), error/denied ✕, skipped ⏭ — inline SVG, `size-3`.
  - `DetailsToggle` → a `⋯` icon button (three-dot SVG or the literal `⋯` glyph at `text-base leading-none`), `title="Show details"/"Hide details"`, `aria-label` matching.
  - Badges: `allowed` → ✓-in-circle green icon `title="Allowed by you"`; `session pass` → ✓ with clock `title="Allowed for this session"`; `denied` → ✕ red `title="Denied"`; `not in plan mode` → 🧭-style compass SVG honey `title="Skipped — not allowed in plan mode"`; `destructive` → triangle-! red `title="Destructive command"`. All `shrink-0 size-4`.
  - `SubagentCard`'s status word (`delegating…/denied/failed/dispatched/done`) gets the same dot+title treatment.
- [ ] **Step 4:** Test passes; `npm test -- tests/tool-label.test.ts tests/agents-renderer.test.ts tests/tool-card-icons.test.ts` green (fix any test asserting the old words).
- [ ] **Step 5: Commit** `feat(chat): tool cards speak in icons, words on hover`

### Task 7: Intent leads on browser cards

**Files:**
- Modify: `src/renderer/src/toolLabel.ts:182-187` (browser_open/browser_navigate case)
- Test: `tests/tool-label.test.ts` (existing)

**Interfaces:** `ToolLabel.path` reused as the URL chip — `PathActions` (ToolCard.tsx:231) already renders a non-workspace-resolvable `raw` as a plain mono span, so a URL needs zero new machinery.

- [ ] **Step 1: Write the failing tests** in `tests/tool-label.test.ts`:

```ts
test("browser_open leads with intent, URL demoted to the chip", () => {
  const l = toolLabel("browser_open", { intent: "Opening the game to test it.", url: "http://localhost:8000/x.html" });
  expect(l.label).toBe("Opening the game to test it.");
  expect(l.path).toBe("http://localhost:8000/x.html");
});
test("browser_open without intent keeps the derived URL headline", () => {
  const l = toolLabel("browser_open", { url: "http://localhost:8000/x.html" });
  expect(l.label).toMatch(/^Opening localhost:8000/);
});
```

- [ ] **Step 2:** Run — expect FAIL.
- [ ] **Step 3: Implement:** in the shared case, `if (intent) return { icon: "globe", label: intent, path: url ?? undefined };` before the URL-derived return. Update the block comment: card = intent-first (round 15); the permission prompt still summarizes as the URL (unchanged, `summarize()` in the bridge).
- [ ] **Step 4:** `npm test -- tests/tool-label.test.ts` green.
- [ ] **Step 5: Commit** `feat(chat): browser cards lead with the model's intent; the URL becomes the chip`

### Task 8: describeCommand vocabulary grows

**Files:**
- Modify: `src/renderer/src/describeCommand.ts`
- Test: `tests/describe-command.test.ts` (or wherever its existing tests live — `grep -rl describeCommand tests/`)

- [ ] **Step 1: Write failing tests** for the common fallbacks: `git status` → "Checking git status", `git add -A` → "Staging changes", `git commit -m "x"` → "Committing changes", `git push` → "Pushing to the remote", `git pull`/`git fetch`, `git log` → "Reading git history", `git diff` → "Comparing changes", `mkdir -p x` → "Creating directory x", `curl https://a.b` → "Fetching a.b", `node x.js` / `python x.py` → "Running x.js", `cp a b`/`mv a b` → "Copying/Moving a", `touch a` → "Creating a", `cat a` / `head`/`tail` → "Reading a", `chmod +x a` → "Changing permissions of a", `echo x > f` keeps existing handling. Follow the file's existing label voice (gerund + object) and destructive-flag conventions.
- [ ] **Step 2:** FAIL run, then implement as data-table entries in the file's existing pattern (no new parser).
- [ ] **Step 3:** `npm test -- <that test file>` green.
- [ ] **Step 4: Commit** `feat(chat): more bash commands get a human headline instead of "Running: …"`

### Task 9: Sending scrolls, opening lands at the bottom, long sessions tail-render

**Files:**
- Modify: `src/renderer/src/components/Transcript.tsx` (scroll + tail logic), `src/renderer/src/components/ChatView.tsx` (submit → scroll signal)
- Test: `tests/transcript-tail.test.ts` (new — pure helper)

**Interfaces:**
- Produces: `visibleTail(items: TranscriptItem[], expanded: boolean, searching: boolean, TAIL = 50): { shown: TranscriptItem[]; hiddenCount: number }` exported from `Transcript.tsx`.
- Produces: `Transcript` accepts `scrollNonce: number` — bump forces scroll-to-bottom (ChatView bumps it on its own submit; initial mount always bottoms).

- [ ] **Step 1: Failing test** for the pure helper:

```ts
test("tail hides all but the last 50, except while searching or expanded", () => {
  const items = Array.from({ length: 120 }, (_, i) => ({ kind: "user", text: `m${i}` }) as TranscriptItem);
  expect(visibleTail(items, false, false).shown).toHaveLength(50);
  expect(visibleTail(items, false, false).hiddenCount).toBe(70);
  expect(visibleTail(items, true, false).shown).toHaveLength(120);
  expect(visibleTail(items, false, true).shown).toHaveLength(120); // search must see everything
  expect(visibleTail(items.slice(0, 10), false, false).hiddenCount).toBe(0);
});
```

- [ ] **Step 2:** FAIL, then implement. Wire into `Transcript`: when `hiddenCount > 0`, render a "Show earlier messages (N)" row above the tail (reuse the §9 boundary row's styling), clicking sets `expanded`. IMPORTANT: the §9 compaction boundary + `outOfContext` dimming must stay intact — the tail slices the ALREADY-assembled item list, so the boundary bubble may itself be hidden until expanded; that is fine (it is item #1 of a 300-item session). The in-conversation SEARCH walks the rendered DOM (`Transcript.tsx:406` TreeWalker), so pass `searching = !!query` into the helper — all items render while a query is live.
- [ ] **Step 3: Scroll.** On mount and whenever `scrollNonce` changes: `bottom.current?.scrollIntoView({ block: "end" })` unconditionally (no `isNearBottom` guard — the guard stays ONLY on the stream-follow effect at line 384). ChatView's `submit()` bumps the nonce after `onSend`.
- [ ] **Step 4:** `npm test -- tests/transcript-tail.test.ts` green; full `npm test` for regressions.
- [ ] **Step 5: Commit** `feat(chat): send scrolls to the bottom, reopen lands there, and 300-item sessions render 50`

### Task 10: The transcript knows what time it is

**Files:**
- Modify: `src/renderer/src/components/Transcript.tsx` (TranscriptItem gains `ts?: number`, assistant gains `turnMs?: number`; render), `src/renderer/src/App.tsx` (stamp on send + on commit; stamp turn duration at agent_end), `src/renderer/src/streaming.ts` (commit path carries ts), `src/main/history.ts` + `src/main/restore.ts` + `src/renderer/src/restoreMap.ts` (thread the session file's `timestamp` through to restored items)
- Create: `src/renderer/src/timeago.ts` — shared by Transcript and Sidebar (Task 12)
- Test: `tests/timeago.test.ts` (new), `tests/restore-map.test.ts` (existing — extend)

**Interfaces:**
- Produces: `timeago(tsMs: number, nowMs?: number): string` → `"now"`, `"5m"`, `"3h"`, `"2d"`, `"4mo"` (used bare in the sidebar, suffixed " ago" in the transcript).
- Produces: `TranscriptItem` user/assistant variants gain `ts?: number` (epoch ms); assistant gains `turnMs?: number`.
- Consumes: `RawMessage.timestamp` (`src/main/restore.ts:42` — verify unit: Pi writes ISO strings or epoch; normalize to epoch ms in `restoreItems`).

- [ ] **Step 1: Failing tests**

```ts
test("timeago buckets", () => {
  const now = Date.parse("2026-08-16T12:00:00Z");
  expect(timeago(now - 30_000, now)).toBe("now");
  expect(timeago(now - 5 * 60_000, now)).toBe("5m");
  expect(timeago(now - 3 * 3_600_000, now)).toBe("3h");
  expect(timeago(now - 2 * 86_400_000, now)).toBe("2d");
  expect(timeago(now - 70 * 86_400_000, now)).toBe("2mo");
});
```

  And in `tests/restore-map.test.ts`: a restored user message carries `ts` from the raw entry's timestamp; a restored assistant message following it carries `turnMs = tsAssistant - tsUser` (only when both exist and it is the turn's LAST assistant message — pair on the same walk `toTranscriptItems` already does).
- [ ] **Step 2:** FAIL, implement `timeago.ts`, thread `ts` through `restoreItems` → `toTranscriptItems`. Live path: App stamps `ts: Date.now()` when pushing the user item on send, and at the agent_end commit sets `turnMs` on the LAST assistant item of the turn (delta from the stamped user ts; steers mid-turn do not reset it).
- [ ] **Step 3: Render.** User bubble: `<span title={new Date(ts).toLocaleString()}>{timeago(ts)} ago</span>` — `text-[10px] text-ink-soft`, bottom-right beside the copy icon. Turn duration: on an assistant item with `turnMs`, append `· {formatDuration(turnMs)}` (`34s`, `2m 10s`) in the same muted style at the message end. Missing ts (old live items) renders nothing.
- [ ] **Step 4:** `npm test -- tests/timeago.test.ts tests/restore-map.test.ts` green; `npm run typecheck`.
- [ ] **Step 5: Commit** `feat(chat): prompts say when, turns say how long`

### Task 11: The cost and context panels anchor below the chat top bar

**Files:**
- Modify: `src/renderer/src/components/ContextPanel.tsx:77` and `src/renderer/src/components/CostPanel.tsx:56` (the `fixed inset-0 … justify-end bg-ink/20` wrappers), `src/renderer/src/components/ChatView.tsx` (give the pane content wrapper `relative`; render the panels inside it)
- Test: none unit-testable beyond typecheck — GUI assertions in Task 16 are the verification.

**Interfaces:** none — the panels' props are unchanged.

- [ ] **Step 1:** In ChatView, the panels currently render at the root (lines ~1370-1383). Move them inside the scroll-region wrapper that sits BELOW the top bar, and change each panel's outer wrapper from `fixed inset-0 z-40 flex justify-end bg-ink/20` to `absolute inset-y-0 right-0 z-30 w-[min(28rem,100%)] border-l-2 border-line shadow-sticker-lg` (no scrim; the panel itself keeps its bg). Outside-click close: replace the scrim's `onMouseDown` with a `fixed inset-0` transparent click-catcher *inside the pane*? No — simplest correct: keep close on the panel's ✕ and add an `absolute inset-0` transparent catcher behind the panel within the pane wrapper. Esc keeps working if it did.
- [ ] **Step 2:** Check the internal confirm dialog (`ContextPanel.tsx:439` compaction confirm, `fixed inset-0 z-50`) still overlays the whole window — it is a modal and stays `fixed` (do not convert it).
- [ ] **Step 3:** `npm test -- tests/modal-layer.test.ts` — this test scans z-index discipline; the panels dropping from the overlay class must not break it. `npm run typecheck`.
- [ ] **Step 4: Commit** `feat(chat): cost and context open below the bar that owns them, inside their pane`

### Task 12: The session row — one trash, a two-outcome dialog, an age

**Files:**
- Modify: `src/renderer/src/components/Sidebar.tsx` (SessionRow lines 240-367 + the confirm at ~762)
- Test: `tests/sidebar-row.test.ts` (new, jsdom) — reuse `timeago` from Task 10.

**Interfaces:** Consumes `timeago` (Task 10). SessionRow props unchanged (`onArchive`/`onDelete` both stay — the dialog calls one or the other).

- [ ] **Step 1: Failing test:** at rest a row shows the age (`"3h"`, from `session.updatedAt`) and NO buttons; a running session (status "running") shows no age (the pulse dot already animates); hovering (simulate with the `group-hover` class assertion or fire mouseOver state) reveals exactly ONE button, labelled "Archive or delete"; clicking it opens a dialog with BOTH "Archive" and "Delete permanently".
- [ ] **Step 2:** FAIL, implement: replace the two hover icons with one TrashIcon button (`title="Archive or delete"`); the resting slot renders `<span className="text-[10px] text-ink-soft group-hover:hidden">{timeago(updatedAt)}</span>` and the button gets `hidden group-hover:flex`. Extend the existing `confirmDelete` CompactDialog to three actions: **Archive** (calls `onArchiveSession(id, true)`), **Delete permanently** (existing destructive style + copy), Cancel. An archived session's dialog offers **Unarchive** instead of Archive. "Show archived" section unchanged.
- [ ] **Step 3:** `npm test -- tests/sidebar-row.test.ts` green.
- [ ] **Step 4: Commit** `feat(sidebar): one trash, a real choice, and rows that say how old they are`

### Task 13: Empty sessions delete themselves; ⌘W closes any tab

**Files:**
- Modify: `src/main/SessionManager.ts` or `src/main/ipc.ts` (wherever `closeSession`/`endSession` and the quit path live — `ipc.ts:1799`), `src/main/store.ts` (helper), `src/renderer/src/App.tsx` (`closeChatTab` ~1481, `closeTab` keydown ~1924), `src/renderer/src/shortcuts.ts:42` (label)
- Test: `tests/empty-session.test.ts` (new, main-side pure), plus extend whatever tests cover the close IPC if any.

**Interfaces:**
- Produces (main): `isSessionEmpty(meta: SessionMeta): boolean` — true when the session file is absent OR contains no user-role message entry (read with the same parser `restore.ts`/`history.ts` already use; do NOT invent a second reader).
- Produces (IPC): `hv:close-session` gains nothing — main checks `isSessionEmpty` itself after ending and deletes via the existing `deleteSessionFile` path, emitting the existing `session.delete` event so the renderer list refreshes.

- [ ] **Step 1: Failing test** (`tests/empty-session.test.ts`): a meta with no `piSessionFile` → empty; a fixture session file with only session-bookkeeping entries → empty; one with a user message → not empty. Fixtures: copy the smallest real shapes from existing history fixtures (`tests/history-*` fixtures dir).
- [ ] **Step 2:** FAIL, implement `isSessionEmpty`, call it at the end of `endSession` when the close came from a tab close (add a `deleteIfEmpty?: boolean` arg threaded from `hv:close-session`; archive/hibernate/MCP-reload paths pass nothing) and in the app-quit path (`before-quit` / wherever sessions are ended on quit — sweep ALL workspaces' metas, not just live children: an empty session with no process still gets deleted). Workspace switch touches none of this (it never ends sessions — verify, don't change).
- [ ] **Step 3: ⌘W.** In the `is("closeTab")` handler (App.tsx:1924): remove the chat exemption — `if (isChatTab(active)) void closeChatTab(wsId, slot, active)` (it already confirms when busy and asks about terminals). Keep preferring the focused pane; drop the fall-through-to-other-panes scan for chats (keep it as-is for the rest). Update the shortcut label at `shortcuts.ts:42` to `"Close the active tab"`.
- [ ] **Step 4: Busy gate everywhere.** `closeChatTab` already `window.confirm`s when `busy[sid]` — confirm the same guard covers the ⌘W route (it does, same function) and that terminal/browser closes are unaffected.
- [ ] **Step 5:** `npm test -- tests/empty-session.test.ts` green; full `npm test`.
- [ ] **Step 6: Commit** `feat(sessions): an empty session leaves nothing behind, and ⌘W closes whatever is in front of you`

### Task 14: Audit — bypass stops flooding, and records what would have happened

**Files:**
- Modify: `pi-runtime/extensions/happyvibe-bridge.ts:734-736` (the dangerous/bypass branch), `src/renderer/src/components/AuditView.tsx` (render + filters), `docs/validation/d1.md` (§B4 wire shape)
- Test: extend the non-live bridge/rules unit tests (`grep -rl "hv.audit" tests/` — the key-free ones) + `tests/audit-view.test.ts` (new, jsdom) if cheap.

**Interfaces:**
- Produces (wire): `hv.audit` gains `source: "bypass"` (replacing `"dangerous"` on NEW events) and optional `wouldHave: "allow" | "ask" | "deny"` — the rule engine's verdict computed even when bypass wins. OLD persisted events keep `"dangerous"`; the renderer maps both.

- [ ] **Step 1: Failing test:** with dangerous mode on, a gated call's audit payload has `source: "bypass"` and `wouldHave` set to what `evaluateRules` returns for that call (use the existing pure-engine test harness for `hv-rules.ts` — no live Pi needed to unit-test the branch if the audit assembly is extractable; otherwise assert via the existing key-free bridge test pattern).
- [ ] **Step 2:** FAIL, implement in the bridge: in the `if (dangerous && !plan.enabled)` branch, run the same rule evaluation the non-bypass path uses (it is already in scope — the verdict function the engine branch calls) and emit `{ decision: "allow", source: "bypass", wouldHave: verdict.action }`. Type unions: extend `AuditSource` (`happyvibe-bridge.ts:367`).
- [ ] **Step 3: Renderer.** AuditView: map `"dangerous"` (old rows) and `"bypass"` to the label `bypass` in a CALM colour (`text-ink-soft`, drop the `text-berry`); when `wouldHave` present render `bypass · rules would have {asked|allowed|denied}`. Add two `<select>` filters beside the existing workspace/session ones: decision (all/allow/allow-session/deny) and source (all/rule/user/bypass/safe-default/plan/terminal) — pure client-side filter over `rows`.
- [ ] **Step 4: Docs.** d1.md §B4: add `wouldHave`, note the `dangerous→bypass` rename and that old logs retain the old string.
- [ ] **Step 5:** `npm test` green. This task touches `pi-runtime/extensions/` — note for the final gate: `npm run live:why` will print; the live batch runs in Task 16.
- [ ] **Step 6: Commit** `feat(audit): bypass is a mode, not a verdict — the log keeps the verdict too`

### Task 15: One-shot model calls appear — audit entries + a Stats line

**Files:**
- Modify: `src/main/titles.ts`, `src/main/agentsMd.ts` (draft path), `src/main/gitMessage.ts` (both `draftCommitMessage` and the PR draft), `src/main/ipc.ts` (they need the `EventLog` instance — thread it or import the existing singleton the way `permission.decision` logging does at `ipc.ts:1052`), `src/renderer/src/components/AuditView.tsx` (show the rows), `src/main/analytics.ts` + `src/renderer/src/components/DashboardView.tsx` (Stats line), `src/main/ipc.ts:2622` (readAudit reads both types or a second channel)
- Test: `tests/oneshot-audit.test.ts` (new)

**Interfaces:**
- Produces (EventLog): type `"assistant.oneshot"`, data `{ kind: "title" | "agents-md" | "commit-message" | "pr-draft", model: string, estTokens: number, ok: boolean }`. `estTokens = Math.ceil((promptChars + outputChars) / 4)`. NO dollars anywhere (no price table in main; the ledger rule).
- Produces (Stats): analytics summary gains `oneshot: { count: number, estTokens: number }`.

- [ ] **Step 1: Failing test:** calling the title generator with a stubbed spawn logs one `assistant.oneshot` event with `kind:"title"`, the model id it resolved, and `estTokens > 0`; a failed run logs `ok:false`. (The four callers share the spawn pattern; test one caller end-to-end + a shared `logOneShot` helper unit.)
- [ ] **Step 2:** FAIL, implement a tiny shared helper in `src/main/oneShotLog.ts` — `logOneShot(log, {kind, model, promptChars, outputChars, ok, sessionId?, workspaceId?})` — called from the four call sites.
- [ ] **Step 3: Audit page.** `readAudit` (ipc.ts:2622) currently filters `type: "permission.decision"`; read both types and tag rows; AuditView renders a oneshot row as `assistant · {kind} · {model} · ~{estTokens} tok (estimated)` with the same timestamp column, filterable via the Task 14 source filter (source shows as `assistant`).
- [ ] **Step 4: Stats.** analytics sums the events; DashboardView adds one quiet line "Assistant extras (estimated): N calls · ~X tok" — labelled estimated, never summed into any dollar figure.
- [ ] **Step 5:** `npm test -- tests/oneshot-audit.test.ts` + full `npm test` green.
- [ ] **Step 6: Commit** `feat(stats): the app's own model calls stop being invisible`

### Task 16: Gate + live batch + GUI pass

**Files:** none (verification).

- [ ] **Step 1:** `npm run gate > /tmp/gate.log 2>&1; echo EXIT=$?` — green.
- [ ] **Step 2:** `npm run live:why` — prints (Task 14 touched the bridge) ⇒ run `npm run test:live` in the background; do only non-tree work (this GUI pass reads, never edits) while it runs. One live failure ⇒ rerun that file in isolation before calling it a regression.
- [ ] **Step 3: GUI pass** (electron-debug attach; screenshot each assertion — a claim without a picture is not verified). **Restart the dev server first** — main-side changes (Tasks 13, 15) need it, a ⌘R does not rebuild main.

**Observable assertions, per surface:**

*Chat top bar (a session open):*
- The model chip has a visible border and card background; side-by-side with the ⌕ button they read as the same control family.
- Clicking the cost pill opens a panel INSIDE the pane, top edge below the chat top bar. **Absence:** no full-window dimming scrim; in a horizontal split, the NEIGHBOURING pane is fully visible and clickable while the panel is open. Same for the context % bubble. Regression sequence: open cost panel in pane A → click pane B's chat → open B's context panel → both panes still render their own panel, closing one leaves the other.

*Transcript (a session with a browser_open call + a long history):*
- The browser_open card's headline is the model's intent sentence; the URL sits in the small mono chip beside it. **Absence:** no headline of the form "Opening localhost:8000/…" on a card that carries an intent.
- A completed tool card shows NO words "DONE" or "DETAILS". **Absence assertion:** the strings `DONE` and `DETAILS` appear nowhere on a completed card; a ⋯ button and a ✓-marked status are present; hovering each shows its text.
- Every user bubble shows "Nm ago" (hover → exact local time); the last assistant message of a finished turn ends with "· Ns".
- Open a session with >50 items: view lands at the BOTTOM, a "Show earlier messages (N)" row sits at the top of the rendered tail; clicking reveals the rest. ⌘F search for a word that only exists in message #1 still finds and highlights it (search auto-expands).
- Scroll to the top, send a prompt → the view jumps to the bottom.

*Panes:*
- Chat | browser side-by-side: a 2px line separates them; browser | browser same. **Absence:** no unmarked cream-on-cream seam between any two panes.
- The pane `+` menu: all four rows carry an icon.

*Right drawer:*
- Files panel header has NO `⇥` close button (absence — count the header buttons: new file, new folder, collapse-all only); the rail files icon still toggles it closed.

*Sidebar:*
- At rest a session row shows its age ("3h") on the right, NO icons (absence). Hover: exactly ONE trash icon (absence of the archive icon). Click → dialog offering Archive AND Delete permanently. A running session shows the pulse, no age.
- The settings-split handle highlights a thin tangerine line on hover and is grabbable ~10px around it — same feel as the pane divider.

*⌘W & empty sessions:*
- Focus a browser tab → ⌘W closes it. Focus an idle chat → ⌘W closes it (process ends — sidebar dot goes idle/asleep). Focus a MID-TURN chat → ⌘W raises the dialog naming what is running; Cancel leaves everything.
- ⌘N, then immediately ⌘W → **absence:** the "New session" row is GONE from the sidebar (deleted, not archived — check Show archived too). ⌘N, switch workspace, switch back → the empty session is STILL there (workspace switch never deletes). ⌘N in workspace A, quit the app, relaunch → **absence:** gone (quit sweep crosses workspaces).

*Audit page (bypass ON, run one gated call; then click "Write it for me" in Changes):*
- The gated call's row reads `bypass · rules would have asked` in a calm colour. **Absence:** no red "dangerous" tag on new rows. Decision + source filter selects present and working.
- A row `assistant · commit-message · <model> · ~N tok (estimated)` exists — observed on the Audit page, not the Changes panel.

*Stats page:*
- The line "Assistant extras (estimated)" with a call count and token estimate — observed on Stats, the surface that owns the number. **Absence:** no dollar figure attached to it, and the workspace cost totals are unchanged by one-shot calls.

**The one regression the design risks, as a sequence:** open a 300-item session (tail renders 50, lands at bottom) → ⌘F, search a top-of-file word (all items render) → Esc (tail collapses again? acceptable either way, but the view must NOT jump) → scroll up into history → agent streams a reply → the view must NOT yank to the bottom (the near-bottom guard on the stream is untouched) → press send on a new prompt → NOW it jumps to bottom.

- [ ] **Step 4: Commit** any GUI-pass fixes individually, then stop for /land.
